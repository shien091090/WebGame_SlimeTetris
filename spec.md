# Demo 規格 v20

## 概述
一句話概念: 在一個窄到讓人難受的盤面上疊史萊姆, 把同色黏成一團消掉; 解開地任務讓盤面往外長, 開滿之後解各種任務換來新外型的方塊。

Demo 要驗證的問題(v14 擴大範圍): **「任務」能不能讓玩家整局都有下一個想達成的目標 — 開地階段追開地, 開滿之後追新外型?**
- 失敗的樣子: 開地階段無視目標色、只是在消; 或開滿之後對新任務與新外型無感, 後段又退回「只剩落速在動」。
- **第 7 輪答案(v19): 是。** 使用者原話「有, 一直在追, 任務獎勵對我來說很重要」; 獎勵吸引力排序「削頂 > 重力球 > 分數倍率」。驗證問題不變, **觀察重點改為: 新增的清色球能不能成為最大的正向回饋, 而且不讓一局失控。**

本規格只描述玩法規則與體驗流程。基本規則(同色四方向相連 4 顆成團整團消失)自 v3 起不變。**不做系統性的重力沉降**自 v8 起不變。**重力觸發球**自 v10 起不變。**懸空結構標示**自 v11 起不變。**單一任務軌、系統自動換目標色、只用鍵盤、遊戲內說明**自 v12 起不變。

**本版相對 v19 的改動, 來源是 spec-review 第 19 版兩回合與 decisions v20(修訂紀錄 v20)**:

- **「連鎖球」改名「清色球」**: 規格內部與玩家可見文字一律用「清色球」(原「連鎖」容易被讀成連消, 而本作不存在連鎖)。獎勵事件那一句定為「能當任何顏色, 消掉它全盤同色一起消失。」
- **清色球的放置(v20 定案, 取代 v19 企劃提案)**: 只在**第 1 與第 5 個多樣化任務**給, **每局上限 2 顆**; 第 4、6 個只削頂; 第 7 個起第奇數個沒有獎勵、第偶數個削頂。每個任務最多一種減壓。v19 的「全解鎖後每個任務一顆」不採用。見「多樣化任務 / 獎勵」與「清色球」。
- **第 1 顆延後發出**: 獲得後第 15 塊一定含清色球(遇重力球排定的塊順延); 第 5 個的那顆照原規則在獲得後第 2 塊出現。
- **可以留在盤面上(寫明)**: 沒被湊成團就一直留著, 之後補成團才生效, 可能被削頂削掉。
- **全盤清除每次固定 500 分, 不乘倍率**(推翻 v19 的不計分); 仍不推任何任務; 被清掉的格不按格數計分。
- **追加消除中的清色球照常觸發全盤清除**: v19 企劃提案, v20 接受。
- **第 3 輪起「大團」與「用新形狀方塊」需求改為 3c − 2**(7 / 10…); 第 1、2 輪不變。
- **目標局長改寫(製作人裁決)**: 開滿並進入多樣化任務的局次 230~270 秒、全檔中位 200~250 秒、硬上限 320 秒。
- **局長調整順位**: 盤面高度(只往上)→ **清色球次數與位置** → 多樣化任務需求量 → 重力球出現率 → 結算呈現時長。
- **實測校正值(第 7 輪)**: 削頂每格回收 1.87(比假設低 25%); 滿寬後窗口 51~78 塊; 大團第 1 輪 7/7 達成; 每塊秒數式成立。
- **埋點**: 修正「連續 20 塊 Σ欄高淨變化最小值」(第 7 輪紀錄仍是舊定義, 本版實作必須改對); 新增逐清色球的清除色選擇、清除前後盤面、清除後 10 塊內的變化等欄位, 逐重力事件距上次全盤清除的塊數, 逐局被上限擋下的清色球數。
- **試玩**: 加問「清掉那一片之後, 你覺得有得到什麼嗎?」。
- **玩家說明**: 第 5 頁面板改為「新形狀方塊 + 清色球」; 第 6 頁改為「放不下下一塊就結束。」(見 `guide.md`)。

**未被上述牽動的一律維持 v19 原樣**: 消除與成團判定、萬用格的仲裁、無系統性沉降、重力觸發球的生成與重力事件判定、重力下落動畫、追加消除(除清色球觸發的定案)、懸空結構標示、削頂的判定方式與出現時機、延展本身、開地任務、重力下落任務、「用新形狀方塊」的計入規則與讀懂輔助、新外型的外型與生成旋轉與 bag 組成與首發保證、全盤清除的範圍與時序、方塊與下落、結束條件三條、暫停與說明畫面、操作。

## 核心循環
1. 玩家接到一個混色方塊(每格顏色各自獨立, **其中可能有一格是重力觸發球或清色球, 一塊最多一格**), 同時看得到下一塊的組成、**任務面板(做什麼 / 得到什麼 / 還差多少)**, 以及**當前所有懸空結構的標示**。
2. 玩家放下方塊; 盤面穩定後, 同色史萊姆以上下左右相連達 4 顆的團整團消失。**消失的格位變成空洞**; **除了被重力事件牽動的結構之外, 盤面其餘格位一律原地不動。**
3. **取捨只有一個, 就寫死在放置的那一刻**: 這一塊要優先湊哪一團 — 湊當前任務要的, 還是湊眼前最好消、最能整地的那團。一塊只有一個落點與一個旋轉態, 把黃格補成第 4 顆黃, 就放棄了同一塊把藍格補成第 4 顆藍。
   - **重力球不新增第二層取捨, 它讓同一層變重**: 拿到球的那一塊, 玩家要在同一個落點同時決定「用它補哪一色的第 4 顆」與「我要讓哪一塊懸空結構掉下來」。兩件事共用同一個落點, 仍是一次選擇、一個代價。**重力球沒有任何主動引爆操作。**
   - **清色球同理(v19)**: 拿到它的那一塊, 「用它補哪一色的第 4 顆」同時就是「全盤要清掉哪一色」。仍是同一個落點的一次選擇; **沒有主動引爆操作**。沒補成團就留在盤面上, 之後再補(v20)。
   - **誠實版的邊界(寫死)**: **重力事件的觸發與否由玩家決定** — 什麼時候塌、從哪一團塌, 全部寫死在放置的那一刻; **下落的精確結果(哪幾塊動、各落幾格、會不會再消一批)屬於執行後才完全確定**。盤面全程標示當前的懸空結構供判讀, **但不提供任何落點試算**; 下落用動畫演出, 讓玩家事後看懂是怎麼掉的。
4. **任何時刻只有一個當前任務**, 做到就達成、換下一個:
   - **第 1~4 個任務是開地任務**: 消掉 N 顆目標色。獎勵是盤面向外長一欄(左右自動交替); **第 1~3 個只長一欄, 不削頂(v16)**; **第 4 個(滿寬)另附削頂(每一欄最上面削掉 1 格)與 1,500 分**。
   - **第 5 個任務起是多樣化任務**: 三種任務依固定順序輪替 — **消掉一團 5 顆以上 → 放下新形狀的方塊, 當場消掉一團 → 讓重力球把史萊姆帶下來**, 每種要求不同的消法。前 3 個的獎勵是**解鎖一種新形狀方塊**, 之後的方塊會開始混入新外型; **第 1 個另給一顆清色球, 第 5 個的獎勵是一顆清色球(v20, 一局最多 2 顆)**; **第偶數個多樣化任務附削頂(v18)**; 第 7 個起的第奇數個沒有獎勵(v20)。
5. **目標色只在開地任務期間存在**, 每個開地任務被進入時系統自動換色; 進入多樣化任務後不再有目標色。
6. 落速持續上升, 每完成一個多樣化任務再上一階(封頂 2 階), 但它只是背景壓力, 不是收局機制。**真正的收局仍是盤面自己爛掉**: 空洞不會自動被填回去, 盤面逐漸蜂窩化, 有效容量單調下降。**蜂窩化的煞車(v20): 前 3 個開地任務期間只有玩家自己規劃的重力事件; 滿寬那次削頂一次, 之後每完成兩個多樣化任務(第偶數個)削頂一次; 第 1 與第 5 個多樣化任務各給一顆清色球(一局最多 2 顆); 加上重力事件。** 滿寬後的難度來源是新外型本身與落速階梯, 回收來源是削頂、清色球與重力事件。

**規則上不存在「把團留著繼續長大」這件事**: 團一碰到 4 顆立刻整團消失, 局面上永遠不存在 ≥4 顆的團(含把相鄰萬用格算進去的情況, 見「消除」節的不變式)。n≥5 只能靠單次放置一口氣跨越產生 — 這正是多樣化任務「消掉一團 5 顆以上」要玩家換的消法。

## 規則

### 盤面與延展
- 盤面初始 6 欄寬、**19 列高**, 左右各可延展 2 欄, 滿寬 10 欄。
- **列座標系**: 列 1 為最底列, **列 19 為盤面頂列**。**列 20、21 為緩衝列**。**列 22 以上不設硬界**: 列 20 起的所有格位一律不可見, 但與盤面格位一視同仁, 可構成連通團、可被消除、可被削頂削除、**也可被重力事件牽動而下落**。
- **欄座標系(寫死)**: 開局 6 欄為**絕對欄 0~5**。左側延展依序新增絕對欄 −1、−2; 右側新增 6、7。**絕對欄全局不重編號**。**相對欄 = 絕對欄 − 當前最左欄**。規則文字中談「最左/最右」一律指相對欄。
- **延展只發生在開地任務(第 1~4 個任務)的獎勵事件中**, 一局最多 4 次, 每次 +1 欄。
- **延展側自動左右交替, 寫死**: 第 1 個任務 → 左(新增絕對欄 −1)、第 2 個 → 右(6)、第 3 個 → 左(−2)、第 4 個 → 右(7)。**玩家不能指定、不能改變延展側。**
- **下一次延展側指示(寫死)**: 自開局起全程常駐, 作為任務面板「得到什麼」的一部分(例: 做到後盤面向左長一欄), 第 4 次延展執行後不再顯示。
  - **它是現況讀數, 不是預測**: 延展側完全由規則的交替順序決定, 不依賴玩家任何行為; 指示只回答「下一次開在哪一側」, **不回答「什麼時候開」**, 也不顯示任何進度試算。
- **新增的欄位全空, 不補任何格位**。既有格位不位移, 絕對欄與高度皆不變。
- **延展執行必須是一個獨立、明確、可單獨辨識的事件(寫死)**: 時序上與消除、與重力事件完全分離(結算時序第 6 步), **不得與消除結算或重力事件併在同一個回饋裡**。
- **第 1~3 次延展只 +1 欄, 不附削頂(v16, 寫死)**。第 4 次延展附削頂與滿寬完成獎勵。
- 第 4 次延展後盤面固定 10 欄, 不再延展; **同一刻任務軌進入多樣化任務**。

#### 削頂(v14 改為每欄固定 1 格; v16 移到中後期; v18 多樣化任務改為每兩個一次)
- **削頂出現在哪裡(v18, 寫死)**: 只出現在 ① **第 4 次延展事件**(滿寬那次) ② **第偶數個多樣化任務的獎勵事件**(d = 2、4、6…, 即整條任務軌的第 6、8、10…個任務; d = 2 附在新形狀方塊之後, **d = 4、6、8… 削頂是該任務唯一的獎勵**, v20)。**第 1~3 次延展與第奇數個多樣化任務不削頂。** 多樣化任務不設上限, 因此削頂**不設次數上限**。
  - 理由(v16): 前期盤面薄, 削一層皮只會把本來就少的方塊削得更薄, 回饋小; 中後期盤面厚, 同樣一層的回饋大得多(v17 實測: 削頂每格回收的堆高前期約 1.7 倍、滿寬後約 2.5 倍以上; v20 第 7 輪實測削頂每格回收 1.87, 比假設低 25%)。
  - **為什麼是每兩個一次(v18)**: v16 原定每個多樣化任務都削, 實測回放推算開滿中位約 280 秒、強玩家 400~520 秒 — 每次削頂換回的存活塊數比完成任務花的塊數還多, 需求序列拉不回來。每兩個一次約 240 / 275 秒, 在當時的 300 秒硬上限內。交錯出現也讓削頂保有「觸發時很高興」的份量。
  - **不採用「每輪只在最後一個任務削一次」(v18, 數值偏好)**: 約 222 / 250 秒, 但第一次後期削頂會落在第 3 個多樣化任務, 被動型玩家多半走不到, 違背老闆「後期也要有削頂」。
  - **v17 不採用「全解鎖後不附削頂」**(數值 r1 備案, 數值 r2 已自行撤回): 違背老闆「後期也要有削頂」。
  - 附註: 多樣化任務一輪 3 個是奇數, 因此同一種任務在相鄰兩輪的削頂有無會交替(第 1 輪只有「用新形狀方塊」附削頂, 第 2 輪是大團與重力下落附削頂)。滿寬那次之後緊接的第 1 個多樣化任務不削, 兩次削頂不連發。
- **每一次削頂, 對盤面的每一欄各削掉最上面那 1 個已填格位。** 每次一律相同, 不隨次數遞增。
- **判定方式(寫死, 不得被實作自行詮釋)**:
  - **逐欄判定, 不以列號判定。** 對盤面**每一欄**(第 4 次延展時含本次新開的那一欄), 各自找出該欄最高的已填格位, **削掉那 1 格**。該欄沒有已填格位則該欄不削。
  - **各欄互不影響**: 結果是**沿著盤面輪廓削掉一層皮**。使用者要的「一層」是這個看起來的一層, 不是某一列 — 照列號削會讓高低不平的盤面常常只削到最高那根柱子的頂端(v9 已證明)。
  - 判定範圍含列 20 以上的不可見區; 第 4 次延展新開的那一欄必定全空, 對結果無影響。
  - **盤面若完全沒有已填格位, 本次削頂不執行、不遞延、不補償**; 該次獎勵事件的其餘動作(延展 +1 欄、滿寬完成獎勵、解鎖或清色球)照常執行; 只有削頂的那幾次(d = 4、6、8…)事件照常發生, 只是沒有格位被削。
  - **萬用格(重力球、清色球)在削頂判定中視為一般已填格位(寫死)**: 照常被找到、被削掉。**被削掉的重力球不觸發任何重力事件, 被削掉的清色球不觸發全盤清除(v19)** — 削頂不是消除, 觸發會讓它的結果不可預測。
- **畫面上不講削了幾格(v14, 寫死)**: 削頂事件只呈現「哪些格位被削掉」, 不顯示任何格數數字。
- **為什麼是削頂而不是清底(寫死)**: 在沒有系統性沉降的規則下清底完全不回收空間, 那一列只會變成一排永久空洞, 反而加速蜂窩化。
- **被獎勵削除的格位不計基礎分、不計目標色分數、不推任何任務進度、不計入消除總顆數**, 它不是一次「消除」。給分等於鼓勵刻意堆高等獎勵。
- **獎勵削除後不重新掃描成團、不觸發任何消除、不觸發任何重力事件**(削除不產生位移, 不可能產生新團)。**但懸空結構標示必須在削除完畢後重算。**
- **獎勵削除留下的也是空洞, 其餘格位一律原地不動。**
- **滿寬完成獎勵**: **第 4 次延展**事件執行完成、盤面達滿寬 10 欄的那一刻, 一次性加計 **1,500 分**, **不乘分數倍率**, 與該次削頂**同屬一個事件**。

### 任務軌
- **全局只有一條任務軌**: 任何時刻只有**一個當前任務**, 全程常駐可見。第 1~4 個是**開地任務**, 第 5 個起是**多樣化任務**, 不設上限, 一直發到局終。
- **兩個計數(寫死)**:
  - **已達成任務數 A**, 開局 0, **只在結算時序第 5 步增加**, 不設上限。
  - **已執行獎勵次數 E**, 開局 0, **只在結算時序第 6 步增加**。
  - **恆有 E ≤ A。獎勵事件觸發條件為 E < A**, 不是「剩餘 ≤ 0」 — 達成後剩餘會停在 ≤ 0, 用剩餘當觸發條件會讓每次結算都重複發獎勵。
  - 第 E+1 次獎勵對應第 E+1 個任務: **E+1 ≤ 4 為延展事件, E+1 ≥ 5 為多樣化任務獎勵事件。**
  - **G = max(0, E − 4)**, 即已執行的多樣化任務獎勵次數(= 已完成的多樣化任務數), 供落速公式使用。
- **當前任務 = 第 A+1 個任務。** 每個任務的種類由序號決定(見下); 剩餘量與目標色**只在該任務被進入的那一刻才設值**, **不得以 0 或任何預設值初始化**; 未進入的任務不參與任何判定。
- **任務面板(寫死)**: 常駐顯示當前任務的三件事 — **做什麼**(玩家語言的一句話, 開地任務含目標色)、**得到什麼**、**還差多少**(一個數字)。「得到什麼」的內容(v20 改; 令 d = 第幾個多樣化任務):
  - 第 1~3 個開地任務: 盤面向哪一側長一欄(**不寫削頂**)。
  - 第 4 個開地任務: 盤面向右長一欄 + 削頂 + 1,500 分。
  - 多樣化任務 d = 1: **「新形狀方塊 + 清色球」**。
  - d = 2: **「新形狀方塊 + 削頂」**。
  - d = 3: **「新形狀方塊」**。
  - d = 4: **「削頂」**(v20)。
  - d = 5: **「清色球」**(v20)。
  - d = 6: **「削頂」**(v20)。
  - d ≥ 7: d 為奇數 **「沒有獎勵」**; d 為偶數 **「削頂」**(v20)。「沒有獎勵」照實寫, 不留空 — 留空會被讀成面板壞了, 寫別的會與實際不符。
  - 多個獎勵時依事件內的演出順序寫: 新形狀方塊 → 清色球 → 削頂。
  - 削頂在面板上**一律只寫「削頂」, 不寫削幾格**。
  - **標籤一律寫玩家讀得懂的「做什麼 / 得到什麼」, 不寫規格內部的名稱**(不出現「階」「開地任務」「多樣化任務」「延展」「萬用格」「全盤清除」等詞; 第 5 輪使用者看不懂內部名稱)。「清色球」是玩家名稱, 照寫(v20)。
  - **獎勵名稱一律寫「新形狀方塊」, 不寫「新方塊」(v18)**: 任務文字與獎勵名稱要同一個詞, 且「新方塊」會被讀成「下一塊」。
  - **不顯示任務序號**: 玩家要知道的是現在要做什麼、做到得到什麼, 不需要知道這是第幾個。
  - **剩餘 ≤ 0 時一律顯示「已達成, 待結算」, 不顯示負數。**
  - 多樣化任務的獎勵若是新形狀方塊, **「得到什麼」只說會得到新形狀方塊, 不預先展示是哪一種外型** — 外型留到解鎖事件才揭曉(新鮮感是試玩要看的第 2 件事)。
  - **「用新形狀方塊」進行中的新形狀小圖(v19, 寫死)**: 當前任務是「用新形狀方塊」時, 面板在「做什麼」的文字旁畫出**目前已解鎖的每一種新外型的小圖**(未解鎖的不畫, 不因此預先揭曉)。任務一結束即移除, **不常駐**。理由: 第 7 輪使用者「一開始看不懂…很多玩家應該不知道新形狀方塊有哪些」; v17 方案 B 曾因畫面花不採用, 現在讀不懂是更大的問題。
  - **不另外向玩家預告下一個任務是什麼(v15, 寫死)**: 畫面上只有一個當前任務; 下一個任務只在獎勵事件尾端公告時才出現。
- **任務進度推進的回饋(v15, v17 改)**: 一次結算有推進當前任務時, 回饋要讓玩家看得出「這次算數了」 — 開地任務標出推進了進度的目標色格; 大團標出算數的那一團; **用新形狀方塊標出算數的各團(每一團都含本塊的格位)**; 重力下落標在算數的那一次重力事件上。**追加消除那一段不推進大團與用新形狀方塊, 不得出現這兩種任務的算數回饋。** 全盤清除不推進任何任務, 不得出現算數回饋(v19)。
- **每次結算的計數對象 = 第 1 步取樣的當前任務**: 本次結算內, 所有計數都只推進這一個任務; 本次結算第 5 步才進入的新任務, 從下一次結算起才開始計數。
- **溢出(v13 定案, v14 限定範圍)**: **只在同一種計數的任務之間發生**。
  - 開地任務之間: 超出的顆數扣抵下一個開地任務, **新任務剩餘 = max(1, 新任務需求 − 溢出量)**, 超出部分捨棄; 換色之後照常抵扣。理由: 公告出來的任務至少要做一次才會換掉。
  - **不同種類的任務之間不溢出**: 第 4 個開地任務的超出量捨棄; 多樣化任務三種依序輪替、相鄰兩個必不同種, 因此多樣化任務之間永遠不溢出, 超出量一律捨棄。
- **每次結算最多推進 1 個任務、最多執行 1 次獎勵事件**。新任務剩餘恆 ≥ 1, 因此不存在「新任務一進入就已達成」的情況。

#### 開地任務(第 1~4 個任務)
- **做什麼**: 消掉 N 顆目標色。
- **計入規則(寫死)**: **被消掉的格位, 顏色 = 當前目標色者, 剩餘 −1。** 逐格判定, **與格位所在欄、列完全無關**。
  - **重力球永遠不計入(製作人拍板)**: 它沒有顏色。**但它所屬的那一團裡真正的目標色格照常逐格計入。**
  - **削頂削掉的格位不是被消掉的格位, 一律不計入。**
  - **追加消除消掉的格位是被消掉的格位, 照常計入。**
  - 清色球只在滿寬後才可能出現, 開地任務期間不存在(v19)。
- **需求顆數(v17 定案)**: **3 / 5 / 6 / 8 顆**(合計 22)。
  - **設計方向(寫死)**: **刻意優先消目標色的玩家要明顯比完全被動的玩家早開滿, 但不以擋住被動玩家開滿為目標。** 不得用調高門檻去堵被動高手 — 門檻每往上推一格, 先死的是中技術玩家。
  - 第 1 個任務壓低到 3 顆是因為開局盤面全空, 前幾塊物理上不可能成團。
  - **為什麼從 3/5/7/9 降下來(v17)**: v16 拿掉前期削頂後, 以實測回放推算, 被動慢型約一半會死在第 4 個開地任務; 降需求讓被動型少約 3 塊開地。
  - 脈絡(v15): 3/6/9/12 在削頂變弱後會讓被動型約 31% 死在第 4 個開地任務, 且滿寬後只剩約 11 塊, 驗證問題後半段測不到, 因此降為 3/5/7/9。
- **獎勵(v16)**: 延展事件。第 1~3 個: **+1 欄**; 第 4 個: **+1 欄 + 削頂 + 1,500 分**。見「盤面與延展」。

#### 目標色的決定(系統自動換色, 玩家不挑)
- **目標色只在開地任務期間存在(v14, 寫死)**; 進入多樣化任務後不抽、不顯示目標色, 也沒有目標色分數。
- **第 1 個任務**: 自 3 色等機率抽 1。
- **第 2~4 個任務, 每個被進入時**: 自 3 色中**排除上一個任務的目標色**, 剩下 2 色等機率抽 1, **保證連續兩個不同色**。
- **任務內不變**: 目標色在該任務被進入時決定, 直到該任務達成都不改變。
- **換色的公告時點(寫死)**: 新任務的目標色在第 5 步決定, **在第 6 步獎勵事件的尾端公告**, 不另立事件、不另計呈現時長。第 5 步到公告之間不發生任何消除, 因此不存在計數歸屬的空窗。
- **不存在任何由玩家挑選、召喚、拒絕或延後換色的操作。**
- 換色後, 盤面上舊目標色的團照常能消、照常計基礎分, 只是不再推進度。

#### 多樣化任務(第 5 個任務起, v15 定案, v17 改第 2 種, v18 定案第 2 種, v20 改需求與獎勵)
- **三種任務, 依固定順序輪替, 寫死**: 令 k 為任務序號(k ≥ 5), 種類 = (k − 5) mod 3; 令輪次 c = ⌊(k − 5) / 3⌋ + 1:

  | 序 | 任務 | 做什麼(玩家語言) | 計數單位 | 需求 | 計入規則 |
  | --- | --- | --- | --- | --- | --- |
  | 0 | **大團** | 消掉一團 5 顆以上的史萊姆 | 團 | **c ≤ 2: 2c − 1(v17); c ≥ 3: 3c − 2(v20)** | 第 4a 步清除的成立團中, 每一個 n ≥ 5 的團 −1(n 含團內萬用格, 與計分同一個 n) |
  | 1 | **用新形狀方塊**(v17 新增, v18 定案) | **放下新形狀的方塊, 當場消掉一團** | 團 | **c ≤ 2: 2c − 1(v18); c ≥ 3: 3c − 2(v20)** | **本塊是已解鎖的新外型時**, 第 4a 步清除的成立團中, **每一個含本塊格位的團 −1** |
  | 2 | **重力下落** | 用重力球讓懸空的史萊姆掉下來 | 次 | **固定 1** | 第 4b 步每一個**實際發生位移**的重力事件 −1; 零位移事件不算 |

- **需求量**: **大團 = 1 / 3 / 7 / 10…(第 1、2 輪 2c − 1, v17; 第 3 輪起 3c − 2, v20; 不封頂)**; **用新形狀方塊 = 1 / 3 / 7 / 10…(第 1、2 輪 2c − 1, v18; 第 3 輪起 3c − 2, v20; 不封頂)**; **重力下落固定 1, 不隨輪次增加** — 它主要在等重力球被發到(每 8 塊 1 顆), 需求再高只是拉長等待。
  - **大團為什麼第 2 輪起加重(v17)**: 實測強玩家在 v16 後段會因「削頂 → 盤面變低 → 任務更容易 → 又拿削頂」的正回饋拉到 285~315 秒; 加重大團是壓住超長局、又不動削頂時機的代價最低的旋鈕, 只影響走到第 2 輪的人。
  - **第 3 輪起改 3c − 2(v20, spec-review v19 定案)**: 與 v17 加重大團同一個方向 — 只影響走到第 3 輪的強玩家, 壓住長局; 第 1 輪(上手任務)與第 2 輪不變。
- **「用新形狀方塊」的計入規則(v17 設計, v18 定案, 寫死)**:
  - **任務文字(v18 定案)**: 「放下新形狀的方塊, 當場消掉一團」。「放下」與「當場」兩個詞就是判定本身: 算的是放下的這一塊, 且是放下後立刻發生的那一段消除。
  - **算**: 本塊的形狀是已解鎖的新外型(角形 / 杯形 / 十字形), 且第 4a 步有成立團含本塊的格位 — **每一團 −1**。一塊同時消掉兩團就 −2, 與大團「每一團 −1」同一個單位, 面板上「還差多少」恆是團數。
    - 本塊的格位**含本塊的萬用格(重力球或清色球)**: 新外型的萬用格與其他格照常補成一團時, 該團照算。
    - **新外型塊內自己湊成的團照算**(杯形約 3%、十字形約 5%, 與「塊內自消合法」一致, 不另外處理)。
    - 附註: 放下前盤面穩定、不存在 ≥4 的團, 因此 4a 的成立團必定含本塊格位; 實作上等同「本塊是新外型時, 4a 每一個成立團 −1」, 但規則以「含本塊格位」為準, 不得改成數盤面上舊的新外型格。
  - **不算**:
    - **先用新外型鋪、再用原 7 種收尾(v18 明定)**: 原 7 種形狀放下造成的消除一律不算, 即使被消掉的團裡有以前放下的新外型格。盤面上已鎖定的格位不記得自己來自哪一種方塊, 玩家也讀不出來; 只看「這一塊」才是玩家在放置那一刻就能判斷的事。這是本任務最容易被誤會的地方(企劃在 spec-review 第 17 版指出), 由任務文字的「當場」承擔, 並以埋點「以為做到卻沒算」直接量。
    - **追加消除(4c)**: 新外型的格被重力事件帶著掉下去後才成團, 不算(放下前算不到, 與大團同一條「只看第一段消除」)。
    - **重力事件本身**(4b): 它不消除任何格位。
    - **全盤清除(4d, v19)清掉的新外型格**: 全盤清除不推任何任務。
    - **削頂削掉的新外型格**: 削頂不是消除。
  - **與追加消除的關係**: 同一次結算中, 4a 算數、4c 不算; 4c 不出現本任務的算數回饋。
  - **讓玩家讀懂「哪些是新形狀方塊」(v19, 寫死)**: 本任務進行中, ① 任務面板在任務文字旁畫出已解鎖新外型的小圖(見「任務軌 / 任務面板」) ② **下一塊預覽若是新外型, 帶一個小記號**, 與面板小圖屬同一類標示, 讓玩家一眼看出「下一塊就是任務要的那種」。兩者**只在本任務為當前任務期間出現**; 本任務以外的時間預覽不帶記號、面板不畫小圖。記號只表示「這一塊是新形狀方塊」, **不表示放在哪裡會算數**(不是落點試算)。
- **為什麼是這三種(設計理由)**: 每一種都要求玩家「換一種方式消」, 而且都不含位置條件:
  - **大團**: 平常的消法是把一團補到 4 顆就消; 這個任務要一塊放下時讓一團一口氣跨過 4 顆。它**被動也常順手完成(約 8.5 塊一次)**, 定位是**上手任務**; 讀數靠「被動基準」埋點扣回來(見「埋點」)。
  - **用新形狀方塊**: 要玩家**拿到新外型時先想好它要去補哪一團**, 而不是把它當成一塊難放的方塊隨便塞掉。它直接逼玩家實際使用剛拿到的新形狀方塊, 正好給驗證問題「開滿之後追新外型」一個看得到的行為; 也讓試玩要看的第 2 件事(新外型是新鮮還是只是變難)有對照。
    - **為什麼只看「這一塊」而不看盤面上的舊格**: 已鎖定的格位在畫面上不帶「來自新外型」的記號(不另加盤面標示, 見「Demo 範圍邊界」), 看舊格會變成玩家讀不出的判定 — 與 v15 否決「接團」同一個理由。
    - **為什麼計團而不計次**: 任務文字是「消掉一團」, 計團才與字面一致; 十字形「一塊碰得到最多相鄰團」的性格在這裡有回報。高盤面上一塊消兩團本來就罕見(實測雙消 288 塊 4 次), 對進度速度影響很小。
    - **第 1 輪是上手任務(v18)**: 第 1 輪需求 1, 又有首發保證, 實測 4/4 都在首發那張完成; 定位與大團相同, **讀數從第 2 輪起才判讀**。第 2 輪起需求 3、第 3 輪起 7(v20), 等的是非首發的新外型塊當場命中(實測非首發約 0.31、角形約 0.28)。
    - **不可能出現「沒有新外型可用」**: 本任務排在大團之後, 大團必定先解鎖角形。
  - **重力下落**: 要玩家**讀懸空標示, 把重力球送到和懸空結構相連的團裡**。會不會掉由玩家放在哪決定; 零位移不算, 所以「亂丟一顆球」不會達成。
  - 三種都**不含任何位置條件**(不指定欄、不分左右半), 也都不是「消掉 N 顆某色」換個數字。
- **v17 刪除「雙消」**: 實測在高盤面上幾乎不會發生(288 塊只發生 4 次, 滿寬後不是當前任務時 0 次), 第 2 輪 72 塊 0 次推進, 4 局中 3 局死在雙消 — 不是需求量能調的, 需求改 1 也拆不掉。
- **計數只看放下後的第一段消除(寫死)**: 大團與用新形狀方塊只計第 4a 步; 重力下落之後追加消除(4c)的那一段不算 — 那段結果放下前算不到, 計入會讓任務部分靠運氣。重力下落計的是 4b, 那一步的觸發本身就是玩家的放置決定。
- **為什麼是這個順序(設計理由)**:
  - **大團排第一**: 最接近玩家已經在做的事, 被動也常順手完成, 讓第一個多樣化任務一定有人做得到; 它解鎖的角形是最好用的外型, 第一印象決定玩家之後對新外型的態度。
  - **用新形狀方塊排第二**: 必須在至少一種新外型解鎖之後才成立; 緊接在角形解鎖之後, 讓玩家第一次拿到新形狀方塊時就有理由去用它。
  - **重力下落排最後**: 它主要在等重力球被發到, 放後面只讓走到這一步的玩家付這個成本。
  - **十字形因此最後解鎖**: 見「新外型方塊 / 解鎖順序」。
- **多樣化任務不另給分(v15 定案)**: 任務的回報是新外型、清色球與削頂, 不再疊加任務分。大團本來就吃 n ≥ 5 的固定額 +25, 再加任務分會變成刷大團; 用新形狀方塊與重力下落另給分則會鼓勵為了分數硬湊。
- **獎勵(v20 定案, 取代 v19 企劃提案)**: 令 d = k − 4(第幾個多樣化任務)。放置表(寫死):

  | d | 任務(輪次) | 獎勵(依事件內演出順序) |
  | --- | --- | --- |
  | 1 | 大團(第 1 輪) | 解鎖角形 + 清色球 |
  | 2 | 用新形狀方塊(第 1 輪) | 解鎖杯形 + 削頂 |
  | 3 | 重力下落(第 1 輪) | 解鎖十字形 |
  | 4 | 大團(第 2 輪) | 削頂 |
  | 5 | 用新形狀方塊(第 2 輪) | 清色球 |
  | 6 | 重力下落(第 2 輪) | 削頂 |
  | ≥ 7, 奇數 | 依輪替 | 沒有獎勵 |
  | ≥ 8, 偶數 | 依輪替 | 削頂 |

  - **每個任務最多一種減壓**(削頂或清色球擇一; 解鎖新外型不算減壓)。
  - **清色球每局上限 2 顆(保護值)**: 以本局「獲得數」計。現行放置一局最多就是 2 顆, 上限不會擋到; 保留它是為了之後若調動放置位置仍守得住。被上限擋下的記入埋點(現行應恆為 0, 可作自我校驗)。
  - **為什麼 d = 1**: 目標是一般玩家一局至少看得到一次、且不早於滿寬; d = 1 是滿寬後最早、也是一般玩家幾乎必定完成的任務(第 7 輪實測大團第 1 輪 7/7 達成)。
  - **為什麼 d = 5, 而不是全解鎖後每個任務一顆(v19 企劃提案不採用)**: 數值以第 7 輪實測算出, v19 的放法會讓強玩家拉到 350~375 秒, 且可能「清盤 → 任務更容易 → 又拿清色球」而自我維持。只給 2 顆、d = 5 落在 d = 4 與 d = 6 兩次削頂之間, 每個任務只有一種減壓。
  - **為什麼第 1 顆延後到獲得後第 15 塊(v20)**: 立即發出時落在滿寬後最薄的盤面, 只清得掉 6~10 格, 比滿寬削頂還弱, 而被動型一局多半只看到這一顆; 延後後約清 12~14 格, 並補在兩次削頂之間的空檔; 局長影響約 0~+3 秒。延後期間由獎勵事件先展示它、出現時由預覽標出來承擔期待。規則見「清色球 / 生成」。
  - 理由(沿用 v16/v18): 兩種獎勵放在同一個事件裡**依序演出**, 仍是一個可單獨辨識的獎勵事件。
- **獎勵事件**: 屬結算時序第 6 步, **獨立、可單獨辨識**, 不與消除結算或重力事件併在同一個回饋裡; **呈現時長 1.5 秒(v17; 單段也維持 1.5 秒, v18 定案)**。事件內依序演出 **解鎖(d ≤ 3) → 清色球(d = 1、5) → 削頂(僅第偶數個)**: 解鎖時**展示這種新外型長什麼樣**(形狀本身, 讓玩家在它第一次被發到之前就認得), 事件文字稱它為「**新形狀方塊**」(v18); 給清色球時**展示清色球本身, 並以一句話講它的效果: 「能當任何顏色, 消掉它全盤同色一起消失。」**(v20 定案; 這是玩家第一次知道清色球是什麼的地方, 玩家說明不另寫); 第偶數個接著削頂; 事件尾端公告下一個任務。**d ≥ 7 的第奇數個沒有獎勵(v20)**: 事件照常發生, 只呈現任務完成與尾端公告, 呈現時長照 1.5 秒(企劃初始值, 見「待定」)。
- **每完成一個多樣化任務, 落速再上一階, 封頂 2 階**(見「方塊與下落」), 不論該次獎勵內容(含沒有獎勵)。
- **已知限制(v17 依實測改寫, v20 補第 7 輪)**: 老闆 v15 試玩 4 局(一人, 只當參考): 滿寬後窗口約 27~64 塊(約為紙上推算的 2 倍); 解鎖外型數被動 1~2 種、刻意 3 種。第 7 輪實測: 滿寬後窗口 51~78 塊; 大團第 1 輪 7/7 達成。十字形仍只有走完一整輪的玩家看得到。

### 新外型方塊(v15 定案)

#### 三種新外型
- 原 7 種四格形狀不變; 新增 **3 種**, 依序由第 1~3 個多樣化任務解鎖。**新外型只在盤面滿寬之後才可能被發到**(解鎖必定發生在第 4 次延展之後), 因此一律在 10 欄盤面上生成與旋轉。玩家可見的名稱一律為「**新形狀方塊**」(v18); 「新外型」是規格內部用語。
- **座標記法**: 每種新外型有一個固定的**旋轉框**; 框內座標 (bx, by), bx 向右、by 向上, 框左下角為 (0, 0)。

| 外型 | 格數 | 旋轉框 | spawn 佔格(框內座標) | spawn 形狀寬 × 外框高 | 放置性格 |
| --- | --- | --- | --- | --- | --- |
| **角形 V** | 3 | 2×2 | (0,0)(1,0)(0,1) | 2 × 2 | 最小、最好塞: 3 格只帶 3 個顏色, 最容易精準補某一團的第 4 顆、塞 1 格缺口; 含球時球的位置最好控制 |
| **杯形 U** | 5 | 3×3 | (0,0)(1,0)(2,0)(0,1)(2,1) | 3 × 2 | 修整輪廓: 開口朝上時自己帶一個 1 格深的坑, 倒扣時可以跨過一根 1 格寬的凸柱; 用對了把凹凸吃掉, 用錯了就蓋出封閉洞 |
| **十字形 X** | 5 | 3×3 | (1,0)(0,1)(1,1)(2,1)(1,2) | 3 × 3 | 一塊碰得到最多相鄰團: 四臂朝四個方向伸出, 最容易一塊同時補兩團; 但兩臂下方必然懸空, 除非盤面剛好有 T 形凹, 否則一放就留洞 — 最難填洞, 是唯一真正加難度的外型 |

- **三種的性格各不相同(設計理由)**: 角形是「更好補」、杯形是「專門處理凹凸」、十字形是「更好一塊顧兩團但更難填洞」。沒有一種只是原 7 種換個樣子: 原 7 種都是 4 格, 性格差異只在輪廓; 新外型改的是格數(3 / 5)與「會不會自帶懸空或坑」這兩個維度。
- **解鎖順序(寫死): 角形 V → 杯形 U → 十字形 X**(對應 大團 → 用新形狀方塊 → 重力下落)。
  - 第 1 個解鎖的必須是「好用的」: 試玩要看玩家對新外型是期待還是只覺得變難, 第一印象決定之後的態度。角形 3 格只會讓放置更容易。
  - **十字形排最後**: 它是唯一真正加難度的外型(每格淨體積 +34%), 一般玩家一局只解得到 1~2 種, 排第 2 會讓多數人最後拿到的是最會造洞的那一塊, 局終印象變成「只是變難」; 排最後時手上已有杯形, 倒扣可以修它留下的凸起。
- **塊內自消合法(v15, 寫死)**: 新外型的格在塊內自己湊成 4 顆同色而於鎖定後成團消除, 照一般消除處理, 不另外處理(杯形約 3%、十字形約 5%)。

#### 生成與旋轉
- **生成位置沿用原規則**: 方塊的**外框底緣固定在列 20**(外框指該旋轉態下實際佔格的 bounding box); 外框**左緣相對欄 = ⌊(w − 形狀寬) / 2⌋**。滿寬 w = 10 時: 角形左緣相對欄 4、杯形 / 十字形左緣相對欄 3。
  - 旋轉框與 spawn 佔格的相對位置照上表; spawn 時旋轉框的 (0, 0) 對齊外框左下角。十字形 spawn 佔列 20~22, 列 22 以上不設硬界, 合法。
- **旋轉**: 佔格在旋轉框內**繞框中心旋轉 90 度**(順時針鍵順轉、逆時針鍵逆轉、180 度鍵轉半圈), 每格顏色與萬用格隨格位一起轉; **簡化踢牆照原規則**(原地不行 → 左 1 格 → 右 1 格, 皆不行則旋轉失敗)。
  - 角形在 2×2 框內旋轉, 相當於缺的那一角換位置。
  - 杯形 4 個朝向為開口朝上 / 右 / 下 / 左。
  - **十字形的旋轉不是 no-op(寫死)**: 佔格不變, 但**外圍 4 格的顏色必須真實繞中心轉動**(逆時針鍵反向, 180 度鍵為對角互換); 中心格不動。**含萬用格時其位置同樣隨之轉動。** 佔格不變, 因此十字形的旋轉永遠成立, 不需要踢牆。
- **可擺放性(自查)**: 三種在 10 欄盤面上的任何朝向寬度都 ≤ 3, 高度都 ≤ 3, 皆能生成、能旋轉、能放進合理的位置; 不存在 10 欄內無法擺放的外型。
- **每格仍各自獨立上色, 顏色 bag 照舊供色**(格數多的外型消耗較多 bag 格); **重力球仍是某一格, 一塊最多 1 顆**, 在該塊所有格中等機率抽 1(見「重力觸發球 / 生成」); 清色球同(見「清色球 / 生成」)。

#### 形狀 bag 的組成(v15 定案)
- **原 7 種維持各 1 張; 每解鎖一種新外型, bag 加 2 張該外型。** 形狀 bag 的大小 = 7 + 2 × 已解鎖新外型數(7 / 9 / 11 / 13)。
  - 理由: 原 7 種是玩家已經熟悉的底, 不拿掉; 滿寬後的窗口有限, 每種 2 張讓新外型在一局內被發到的次數夠多, 「解鎖之後被發到幾次」才有讀數。
- **生效時點(寫死)**: 解鎖**從下一個形狀 bag 起**加入; 已在進行中的 bag 不改寫, 預覽中已確定的那一塊也不改寫。
- **首發保證(v15 定案)**: **解鎖後的第一個 bag 中, 新解鎖外型的其中 1 張固定排在該 bag 的第 1 張**, 其餘(含該外型的另 1 張)照常洗牌; 同一個 bag 前若有兩種新解鎖, 依解鎖順序各取 1 張排在第 1、2 張。被首發保證排定的那一張稱為「**首發那張**」(埋點與「用新形狀方塊」的讀數都分首發 / 非首發)。
  - **邊界(v17, 寫死)**: 解鎖當下若下一塊預覽已經是從一個新的 bag 抽出的(即該 bag 的第 1 張已在預覽中), **該 bag 視為進行中**, 不改寫, 首發順延到再下一個 bag 的第 1 張。理由: 預覽中已確定的那一塊不得改寫, 而它正好佔掉了新 bag 的第 1 張。
  - 理由: 滿寬後剩的塊數有限, 純洗牌下從解鎖到第一次拿到可能隔很多塊, 解鎖的興奮會在等待中冷掉。排在 bag 第 1 張, 玩家最多等到當前 bag 用完。
  - 代價: 這一張的出現時點變得可預期。只影響解鎖後第一個 bag 的第 1 張, 之後照常洗牌。

### 顏色
- 顏色共 3 種(**禁改**)。
- 方塊的每一格顏色獨立, 由顏色 bag 依序填入: **每 18 格為一個 bag, 內含各色各 6 格**, 洗牌後依序發放, 用盡再洗下一個 bag。**同一塊各格的填入順序寫死為: 列號(y)小者在前, y 相同時相對欄(x)小者在前。**
  - **萬用格(重力球、清色球)那一格不從顏色 bag 取色, 也不消耗 bag 的任何一格(寫死)**: 含萬用格的塊只有其餘格從 bag 取色。消耗 bag 會破壞三色均衡, 而三色等量是供給模型與目標色佔比讀數參照的前提。
- 方塊形狀為標準 7 種四格形狀加上已解鎖的新外型, 以形狀 bag 發放(組成見「新外型方塊」); 形狀、顏色、重力球三個 bag 各自獨立; 清色球不用 bag, 依獎勵發出(見「清色球」)。
- **重開一局時, 三個 bag 一律清空重洗, 已解鎖的新外型一律歸零, 清色球的待發清單與本局獲得數歸零。**

### 重力觸發球

#### 生成
- **重力球不單獨出現, 必定是某一塊中的某一格。** 盤面上不會憑空生成重力球, 也沒有任何道具欄或引爆鍵。
- **一塊最多 1 顆(寫死)**: 2 顆會讓單塊同時補滿兩色, 把「取捨只有一個」拆成兩條並行路線。**v19: 一塊最多 1 顆萬用格 — 重力球與清色球不同時出現在同一塊。**
- **出現率以「重力球 bag」控制, 不逐塊擲骰(寫死)**: **每 8 塊為一組, 洗牌決定組內哪一塊含 1 顆重力球**, 出現率恆為 **12.5%**, 不論形狀。逐塊擲骰會出現連續 3~4 塊都有球、或 20 塊都沒有球的極端序列。**清色球不佔用、不改寫重力球 bag(v19)。**
  - **12.5% 維持**, 位於局長旋鈕的第 4 順位(v20: 清色球插入第 2 順位)。往下調會連「重力球存在的理由」一起砍掉, 而且會讓玩家更少練習、更學不會, 對「運氣感」是反效果。
- **保證不連續(寫死)**: 新一組洗牌後, 若含球塊落在該組第 1 塊、且上一組的含球塊是該組第 8 塊, **則該組重洗**, 直到兩顆球之間至少隔 1 塊。連續兩塊有球會讓第二顆在第一顆的結果還沒讀懂前就要決定落點, 那是執行門檻不是決策。
- **哪一格是重力球**: 該塊各格依填入順序編為索引 0 ~ (格數 − 1), **等機率抽 1 個索引**, 其餘格依序從顏色 bag 取色。不對形狀或索引加權。
- **下一塊預覽必須能讀出該塊是否含重力球、以及是哪一格。** 這是放置規劃的前提資訊, 不屬於預測性提示。

#### 成團判定中的參與(v19: 條文擴為「萬用格」, 規則不變)
- **萬用格 = 重力球與清色球(v19)**。兩者在成團判定中完全相同, 以下條文對兩者一體適用。
- **萬用格是萬用色**: 成團判定中可代替任何一種顏色, 與任一顏色的格位四方向相連時視為同色。
- **一顆萬用格在單次掃描中最多參與 1 個成立團(寫死)**: **不能同時讓紅團與藍團都成立。** 允許共用會讓一顆球的期望威力暴增且玩家算不出來, 更會把「這顆球用在哪」的取捨變成通吃。
- **純萬用格的團不成立消除(寫死)**: 一個團必須至少含 1 個真正的顏色格。**不對團內萬用格的數量另設上限。**
- **成團掃描程序(寫死)**:
  1. 建立空的「已認領集合」。
  2. 對每個顏色 c ∈ {A,B,C}: 取所有「顏色 = c 且不在已認領集合」的格位, 併上所有「萬用格且不在已認領集合」的格位, 做四方向連通。每個連通團若**總格數 ≥ 4 且其中真顏色格數 ≥ 1**, 列為候選團(該團的顏色 = c)。
  3. 若無候選團 → 掃描結束, 已認領集合即本次全部成立消除的格位。
  4. 依仲裁順位選出**恰好 1 個**候選團, 將其全部格位(含其用到的萬用格)加入已認領集合, 回到第 2 步。
- **仲裁順位(寫死, 依序比較)**: ① **真顏色格數多者優先** ② 相同則**團總格數多者優先** ③ 再相同則**錨點 y 大者優先** ④ 再相同則**錨點 x 小者優先**。錨點 = 團中 y 最大、y 相同取 x 最小的格位。**重力球與清色球在仲裁中不分彼此, 清色球不享有任何優先(v19)。**
- **這個程序對不含萬用格的盤面與一次性掃描完全等價**, 實作可據此優化, 但不得改變含萬用格情況下的仲裁結果。
- **迭代必定終止**: 每輪至少認領 1 個團、格位只減不增。**這個迭代不是連鎖** — 全程沒有任何格位移動, 它只是同一次掃描內對「萬用格歸誰」的確定性仲裁。
- **不變式(寫死, 可作為實作的自我校驗)**: **盤面穩定後, 不存在「以某顆萬用格為橋、與之連通的同色真顏色格數 ≥ 3」的狀態。** 玩家在盤面上看到的 3 顆同色團, 旁邊必定沒有萬用格。

#### 重力事件(下落)
- **觸發**: 某個**含重力球的成立團**被消除時, 觸發一次重力事件。**以團為單位, 一團一次。** 只含清色球、不含重力球的團不觸發重力事件(v19)。
- **多個重力事件的順序(寫死)**: 同一次結算有多個含球團時, 依各團錨點 **y 大者先、y 相同 x 小者先** 依序執行。**後一個事件在前一個事件的結果盤面上重新計算受影響集合。**
- **受影響集合的判定(寫死, 三步)**:
  1. **種子**: 以**該成立團被清除前所佔的格位座標**為基準, 取其**四方向鄰格中在本事件開始時仍為已填**的格位; **若該基準座標本身在本事件開始時已為已填**(被同結算中較早的重力事件填入), **該格位同樣納入種子**。理由: 以原團座標為基準而非「當下仍存在的空洞」, **後一個事件的作用範圍不會因執行順序而憑空縮小**。
  2. **受影響結構**: 自種子集合出發, 對**已填格位**做四方向連通, 得到若干連通塊。**種子集合以外的連通塊完全不受影響, 即使它也懸空。**
  3. **懸空判定**: 一個連通塊**懸空 ⟺ 把該塊整體下移 1 格後, 塊內沒有任何格位落到列 1 以下, 且不與任何「不屬於該塊的已填格位」重疊**。不懸空的連通塊本次不動。**本條與「懸空結構標示」共用同一條定義, 不得分成兩套。**
- **為什麼種子取「整個被消團的鄰域」而不是「重力球那一格的四鄰」(寫死)**: 球常落在團的中間, 四周整片都是同團被消掉的格位, 只取球的四鄰會讓種子經常是空的、事件當場什麼都不發生。**收窄到球那一格的四鄰不但治不了空轉、還會讓空轉更嚴重, 不列為退路。** 取團的鄰域仍然嚴格局部, **不得改用全盤判定**。
- **下落方式(寫死)**: **剛體整體下落, 一路落到不能再落為止**。**不是固定 1 格**: 固定 1 格會留下「還是懸空但不掉了」的半吊子, 完全解不了滯空碎片。
- **多塊的下落順序(寫死)**: 受影響且懸空的連通塊若有多個, 依**該塊最低格位的列號小者先**、相同則**該塊最小 x 小者先**, **逐塊落到底, 一塊落定後才處理下一塊**。
- **受影響集合在事件開始的那一刻一次決定, 不因下落而擴張(寫死)**: 下落後新接觸到的其他懸空塊**不被拉進本次事件**。擴散在堆滿的盤面上幾乎等同全盤沉降。
- **剛體下落不破壞任何相鄰關係(寫死, 這是它與第 2 輪沉降的本質差別)**: 第 2 輪是**逐格壓實**, 玩家養的同色色區會被整片重排; 剛體下落是**整塊同步位移**, 塊內相鄰關係完全保留。新的同色相鄰**只可能出現在兩個原本不相連的結構因下落而貼在一起的接縫上**。
- **重力球本身在觸發時已被消除, 不留在盤面上。** 未被消除的萬用格照常佔格位、參與 lock out 與 block out、可被削頂削除、隨重力事件一起下落。
- **重力事件不計分、不推進開地任務、不計入消除總顆數。** 它移動格位, 不移除格位。**唯一的例外是多樣化任務「重力下落」: 每個實際發生位移的事件推進 1 次。**

#### 重力下落的呈現(v14 改為動畫, v15 定案時長)
- **判定先於動畫(寫死)**: 每個重力事件的受影響集合、各塊下落順序與落距, 在該事件的動畫開始前就已全部確定; **動畫只是呈現, 不參與任何判定**, 也不得因動畫進行中的任何輸入而改變結果。
- **一塊一塊看得到地往下落(寫死)**: 受影響且懸空的連通塊, **依規則的下落順序逐塊演出**: 一塊從原位置連續下落到落定位置, 落地時有落地的回饋, 然後才開始下一塊。**不得瞬移, 不得多塊同時落**(同時落會讓玩家讀不出規則的逐塊順序)。
- **下落中的塊保持形狀不變**(剛體), 讓玩家看出「整塊一起掉、不是一格一格壓實」。
- **追加消除是明顯分開的第二段(寫死)**: 本次結算所有重力事件的動畫都演完之後, 若有追加消除, 才以獨立的一段呈現(見「追加消除」), 不得與最後一塊的落地回饋疊在一起。
- **呈現時長依落距決定(取代 v11 固定的 0.3 / 0.5 秒, v15 定案)**:
  - 一塊的下落時長 = max(**每塊最短 0.15 秒**, 該塊落格數 × **每格 0.06 秒**); 每塊落地回饋另計 **落地停頓 0.12 秒**。
  - 一個事件的時長 = Σ(各塊下落時長 + 落地停頓)。
  - **單次事件上限 1.5 秒**: 超過上限時, 本事件內所有塊的下落時長與落地停頓**等比壓縮**到總長等於上限(不跳過任何一塊、不改變順序)。
  - 理由: 對局長的影響不到 1%, 所以完全照看不看得清楚來定; 落 1 格若只演 0.05 秒只有 3 幀, 看起來跟瞬移一樣。
  - **零位移(空轉): 0.2 秒, 且必須產生一個可單獨辨識的回饋, 不得無聲跳過(照舊)。** 玩家要能分辨「我算錯了、那一坨跟這團不相連」與「機制根本沒生效」; 無聲跳過會被讀成 bug, **而空轉率正是本機制成敗的瓶頸指標**。
  - 同一次結算的多個事件各自計時、依序演出; 期間本局經過時間 t 照常推進。
- **退路順位(三級, 寫死)**: 若空轉率或下落規模偏離紅線(見「埋點」), 依序動:
  1. **先靠懸空現況標示與下落動畫 — 不改任何規則。**(已內建, 這一級已經先付了。)
  2. **再調整重力球出現率。** 往下調會同時削弱機制的存在理由與玩家的練習量, 動之前先確認不是第 ① 級沒讀到。
  3. **最後才是對單次事件的下落格數設上限**(備用旋鈕 **5 格**, **預設不啟用**)。
  - **「收窄種子到球那一格的四鄰」不得作為退路。**

#### 懸空結構標示
- **全程常駐標示, 無開關。** 它不改任何規則, 只把玩家已經需要、卻必須靠數格子才讀得到的資訊直接攤開; 這是所有壓低空轉率的手段裡代價最低的一個。
- **懸空判定(與重力事件共用同一條定義, 不得分成兩套, 寫死)**: 對**全部已填格位**(含不可見區、含萬用格)做四方向連通得連通塊; **一塊懸空 ⟺ 整體下移 1 格後塊內沒有任何格位落到列 1 以下, 且不與任何不屬於該塊的已填格位重疊**; 含列 1 格位的塊必不懸空。
  - **兩份定義一旦不同步, 玩家會看到標著懸空的結構在重力事件中不動, 機制的可信度當場失效。** 實作必須是同一個判定, 不得為了效能另寫一套近似。
- **標示內容(寫死)**: 必須讀得出**哪些格位懸空**, 以及**哪些格位屬於同一個連通塊**, 不同塊之間必須彼此可區分。
  - **不得只標「這一格懸空」而不表達塊的歸屬** — 重力事件是整塊同步位移, 玩家若把一個塊讀成一堆各自獨立的格位, 就會預期成逐格壓實, **那正是第 2 輪被否決的心智模型**。
  - **不標示任何塊會下落幾格。**
- **更新時機**: 消除完畢(4a)、每次重力事件下落完畢(4b)、追加消除完畢(4c)、**全盤清除完畢(4d, v19)**、削頂削除完畢。
- **分界判準(寫死)**: **懸空判定的輸入只有已鎖定格位; 下落中的方塊與 ghost 一律不參與, 也不因它們的存在而改變任何塊的懸空狀態。** 允許的是**現況讀數**, 禁止的是**任何落點試算** — 包含「這樣放會觸發多大的下落」「下落後會不會再消一批」一律不做。
- **本標示只回答「哪些結構此刻是浮著的」, 不回答「這次重力事件會不會牽動它」** — 後者取決於玩家把球放在哪、消掉的是哪一團, 屬於落點試算, 一律不做。**玩家仍必須自己判斷它和要消的那一團相不相連 — 這一層心算刻意保留, 它就是重力球的技術。**

#### 追加消除(下落之後的重新掃描)
**裁決: 消, 但深度上限 1, 且不給任何倍率。**
- 本次結算的**所有**重力事件都執行完畢之後, 若**有任何格位實際發生過位移**, 對定版盤面**再做一次**完整的成團掃描(含萬用色仲裁), 所有成立團照常消除。
- **追加消除照常計基礎分、照常推進開地任務(目標色格逐格 −1 並計目標色分數)、照常計入消除總顆數與目標色佔比**, 沿用本次結算開始時的當前任務、目標色與分數倍率。**不推進多樣化任務「大團」與「用新形狀方塊」**(兩者只計放下後的第一段消除, 理由見「多樣化任務」)。
- **不給任何倍率或加成**: 本作沒有連鎖倍率、沒有連鎖段數, 不得新增。
- **追加消除中被消掉的重力球不觸發任何重力事件**, 且**追加消除之後不再重新掃描**。整個結算的位移—消除深度**硬性上限為 1**。
- **追加消除中被消掉的清色球照常觸發全盤清除(v19 企劃提案, v20 定案接受)**: 全盤清除只移除格位、不造成位移、之後不重掃, 不增加位移—消除深度; 若不觸發, 玩家換來的清色球會被重力事件「吃掉」而毫無效果, 會被讀成 bug。
- **呈現時長 0.4 秒**, 僅在實際有追加消除時計入, t 照常推進; 與重力下落動畫是明顯分開的第二段。

**為什麼不是「不重掃」**: 不重掃會讓盤面留著一個 ≥4 卻沒消的團, 直接違反「局面上永遠不存在 ≥4 顆的團」這條不變式, 而且下一次結算的全盤掃描還是會把它消掉, 等於「延遲一塊才爆的連鎖」。**三個選項裡只有「消掉」是自洽的。**

**為什麼這不是把第 2 輪的連鎖從後門請回來(寫死, 任何後續討論以此為準)**:
1. **歸屬不同**: 第 2 輪的連鎖每次消除都自動發生、塌哪裡由系統決定; 這裡的位移**只在玩家刻意把球黏成團並消掉時發生**。製作人拍板的「差別是誰決定, 不是有沒有重力」在這裡是判準。
2. **有界**: 深度硬性上限 1, 不存在迴圈, 一次結算最多兩批消除。
3. **不給獎勵**: 沒有倍率、沒有段數。刷追加消除的期望收益等於同樣顆數的普通消除, 而它還要先花一顆球。
4. **可觀測**: 埋點單獨記錄發生次數與顆數, **紅線已定**(≥5 次/局, 或佔消除總顆數 ≥12%)。**觸發紅線時走退路順位第 ② 級(出現率), 不放寬也不收緊這條規則; 深度上限不得放寬到 2 以上。**

### 清色球(v19 新增為「連鎖球」, v20 改名與定案; 規則骨架由製作人定)

#### 是什麼
- **名稱(v20, 寫死)**: 玩家可見名稱與規格內部一律稱「**清色球**」。v19 名為「連鎖球」, 「連鎖」容易被讀成連消, 而本作不存在連鎖, 因此改名。
- **方塊中的某一格, 萬用色**; 與重力球同屬「萬用格」, 在成團判定中與重力球完全相同(見「重力觸發球 / 成團判定中的參與」)。**不單獨出現, 沒有道具欄, 沒有引爆鍵。**
- 它參與的成立團被消掉時, **那團的顏色在全盤的所有真顏色格一起被清掉**(以下稱「全盤清除」)。
- 使用者原話(第 7 輪):「我覺得中後期可以給一個獎勵是連鎖消除獎勵, 類似重力球可以當任何方塊, 當消除時則消除的該顏色會擴及到整個盤面, 所有盤面上該顏色的方塊都會跟著消除, 效果很強, 會是一個超大的正向反饋獎勵」。

#### 怎麼拿到(v20 定案)
- **第 1 與第 5 個多樣化任務的獎勵各給 1 顆; 每局上限 2 顆**(以本局獲得數計)。其餘多樣化任務不給。放置表與理由見「多樣化任務 / 獎勵」。

#### 生成(怎麼進到方塊裡)
- **待發清單**: 開局為空。每次給清色球的獎勵事件(第 6 步)中:
  - 若本局獲得數 < 2: 加入 1 顆到待發清單, 並記下它的「**最早出現塊序**」, 本局獲得數 +1。
  - 若本局獲得數已達 2: 不加入, 記一筆「被上限擋下」(現行放置下不會發生)。
  - 令 N 為執行該獎勵事件的那次結算所屬方塊的塊序; 之後依序生成為當前方塊的是「獲得後第 1、2…塊」(塊序 N+1、N+2…)。
  - **第 1 個多樣化任務給的那顆(第 1 顆, v20)**: 最早出現塊序 = **N + 15**, 即**獲得後第 15 塊一定含清色球**。
  - **第 5 個多樣化任務給的那顆**: 最早出現塊序 = **N + 2**(照 v19 原規則: 獎勵事件後第一個新抽出的預覽; 獎勵當下已在預覽中的 N+1 不改寫)。
- **出入口(寫死)**:
  - **入口**: 每次**抽出新的一塊作為下一塊預覽**時(結算時序第 8 步), 若待發清單非空, 取其中最早獲得的那顆; 若新預覽的塊序 ≥ 它的最早出現塊序, 且該塊**不是重力球 bag 排定的含球塊**, 且**目前的當前方塊不含清色球**, 則該塊含這顆清色球, 自清單移除。否則本次不放, 留到下一次抽預覽再判。
  - 結果: 第 1 顆出現在獲得後第 15 塊, 第 5 個的那顆出現在獲得後第 2 塊; 該塊若是重力球排定的含球塊則**順延 1 塊**(重力球保證不連續, 因此最多順延 1 塊)。
  - **不改寫**: 預覽中已確定的那一塊不改寫; 重力球 bag 不改寫、不佔用(decisions: 清色球不佔用重力球 bag)。
  - **兩顆清色球之間至少隔 1 塊**(同重力球的不連續理由: 第二顆要在第一顆的結果讀懂之後才決定)。
  - **出口**: 局終仍在待發清單中者作廢(記入埋點); 重開一局歸零。
- **哪一格是清色球**: 該塊各格依填入順序編索引, **等機率抽 1 個索引**, 不從顏色 bag 取色、不消耗 bag(與重力球相同)。
- **一塊最多 1 顆萬用格**: 同一塊不會同時有重力球與清色球。
- **下一塊預覽與 ghost 必須讀得出該塊是否含清色球、在哪一格, 且與重力球可區分。** 這是放置規劃的前提資訊, 不屬於預測性提示。
- **第 1 顆延後期間的期待(v20)**: 由獎勵事件先展示清色球本身與那一句效果; 它出現在預覽中時, 玩家要認得出這就是獎勵事件展示過的同一個東西。不另加倒數或待發提示(見「Demo 範圍邊界」的預測性提示)。
- **只在滿寬之後才可能出現**: 給清色球的獎勵最早是第 1 個多樣化任務, 因此開地任務期間盤面上不存在清色球, 與目標色完全無交集。

#### 可以留在盤面上(v20 寫明)
- 放下後若沒有被湊進成立團, 清色球就作為萬用格**一直留在盤面上**, 不過期、不自動觸發、不移回待發。
- 之後任何一次結算中它被補進成立團(4a 或 4c), 才觸發全盤清除。
- 留著的期間照常佔格位、參與成團判定與 lock out / block out、可隨重力事件一起下落。
- **可能被削頂削掉**: 削頂時它視為一般已填格位; 被削掉不觸發全盤清除, 這顆就此消失, 不補發。
- 不被其他清色球的全盤清除移除(萬用格不清)。
- 盤面上可能同時有 2 顆(每局上限 2)。

#### 全盤清除的顏色
- **顏色 = 該成立團的顏色**, 即成團掃描程序第 2 步產生它時的 c。掃描程序每一團都只以一個顏色 c 建立, **一個成立團在結構上不可能含兩種真顏色**, 因此「團含多色」的情況不存在, 不需要另設規則。
- **團裡只有萬用格(例如清色球 + 重力球 + …)**: 純萬用格的團不成立消除(既有規則), 清色球不被消耗, 留在盤面上, 不發生全盤清除。
- **同一團含 2 顆清色球**: 仍只清該團的一個顏色, 一次。
- **同一團同時含清色球與重力球**: 兩者各自生效 — 重力球觸發重力事件(4b), 清色球觸發全盤清除(4d), 時序照下方。
- **同一次結算有多個含清色球的團**: 各團的顏色取聯集, 每種顏色清一次(同色不重複清); 不同色則各清各的。
- **清色球在仲裁中不享有優先**: 它貼著兩色時歸誰, 照既有仲裁順位決定, 與重力球一樣; 玩家要靠落點與旋轉決定它補哪一團。

#### 全盤清除做什麼
- 清除範圍: **全盤所有顏色 = 清除色的真顏色格, 含列 20 以上的不可見區**。
- **萬用格不清**: 盤面上其他重力球與清色球沒有顏色, 不被全盤清除移除, 原地保留(也不觸發)。
- **不觸發重力下落(寫死)**: 只留空洞, 其餘格原地不動 — 與削頂同一個原則。理由: 一次清掉約三分之一的盤面會冒出大量懸空結構, 若讓它們一起掉就是第 2 輪被否決的「系統決定塌哪裡」; 塌不塌仍由玩家用重力球決定。
- **不重新掃描成團**: 只移除格位不會產生新的相連, 不可能出現新團。
- **得分(v20, 推翻 v19 的不計分)**: **每一次全盤清除固定 +500 分, 不乘分數倍率**。**一種清除色算一次**: 同一次結算兩顆清色球清同一色只算 1 次, 清兩種顏色算 2 次(企劃依「每種顏色清一次」詮釋, 見「待定」)。被清掉的格**不計基礎分、不按格數計**。
  - 理由: 清色球的供給被任務卡死, 本來就刷不了分; 大演出配分數不動反差太大。不按格數計, 避免玩家囤著等最大一次。
- **不推任何任務進度、不計入消除總顆數與目標色佔比**(寫死); **只有清色球參與的那一團照常計**(基礎分, n 含萬用格; n ≥ 5 固定額; 大團與用新形狀方塊的計入照該團所在的 4a / 4c 規則)。
- 被清掉的格位若屬於「本塊格位集合」, 一併移除(本塊被清光也算救回一命, 與削頂一致)。
- **清除完畢後重算懸空結構標示。**
- **呈現(寫死)**: 一個**獨立、可單獨辨識**的事件, 在消除結算、重力下落與追加消除之後單獨一段演出, 讓玩家看出「這個顏色在全盤一起消失」, 並呈現這次加的 500 分; 不與其他段疊在一起。**呈現時長 1.0 秒(企劃提案)**, t 照常推進。這是它存在的理由, 不得以「只是表演」為由縮短。
- **不預告**: 放下前不標示「這樣放會清掉哪些格」, 屬落點試算, 不做。
- **實測與推算提醒(v20)**: 一次全盤清除的「格數」大(約 1~3 次削頂), 但對堆高只有約 0.5~0.65 次削頂 — 它的爽點在畫面份量, 空間價值要靠之後的重力球兌現。試玩加問「清掉那一片之後, 你覺得有得到什麼嗎?」(見「埋點 / 試玩要看的三件事」)。

#### 在結算時序中的位置(寫死)
- 位於 **4c 追加消除之後、4e 定版之前**, 編為 **4d**。4a 與 4c 中被消掉的清色球都觸發, 顏色合併後在 4d 一次清除。
- **為什麼在重力事件與追加消除之後**:
  - 重力事件的受影響集合要在「玩家放下時看得到的盤面」上計算; 若先全盤清除再做重力事件, 清除冒出的大量懸空結構會被種子拉進下落, 等於系統決定塌哪裡。
  - 全盤清除不造成位移、不重掃, 放在最後不會再引發任何後續消除, 結算深度不變。
- 與削頂: 削頂在第 6 步, 必在全盤清除之後; 兩者都只移除格位。

### 消除
- **成團定義**: 同一顏色的格位以上下左右四方向相連構成一個連通團; **萬用格(重力球、清色球)可代替任何顏色參與連通**。**對角相鄰不算相連**。
- 連通團的格數 ≥ 4(且至少含 1 個真顏色格)時, 該團成立消除, 整團(不只 4 顆)全部消失。
- **消除本身不造成位移(結構, 寫死)**: 被消掉的格位變成空洞, **盤面上其餘所有格位一律留在原位**, 不向下壓實、不橫向滑落。
- **唯一的位移來源是重力事件**: 任何格位在放下之後的座標永遠不變, **除非它被某次重力事件納入受影響的懸空連通塊而整體下落**。**不存在任何系統自動發生的沉降。** 全盤清除只移除格位, 不造成位移(v19)。
  - **這條不得被擴大解釋**: 不得因為「局太短」「洞太多」而加入任何不依賴重力球的自動沉降、定時壓實或保底清盤。
- **連鎖倍率與連鎖段數在規則上不存在(寫死)**: 追加消除深度硬性上限 1, 且**不給任何倍率或依深度遞增的計分**。任何文件不得出現「連鎖倍率」「連鎖段數」作為本作的機制名詞。「清色球」是一種萬用格的名稱(v20 自「連鎖球」改名, 正是為了不被讀成連消); 它的全盤清除固定 500 分, 不是依深度遞增的計分。
- **緩衝列與其上的不可見區全程參與成團判定(寫死)**: 不因不可見而享有任何豁免, 可構成連通團、可被消除、可被削頂削除、可被重力事件牽動下落、可被全盤清除。
- **判定時機為方塊鎖定後統一判定**: 依「成團掃描程序」掃描全盤(含不可見區), 一次性決定所有成立團並同時消除。不逐顆落定即判。
- 同一次判定中若有多個成立團(含不同顏色), 一律同時消除, 不分先後。**唯一的例外是萬用格的歸屬仲裁**, 它決定的是「哪個團成立」, 不是「哪個團先消」。
- **開地任務計數逐格判定**: 一個團被消掉時逐一檢查其每一個格位, 顏色 = 當前目標色者扣減剩餘; **重力球一律不計**; 其餘不扣。多樣化任務的計數見「多樣化任務」。
- 非目標色的團正常消除、正常計基礎分, 但不推進開地任務。
- 鎖定的方塊本身若與既有格位構成成立團, 照常成立消除; 不存在「剛落下的格位免疫一次判定」。新外型塊內自己湊成的成立團同樣照常消除。
- **團的顆數只影響基礎分的線性項與 n≥5 的固定額**, 不存在隨顆數遞增的加成。**n 含團內的萬用格**(它佔了一格、也被消掉了), 但萬用格不計目標色分數。
- **目標色分數**: **只在開地任務期間**, 被消掉的格位中顏色 = 當前目標色者每顆 **+12 分**, 不分位置, 乘分數倍率。多樣化任務期間沒有目標色, 也沒有目標色分數。
- **同色團不顯示顆數(v14, 寫死)**: 盤面上不標示任何團的顆數; 玩家以盤面本身判讀。
- **消除結算標記不標示團內含幾顆萬用格(v15, 寫死)**: 與不顯示顆數同一方向; 萬用格本身在盤面上可辨識, 不另給數字。

### 死局與盤面耗盡
- **本作不存在任何保底清除手段**。整列填滿不會消除, 也沒有全盤下沉、定時清列、雜色轉色等補償。清色球的全盤清除是玩家做任務換來、一局最多 2 顆、且由玩家決定何時用的獎勵, 不是保底(v20)。
- **空洞不會自動被填回去(核心收局機制)**: 回收空間只有四條路, **全部要玩家自己換來**:
  1. 從正上方把落下的方塊的格位落進去(封閉洞塞不進);
  2. **消掉重力球, 讓相連的懸空結構整體落下填掉下方的洞**;
  3. 削頂(v18: 只在第 4 個開地任務與第偶數個多樣化任務的獎勵, 不設次數上限)把洞上方的格位移除, 把封閉洞變回開放空洞;
  4. **清色球的全盤清除(v19; v20: 一局最多 2 次)**: 移除一整個顏色, 把大量封閉洞變回開放空洞, 同時留下大量懸空結構, 之後由玩家用重力球決定塌哪裡。
- **蜂窩化仍是一條正回饋**(消除率低 → 包絡漲快 → 落距短 → 決策時間短 → 消除率更低)。**v20: 前 3 個開地任務期間的煞車只有重力事件; 滿寬那次削頂一次, 之後每完成兩個多樣化任務削頂一次, 第 1 與第 5 個多樣化任務各給一顆清色球, 加上重力事件**, 都要靠玩家換來。
- **滿寬後的反向正回饋(v17 實測, v18 改解法, v20 收斂清色球)**: 強玩家會進入「削頂 → 盤面變低 → 任務更容易 → 又拿削頂」的循環。v17 以加重大團(2c − 1)與「用新形狀方塊」的需求序列壓住; v18 數值算出需求序列拉不回來(每次削頂換回的存活塊數比完成任務花的塊數還多), 改為**削頂每兩個多樣化任務一次**。數值 r2 算出的臨界仍作為判讀: 兩次削頂之間的塊數要大於約 3 × 每次削頂回收 ÷ 每塊淨增。**v20: v19 企劃提案的「全解鎖後每個任務一顆清色球」經數值以第 7 輪實測推算, 會讓強玩家拉到 350~375 秒且可能自我維持; 改為一局只給 2 顆(第 1、5 個), 不隨任務數增加; 第 3 輪起需求改 3c − 2。**
- **收局主力仍是「盤面被自己的洞吃光」, 不是落速。**
- **局長偏離目標時的調整順位(寫死, v20 改)**: **盤面高度(只往上)→ 清色球次數與位置 → 多樣化任務需求量 → 重力球出現率 → 結算呈現時長。落速不在清單內; 削頂自 v14 起固定每欄 1 格, 不再是旋鈕。**
  - **盤面高度是往上調的旋鈕**: 維持 19 列; +1 列的影響待數值以實測式重推。
  - **清色球次數與位置(v20 新增第 2 順位)**: 它是局長最敏感的新旋鈕; 可動的是給在第幾個多樣化任務、每局上限幾顆、第 1 顆延後幾塊。
  - **多樣化任務需求量**: 需求變低, 新外型與落速階梯來得更早(後段更硬), **但削頂與第 5 個的清色球也跟著來得更早(後段更鬆)**; 需求變高則相反。兩個方向互相抵銷, 這個旋鈕的淨方向待數值判斷(見「待定」)。
  - 重力球出現率往下調會連機制的存在理由與玩家的練習量一起砍掉, 對「運氣感」是反效果。
  - 呈現時長只影響秒數不影響塊數, 是最末端的微調。
  - **落速不調**: 決策時間的壓縮絕大部分來自落距; 動它既改不動局長, 又會讓所有塊數時點與供給推算全表重算。
  - **不得恢復系統性沉降, 不得加保底清盤。**
- 盤面填滿但尚未觸發結束條件時, 遊戲照常繼續, 直到滿足 block out 或 lock out。**不做「無處可放即結束」的提前判定。**
- 玩家可隨時以「放棄本局」主動結束已無救的一局, 該局照常結算, 但標記為放棄局。

### 結算時序
方塊鎖定後依下列順序執行, 順序寫死不可調換。

1. 方塊鎖定, 進入結算狀態, **鎖住玩家所有方塊操作輸入**(僅暫停、叫出說明與放棄長按仍受理)。記錄本次鎖定方塊的所有格位為「本塊格位集合」, **並記錄本塊的形狀(供「用新形狀方塊」判定)**。**取樣本次結算的當前任務、目標色(開地任務期間才有)與分數倍率, 本次結算全程沿用。** (埋點, v18: 本塊鎖定前的盤面懸空連通塊數在此之前取, 不含本塊。)
2. **依「成團掃描程序」掃描全盤(含不可見區), 取出所有成立團**(含萬用格的萬用色仲裁)。無成立團則跳至第 5 步。
3. 對所有成立團累加各團基礎分(n 含團內萬用格; 含 n≥5 的固定額)與目標色分數, 並**推進當前任務**: 開地任務逐格扣減剩餘(顏色 = 目標色者 −1, 重力球不計); 「大團」每個 n ≥ 5 的團 −1; **「用新形狀方塊」: 本塊為已解鎖新外型時, 每個含本塊格位的成立團 −1**。**剩餘允許扣成負數, 負數的絕對值即為溢出量。** **本步不做任何任務推進、不做達成判定、不觸發任何獎勵事件。** 同時把被消掉的格位自「本塊格位集合」中移除。
4. **消除、重力事件、追加消除與全盤清除(本步內依序執行 4a → 4e)**:
   - **4a 清除**: **一次性清除所有成立團的格位**, 該處變為空洞, 盤面其餘格位原地不動。重算懸空結構標示。**消除結算呈現時長 0.4 秒於此計入**(本次無成立團則為 0); 本次有推進當前任務時, 消除結算標記同時呈現「這次算數了」的回饋。**被清除的團若含清色球, 記下該團的顏色(清除色, v19)。**
   - **4b 重力事件**: 若本次被消除的格位中含重力球, 以**含球團為單位**、依錨點順序**逐一執行重力事件**(種子 → 受影響集合 → 懸空判定 → 剛體逐塊落到底)。**每個事件在前一個事件的結果盤面上重新計算受影響集合, 種子以原團座標為基準。** 當前任務為「重力下落」時, 每個實際發生位移的事件剩餘 −1。每次下落完畢後重算懸空標示。**每個事件的判定先完成, 再依「重力下落的呈現」演出動畫; 呈現時長依落距計算(零位移 0.2 秒且必須有可單獨辨識的回饋), 每個事件各計一份。**
     - **本塊格位集合中的殘存格位, 若隨某個受影響連通塊一起下落, 其座標必須同步更新**(供第 7 步的 lock out 判定使用)。這是全規格唯一需要座標同步的地方。
   - **4c 追加消除**: 若 4b 有任何格位實際發生過位移, 對定版盤面**再做一次**成團掃描並消除。**照常計基礎分與目標色分數、照常逐格扣減開地任務剩餘(沿用第 1 步取樣的任務、目標色與倍率)、照常計入消除總顆數; 不推進「大團」與「用新形狀方塊」; 不給任何倍率或加成。** 被消掉的格位同樣自「本塊格位集合」中移除。**其中的重力球不觸發任何重力事件, 且本步之後不再重新掃描 — 深度硬性上限 1。** 其中的清色球照常記下清除色(v20 定案)。重算懸空標示。**呈現時長 0.4 秒於此計入, 與 4b 的動畫明顯分開。**
   - **4d 全盤清除(v19; v20 加計分)**: 若 4a 或 4c 記下了清除色, 對每一種清除色(聯集, 同色一次), 移除全盤所有該色的真顏色格(含不可見區; 萬用格不移除)。**每一種清除色加計固定 500 分, 不乘分數倍率(v20); 被清的格不計分、不推任何任務、不計入消除總顆數、不觸發重力事件、不重新掃描。** 被移除的格位自「本塊格位集合」中一併移除。重算懸空標示。**呈現時長 1.0 秒於此計入(企劃提案; 本次無清除色則為 0), 與前面各段明顯分開。**
   - **4e 定版**: 本步結束後盤面定版, 後續步驟不再改變任何格位的座標(第 6 步的削頂只移除格位, 不移動格位)。(埋點, v18: 當前任務為「用新形狀方塊」時, 本次 4a / 4c 有來自新外型的格位被消掉、但本次結算對它的推進為 0 者, 於此記一筆「以為做到卻沒算」; 4d 清掉的新外型格不列入。)
   - 上述所有呈現時段 **t 照常推進**。
5. **任務推進(A 的唯一發生點, 寫死)**: 若當前任務(第 A+1 個)剩餘 ≤ 0, 則 **A ← A+1**, 並**在此刻才為新進入的任務設值**:
   - 新任務為開地任務(第 2~4 個): 剩餘 = max(1, 需求 − 溢出量)(超出部分捨棄); 目標色 = 自 3 色排除上一個任務的色後 2 色等機率抽 1。
   - 新任務為多樣化任務(第 5 個起): 種類 = (k − 5) mod 3(0 大團 / 1 用新形狀方塊 / 2 重力下落); 剩餘 = 需求(大團與用新形狀方塊: c ≤ 2 時 2c − 1, c ≥ 3 時 3c − 2(v20); 重力下落 = 1; 不溢出, 超出量捨棄); 不設目標色。
   - **每次結算最多推進 1 個任務。** 本步每次結算都執行, 即使本次結算沒有任何消除。
6. **獎勵事件(E 的唯一發生點)**: 若 **E < A**, 對第 E+1 個任務執行一次獎勵事件(獨立事件)。事件內的**削頂**一律照「削頂」節執行: 每一欄削掉最上面 1 個已填格位; 被削除的格位自「本塊格位集合」中一併移除, 不計分、不推任務、不觸發消除、**不觸發重力事件, 被削的清色球不觸發全盤清除**; 削除完畢後重算懸空標示。(埋點, v18: 逐削頂事件的「削前 Σ欄高」在 4e 定版之後、削頂執行之前取。)
   - **6-1 延展事件(E+1 ≤ 4)**: 依序 ① 依交替順序(第 1、3 次左, 第 2、4 次右)向外新增 1 欄, 全空, 寬度 +1 ② **僅第 4 次延展: 削頂**(v16; 第 1~3 次跳過本項) ③ 若本次為第 4 次延展, 於同一事件內加計滿寬完成獎勵 1,500 分(不乘倍率) ④ **E ← E + 1** ⑤ **尾端公告下一個任務**(做什麼 / 得到什麼 / 還差多少; 下一個是開地任務時含新目標色)。**呈現時長 1.2 秒。**
   - **6-2 多樣化任務獎勵事件(E+1 ≥ 5)**: 令 d = E+1 − 4。依序 ① **解鎖(d ≤ 3)**: **解鎖第 d 種新外型**, 事件中展示其外型並稱之為「新形狀方塊」, 記入已解鎖清單, 自下一個形狀 bag 起加入發牌(2 張, 首發保證與其 v17 邊界) ② **清色球(d = 1 或 d = 5, v20)**: 展示清色球與那一句效果說明; 本局獲得數 < 2 時加入待發清單並記最早出現塊序(d = 1: 本塊塊序 + 15; d = 5: 本塊塊序 + 2), 獲得數 +1; 已達上限則不加入, 記「被上限擋下」 ③ **僅 d 為偶數: 削頂**(v18) ④ **E ← E + 1**, 並**依新的 G 更新落速曲線**(封頂 2 階) ⑤ **尾端公告下一個任務。** d ≥ 7 的第奇數個 ①~③ 皆無, 事件只呈現任務完成與尾端公告(v20)。**呈現時長 1.5 秒(v17; 單段與沒有獎勵時也維持 1.5 秒)。**
   - **每個獎勵事件的呈現時長於事件內所有動作(含削頂與尾端公告)完成後計入**, t 照常推進。**每次結算最多執行 1 個獎勵事件。**
7. **結束判定(block out 與 lock out 在此同一步一起判)**。判定使用**本次結算全部完成後的定版盤面與寬度**: lock out 以本塊殘存格位判定(**座標為第 4b 步同步更新後的座標**); block out 以下一塊在定版盤面上的生成格位試算是否重疊。
8. **未結束則解除輸入鎖定, 實際生成下一塊**(原預覽成為當前方塊, 並抽出新的預覽; **清色球的入口判定在抽出新預覽時執行**, v19)。本步在上述所有呈現時段之後才執行。

- **結算期間本局經過時間 t 照常推進**, 落速不凍結。
- **結算呈現時長是遊戲參數, 不是演出細節(寫死)**: **消除結算 0.4 秒**(有成立團才計)、**重力事件依落距計算 × 事件數**(零位移 0.2 秒)、**追加消除 0.4 秒**(有追加消除才計)、**全盤清除 1.0 秒(v19, 有清除色才計, 企劃提案)**、**延展事件 1.2 秒、多樣化任務獎勵事件 1.5 秒(v17)**。這些值**直接吃掉玩家的操作時間並計入局長**, 不得以「只是表演、可以縮掉」為由砍短。
  - **可調範圍**: 消除結算 **0.2~0.6 秒**; 重力事件的每格時長、每塊最短時長、落地停頓與單次上限 v15 定案為 0.06 / 0.15 / 0.12 / 1.5 秒(**下界理由是玩家必須看清「掉下來的是哪一塊、掉了幾格、掉完發生什麼」, 這是重力球從工具變成可規劃資源的唯一學習管道**), **零位移固定 0.2 秒且不得降為 0 或無聲跳過**; 追加消除 **0.3~0.6 秒**(**必須與重力下落動畫在時間上明顯分離**, 否則玩家會把兩段讀成同一批, 就真的變回「突然連消」); 全盤清除 **0.8~1.5 秒**(v19, 企劃提案); 獎勵事件 **0.8~1.5 秒**(多樣化任務獎勵事件已在上限)。
  - 呈現時長是局長旋鈕的**末位**。
- **結算期間(含消除、重力事件、追加消除、全盤清除、延展、削頂、解鎖、給清色球)所有方塊操作輸入一律無效, 不緩衝不排隊**; 僅暫停、叫出說明與放棄長按受理。**按下暫停或叫出說明一律排隊, 於第 8 步生效**; **同一次結算期間 Esc 與 H 都按了, 第 8 步以 H 為準**(說明畫面本身就含暫停); 若該次結算即判定遊戲結束, 排隊中的請求作廢。**放棄長按在結算期間照常累計與生效。**
- **輸入鎖定解除時的銜接(寫死)**: 結算期間發生的**離散輸入(旋轉、硬降、軟降的按下事件)一律丟棄**, 不補發。但**結算期間持續按住不放的左右方向鍵, 於第 8 步解除鎖定時沿用既有的 DAS/ARR 狀態繼續計時, 不重新起算**。

### 方塊與下落
- **方塊生成位置(寫死)**: 方塊的**外框底緣固定在列 20**(外框指該旋轉態下所有實際佔格的 bounding box)。外框**左緣相對欄 = ⌊(w − 形狀寬) / 2⌋**(偶數寬度時向左取整)。延展後生成位置依新寬度重算。
- **原七種形狀的 spawn 朝向寫死如下**(新外型見「新外型方塊 / 生成與旋轉」):
  | 形狀 | 形狀寬 | 外框高 | 佔格(列 20 = 底列) |
  | --- | --- | --- | --- |
  | I | 4 | 1 | 列 20: 相對左緣 +0、+1、+2、+3 |
  | O | 2 | 2 | 列 20: +0、+1; 列 21: +0、+1 |
  | T | 3 | 2 | 列 20: +0、+1、+2; 列 21: +1 |
  | J | 3 | 2 | 列 20: +0、+1、+2; 列 21: +0 |
  | L | 3 | 2 | 列 20: +0、+1、+2; 列 21: +2 |
  | S | 3 | 2 | 列 20: +0、+1; 列 21: +1、+2 |
  | Z | 3 | 2 | 列 20: +1、+2; 列 21: +0、+1 |
- **I 形只佔列 20 一列, 因此比其他形狀略不容易觸發 block out, 不做補償(寫死)。**
- 方塊依當前落速自動下落; **落速 = min(4.0 × 2^(t/290) + 0.5 × min(G,2), 6.0 + 0.5 × min(G,2)) 格/秒**, t 為本局經過秒數(暫停與說明畫面期間不推進), **G = max(0, E − 4) = 已完成的多樣化任務數**(未完成任何多樣化任務時 G = 0, 公式退化為 min(4.0 × 2^(t/290), 6.0))。基準曲線約於 t ≈ 170 秒觸頂; **階梯封頂 2 階, 落速上限最高 7.0 格/秒**。
  - **為什麼用 G(E 的函數)而不是已達成任務數**: A 在第 5 步增加、E 在第 6 步增加, 兩者之間隔著獎勵事件的呈現且 t 照常推進; 用 A 會讓那段時間的落速歸屬沒有定義。**用 G 則生效點唯一, 且與玩家看到獎勵的那一刻對齊。**
  - **為什麼封頂 2 階**: 階梯的代價落在「玩家剛拿到獎勵」的那幾塊, 而真正想加難度的局末只差極少 — 不封頂等於把壓力加在錯的位置。
  - **誠實的限制**: 每塊決策時間的壓縮絕大部分來自落距, **單靠落速階梯加不動多少實質難度**。滿寬後難度的實際來源是新外型本身與盤面自身的蜂窩化。**若實測後段仍不夠難, 不要再往上堆落速。**
- **落速原則(寫死, 這一條是規格, 上面的數字是為了守住它而定的)**:
  - **開局每一塊至少要有 4~5 秒的可用決策時間。**
  - **到局末每一塊仍不得低於 1.5 秒**(以「盤面堆到一半」為局末基準情境)。
  - 「可用決策時間」定義為: 方塊自生成到在當前堆高上自然落地所經過的秒數, 不含硬降與軟降。
  - **落速三參數的基準值定案後不再調整**: 決策時間的壓縮絕大部分來自落距變化(v17 實測: 每塊秒數約 4.4 − 0.19 × 平均堆高, 由堆高決定而不是塊序; v20 第 7 輪確認成立)。**局長的旋鈕見「死局與盤面耗盡」的順位, 不是落速。**
- **更新粒度**: 固定更新步長 1/60 秒。**單一更新步內的處理順序寫死如下**:
  1. 取本步輸入, 依 DAS/ARR 產生至多 1 次橫移與至多 1 次旋轉, **橫移先於旋轉**; 若本步有硬降輸入, 於此處立即執行硬降並跳過本步的步驟 2。
  2. 依落速累積下落距離, 單步可下落多格但**逐格判定碰撞, 不得穿透**。**單步最大下落格數上限為 21 格**(保護值; 落速上限 7.0 下單步實際最多 0.12 格, 軟降 12 格/秒下最多 0.2 格, 此上限不會觸及)。
  3. 判定是否接觸下方障礙, 處理鎖定延遲的進入、重置與逾時鎖定。
  - 結論: 同一步內玩家的橫移與旋轉永遠先於該步的下落結算。
- 方塊接觸下方障礙時進入鎖定延遲 0.5 秒; 期間任何成功的移動或旋轉重置計時, **單一方塊最多重置 1 次**。
- **軟降仍走完整鎖定延遲**; 只有硬降跳過鎖定延遲。硬降時方塊立刻落到最低可行位置並即時鎖定。
- **簡化旋轉踢牆**: 原地旋轉若與牆或既有格重疊, 依序嘗試向左平移 1 格、向右平移 1 格; 任一可行即採用, 兩者皆不可行則旋轉失敗。不做完整 SRS 踢牆表, 不做上推。新外型同樣適用。
- **旋轉向上溢出不構成失敗(寫死)**: 盤面上方不設硬界, 旋轉後格位落到**列 22 以上**一律合法。
- 每格顏色隨旋轉一同轉動。**萬用格那一格同樣隨旋轉移動, 它在旋轉上與一般格位完全相同。**
- **O 形方塊的旋轉不是 no-op(寫死)**: O 形外框位置與佔格不變, 但其 **2×2 顏色矩陣必須繞中心真實旋轉 90 度**, 逆時針鍵反向, 180 度鍵為對角互換。**含萬用格的 O 形, 其位置同樣隨矩陣旋轉。** 十字形同理(見「新外型方塊」)。
- **提供 180 度旋轉鍵**, 同樣適用簡化踢牆, 新外型同樣適用。
- **下一塊預覽固定為 1 塊(寫死)**: **不提供 2 塊以上的前瞻**; 預覽須能讀出形狀、每格顏色, **以及該塊是否含重力球或清色球與在哪一格**; **「用新形狀方塊」為當前任務時, 新外型帶小記號(v19)**。
- 不提供暫存(hold)。

### 暫停與說明畫面
- 暫停期間經過時間 t、落速、方塊狀態全部凍結。
- **暫停必須完全遮蔽盤面**, 使盤面內容不可讀; 下一塊預覽、任務面板(含目標色與下一次延展側)、**懸空結構標示**同時遮蔽。
- 暫停期間不接受任何方塊操作輸入, 只接受恢復、叫出說明與放棄長按。
- **結算期間按下暫停一律排隊, 於第 8 步生效**; 與叫出說明同時排隊時以說明為準。
- **放棄長按的計時基準為真實時間(寫死)**: 不受暫停凍結影響、不與 t 掛鉤。
- **遊戲內玩家說明(守則 R2, 寫死)**: 內容見 `guide.md`, 為可翻頁的靜態說明, 不是教學關卡。
  - **開場顯示**: 每次開啟遊戲時, 第一局開始前先顯示說明第 1 頁。此時局尚未開始: t = 0, 不生成方塊, 不抽第 1 個任務的目標色。
  - **開場說明何時可關閉**: **任何一頁都可立即關閉, 不強制翻完**。關閉即開始第一局(抽第 1 個任務的目標色、生成第一塊)。關閉時已按住的方向鍵(含 A / D)與硬降鍵, 必須先偵測到放開才被遊戲採計。
  - **可再叫出**: 進行中、暫停中、結束畫面皆可叫出(H)。進行中叫出 = 進入暫停並顯示說明(t 凍結、盤面遮蔽, 規則同暫停); 結算期間叫出則排隊至第 8 步生效。
  - **關閉(Enter 或 H)後回到何處**: 開場 → 開始第一局; 局中(含暫停中)叫出 → 回到暫停畫面, 需再按恢復才繼續; 結束畫面叫出 → 回到結束畫面。
  - **說明畫面中只受理翻頁(左 / 右方向鍵或 A / D)與關閉(Enter 或 H)**, 其餘輸入(含方塊操作、Esc、重開、放棄長按)一律無效; 只認 key-down 邊緣。第 1 頁按上一頁、最後一頁按下一頁皆無作用, 不循環。
  - **說明畫面中不受理放棄長按(寫死)**: 要放棄, 先關閉說明回到暫停畫面再長按 R。
  - 說明畫面不遮蔽任何規則判定的時序, 因為它只在局未開始、暫停或結束時存在。

## 操作

| 輸入(鍵盤) | 行為 |
| --- | --- |
| 左方向鍵 / A | 方塊左移一欄; **說明畫面中 = 上一頁**(左方向鍵或 A 皆可) |
| 右方向鍵 / D | 方塊右移一欄; **說明畫面中 = 下一頁**(右方向鍵或 D 皆可) |
| 上方向鍵 / W | 順時針旋轉 90 度(含簡化踢牆; O 形與十字形真實旋轉顏色) |
| Z | 逆時針旋轉 90 度(含簡化踢牆; O 形與十字形真實旋轉顏色) |
| X | **旋轉 180 度**(含簡化踢牆; O 形為 2×2 顏色矩陣對角互換, 十字形為外圍 4 格對角互換) |
| 下方向鍵 / S | 軟降, 下落速度為 **固定 12 格/秒**; 仍走完整鎖定延遲 |
| 空白鍵 | 硬降並立即鎖定 |
| 左右方向鍵長按 | 首次移動後延遲 100 毫秒(DAS)開始連續移動, 每 33 毫秒(ARR)一欄; **跨越結算期間持續按住者, 解鎖時沿用既有 DAS/ARR 狀態, 不重新起算** |
| 下方向鍵長按 | 持續軟降, 放開即恢復當前落速 |
| Esc | 暫停 / 恢復(暫停時凍結並遮蔽盤面); 結算期間按下則排隊至第 8 步生效(同一次結算內 H 也按了則以 H 為準) |
| H | 叫出說明畫面: 進行中按下 = 暫停並顯示說明; 暫停中、結束畫面皆可按; 結算期間按下則排隊至第 8 步生效, 與 Esc 同排時以 H 為準。**說明畫面中 = 關閉說明**(同 Enter) |
| Enter / H(說明畫面中) | 關閉說明: 開場 → 開始第一局; 局中叫出 → 回到暫停畫面; 結束畫面叫出 → 回到結束畫面。任何一頁皆可關閉 |
| R(遊戲結束時按下) | 立即重開一局 |
| R(進行中、結算中或暫停時長按 1 秒) | 放棄本局: 立即結束, 結束原因記為「玩家放棄」, 照常結算輸出但不更新最佳紀錄。未滿 1 秒放開則取消。長按以真實時間計。**說明畫面中不受理**, 須先關閉說明回到暫停畫面 |

- **只用鍵盤(守則 R1, 寫死)**: 不提供任何滑鼠操作。保留鍵盤是因為方塊的連續橫移(DAS/ARR)與旋轉在鍵盤上最穩定。
- **重力球不新增任何輸入(寫死)**: **沒有引爆鍵、沒有道具欄、沒有「保留這顆球」的功能。** 它的全部結果由方塊落在哪裡、旋轉成什麼朝向決定。
- **清色球不新增任何輸入(v19, 寫死)**: 同重力球 — 沒有引爆鍵、沒有道具欄、玩家不能保留或延後它的發出(第 1 顆的延後是規則決定, 不是玩家操作); 清哪一色、何時清, 全由它被放在哪、補進哪一團決定。
- **懸空結構標示不新增任何輸入(寫死)**: 常駐、無開關、不可切換、不可查詢細節。它是現況讀數, 不是查詢介面。
- **任務軌與新外型不新增任何輸入**: 任務種類與目標色由系統決定、延展側由規則交替、獎勵與解鎖與削頂與清色球自動發放, 玩家不挑、不確認、不改; 獎勵事件不需要玩家按鍵確認, 呈現時長(延展 1.2 秒、多樣化任務獎勵 1.5 秒)結束後自動繼續; 「用新形狀方塊」的面板小圖與預覽記號自動出現與移除。
- **結算期間的輸入處理(寫死)**: 所有方塊操作輸入無效且不緩衝; **離散輸入一律丟棄, 不補發**; 持續按住的左右方向鍵在第 8 步解鎖時沿用既有 DAS/ARR 狀態; 暫停與叫出說明排隊至第 8 步, 兩者都按了以 H 為準。
- **說明畫面的輸入處理(寫死)**: 只認翻頁(左 / 右方向鍵、A / D)與關閉(Enter、H)的 key-down 邊緣, 其餘輸入(含放棄長按)無效。
- **放棄本局後須先放開按鍵(寫死)**: 放棄長按滿 1 秒生效的瞬間, R 鍵進入鎖住狀態, 必須先偵測到該鍵放開才重新接受重開輸入。
- 落點指示(ghost)**全程顯示**, 標出方塊硬降後會停在的位置, **並呈現每一格的顏色, 含重力球或清色球在哪一格**; **不顯示任何「這樣放會消掉幾顆 / 會不會成團 / 會不會推進任務 / 會觸發多大的下落 / 下落後會不會再消一批 / 會清掉哪些格」的預測資訊**。**ghost 的格位不參與懸空判定。** ghost 不設開關。
- 暫停期間除 Esc、H 與放棄長按外所有輸入無效。

## 勝負與結束條件
- 本作無勝利條件, 只有結束與分數。
- 結束條件三者之一成立即結束:
  1. **Block out**: 下一塊實際生成的那一刻, 其生成格位中任一格與既有非空格重疊。「生成格位」即該形狀 spawn 朝向下的所有實際佔格(新外型為 3 / 5 / 5 格), 不含 bounding box 內的空格。**萬用格格位在此與一般已填格位完全相同。**
  2. **Lock out**: 本次鎖定方塊的**殘存格位全部位於盤面頂列(列 19)之上**, 即全部位於列 20 及以上。**殘存格位 = 本塊所有格中, 在本次結算的消除、重力事件、追加消除、全盤清除與削頂都執行完之後仍留在盤面上的格位。**
     - **殘存格位的座標必須同步更新**: 本塊的殘存格位若被第 4b 步的某次重力事件納入受影響的懸空連通塊, 會**隨該塊一起下落**, 其列號因此改變。**lock out 一律以第 4 步定版後的座標判定。**
     - 若本塊所有格全部被消掉、被全盤清除、被削頂削除, **不成立 lock out**(被獎勵削掉也算救回一命, 刻意保留)。**被重力事件帶著掉回列 19 以下同樣算救回一命。**
  3. **玩家放棄**: 長按 R 滿 1 秒(真實時間計)。
- **結束判定統一發生在「結算時序」第 7 步**, block out 與 lock out 同時點判定, 不在方塊鎖定的當下先判 lock out。理由: 先判會砍掉局末幾塊, 也會否定「這次結算剛好觸發削頂 / 剛好觸發重力事件把我拉下來 / 剛好用清色球清掉」這條翻盤路徑。
- **結束原因優先序(寫死)**: **放棄 > lock out > block out**。
- **block out 不做預判(寫死)**: 第 7 步的試算用的是**本次結算全部完成後的定版盤面與寬度**, 以及**確定會生成的那一塊**。
- **盤面填滿、無任何可消團、玩家已無可行落點, 都不構成獨立的結束條件。**
- 不設遊戲內時間上限。**收局主力是盤面蜂窩化**, 不是落速。**多樣化任務不設上限, 因此不構成收局機制。**
- 結束時凍結盤面, 顯示本局分數、存活秒數、已解鎖欄數、**已解鎖的新外型數**、**局終停在的任務(做什麼、還差多少)**、結束原因, 並與最佳紀錄比對。
- 最佳紀錄只保留單一筆, 以分數為比較基準。**玩家放棄的局次不更新最佳紀錄。**
- **每局結束(含放棄)自動下載本局試玩紀錄 JSON**, 見「埋點」。
- 結束後可立即重開一局, 盤面、寬度、任務進度(A、E 歸 0, 各任務的需求與目標色一律不預先設值)、第 1 個任務的目標色(重新抽)、延展側交替順序(重新從左開始)、**已解鎖的新外型(歸零)**、**清色球的待發清單與本局獲得數(歸零)**、速度、**三個 bag(形狀 / 顏色 / 重力球)**全部重置並重新隨機。重開不再顯示開場說明。

## 埋點
Demo 不做任何統計判定、不做 bot、不設判定門檻。結論由老闆試玩的體感給, 紀錄供製作人與數值對照。

**紀錄方式(守則 R3, 寫死)**: 遊戲逐筆與逐局記錄, **每局結束(含放棄)自動下載一份 JSON**, 檔名 **`gamelog-WebGame_SlimeTetris-<YYYYMMDD-HHMMSS>.json`**(時間取該局結束當下)。

**v20 改動**: 欄位與種類值中的「連鎖球」一律改為「清色球」; **修正**「連續 20 塊 Σ欄高淨變化(含削頂與全盤清除)的最小值」— 第 7 輪紀錄中仍是 v17 以前的舊定義(slidingMaxRecoverAvg20, 連續 20 塊回收量最大值), v17 的修改沒落地, **本版實作必須改對**; **新增**逐清色球的清除色選擇、清除前後盤面、清除後 10 塊內變化、出現到觸發的塊數、觸發時盤面清色球數、全盤清除得分, 逐重力事件距上次全盤清除的塊數, 逐局被上限擋下的清色球數(見「清色球」段); v19 的「其餘校正欄位待數值補」由上列取代。

**v19 改動**: 新增清色球(當時名為連鎖球)各欄位(照 decisions: 發出、參與消除、全盤清除格數、清除前後 Σ欄高與懸空結構數); 逐任務的獎勵欄位改記清色球; 刪除倍率獎勵相關欄位(倍率實際增量、「倍率已達上限」); 呈現時長分類加「全盤清除」。

**v18 改動**: 「用新形狀方塊」的被動基準改為新外型當場命中率(見「多樣化任務的被動基準」); 新增「以為做到卻沒算」、逐塊是否為首發那張、逐塊鎖定前懸空連通塊數、逐新外型「被發到幾次」自我校驗; 明定逐削頂事件削前 Σ欄高的取值時點; 任務種類值「用新方塊」改為「用新形狀方塊」。

**v17 刪除的欄位**: 一切「雙消」相關(開地期間依寬度記的「一次消兩團以上」次數、雙消任務的種類值); 開地期間依寬度分段的被動基準(改為滿寬後非當前任務時, 見下); 「連續 20 塊滑動平均的回收量最大值」(改寫, 見下); 「逐延展」裡的削頂欄位(v16 已移至逐削頂事件, 本版確認刪除)。

**v14 / v16 已刪或已改的欄位維持**: v13 第 5 階起輪替獎勵關卡的整段彙整、削頂格數按兩種來源分開計、整局與第 1~4 階兩種目標色佔比、每階達成行、顆數顯示相關皆不記。

### 回答驗證問題用(製作人決定)
- **開地階段的目標色佔比**(逐局一值): **開地任務期間消掉的目標色顆數 ÷ 開地任務期間的消除總顆數**。目標色以該格被消當下的當前目標色判定; 追加消除照常計入; **重力球不計入分子也不計入分母**; 削頂削除的格數不進分子、不進分母。
  - 讀數參照(隨紀錄一併寫入, 僅供讀數, 不構成判定門檻): **完全被動的期望 0.35; 3 局合併訊號線 0.45**。單局樣本太小不判讀。v17 實測: 被動 0.32、刻意 0.61~0.67, 0.45 分得開。
- **每個任務一筆**(逐筆): 第幾個任務、**種類**(開地 / 大團 / 用新形狀方塊 / 重力下落)、**要求**(需求量; 開地任務含目標色)、**進入時的塊數與秒數**、**達成時的塊數與秒數**、**獎勵**(開地: 延展側, 以及是否附削頂(只有第 4 個為是); 多樣化: 解鎖了哪一種外型(d ≤ 3)、**是否給清色球(v20: d = 1 與 d = 5 為是; 被上限擋下者另標)**, 以及**是否附削頂(第偶數個為是)**; d ≥ 7 的第奇數個記「沒有獎勵」)。
  - **「用新形狀方塊」任務另記(v17 新增)**: 期間被發到的新外型塊數(依外型分)、其中當場有消除的塊數、每次推進時的外型與推進團數。
  - (v19 製作人刪除: 「用新形狀方塊」期間放下新形狀方塊的比例 — 玩家不能挑方塊, 這個比例反映的是發牌不是行為; 認不認得出新形狀改看當場命中率與「以為做到卻沒算」)
  - **「以為做到卻沒算」次數(v18 新增)**: 「用新形狀方塊」是當前任務時, 一次結算中有來自新外型的格位被消掉、但本次對該任務推進為 0 的次數(一次結算至多記 1)。每筆記: 塊數、原因分類(**原 7 種放下的消除** / **追加消除** / 兩者皆有)、被消掉的新外型格數。直接量「玩家以為做到了卻沒算」; 對照試玩加問的回答讀。全盤清除(4d)清掉的新外型格不列入(v19)。
    - 為此, 紀錄內部要記住每個已鎖定格位是否來自新外型(只供本欄位, 不影響任何判定, 畫面上不呈現)。
- **局終停在哪個任務、還差多少**(逐局): 局終當下的任務序號、種類、剩餘量。
- **每種新外型一筆**(逐筆, 未解鎖者不記): **解鎖時點**(塊數與秒數)、**之後被發到幾次**(v17 修正: 以「生成為當前方塊」計, 含局終時生成即 block out 的那一塊, 不論是否放下; **v18 新增自我校驗: 必須等於逐塊生成紀錄中該外型的筆數**)、**第一次被發到的塊數**、**每次放下後當塊消除了幾顆**(該次結算 4a 與 4c 消除總顆數, 不含萬用格; 每次放下各記一個值; 4a 與 4c 分開記)。

### 清色球(v19 新增, v20 改名與新增欄位, 照 decisions「spec-review v19 的定案」)
- **逐清色球一筆**:
  - **發出**: 來源(第 1 或第 5 個多樣化任務)、獲得時的塊數與秒數、最早出現塊序、實際出現在第幾塊(生成為預覽時的塊序)、是否因重力球塊或間隔規則而順延。
  - **出現到觸發隔了幾塊(v20)**: 本顆所在方塊的塊序到它被消除那次結算的塊序之差(未觸發者記「—」)。
  - **參與消除**: 被消除時的塊數與秒數、發生在 4a 或 4c、所屬團的顏色與顆數(含萬用格)、該團是否同時含重力球。
  - **觸發時盤面上同時有幾顆清色球(v20)**: 本次結算 4a 之前盤面上(含本塊)的清色球數。
  - **4d 前的清除色選擇(v20)**: 全盤各色的真顏色格數(A / B / C 各一值)、清除色是否為當下最多的顏色(並列最多記是); 各欄頂格(該欄最高已填格)是清除色的欄數。
  - **全盤清除格數**: 本顆觸發的 4d 實際移除格數(同一次結算多顆同色時, 該次清除只記一次, 其餘顆標「併入」)。
  - **全盤清除得分(v20)**: 本顆觸發的那次清除加計的分數(500; 標「併入」者記 0)。
  - **4d 前後的盤面(v19, v20 擴充)**: 4d 執行前與執行後各取一次 — Σ欄高、懸空結構數(懸空連通塊數, 與懸空結構標示同一條定義)、**懸空格數(v20)**(懸空連通塊的格數合計)、**已填格數(v20)**、**封閉洞數(v20)**、**生成區欄高(v20)**(下一塊生成外框涵蓋各欄的最高欄高)。
  - **清除後 10 塊內(v20)**: 重力事件數與其位移格數合計; 第 5 塊與第 10 塊定版後的 Σ欄高; **被填回格數**(4d 移除的格位座標中, 之後 10 塊內重新變成已填的格數)。局終不足 10 塊者記實際塊數, 第 5 / 10 塊未到者記「—」。
  - **結局**: 被消除 / 被削頂削除 / 局終殘存在盤面上 / 局終仍未發出(待發中)。
- **逐重力事件另記(v20)**: **距上次全盤清除的塊數**(本局尚未發生過全盤清除記「—」)。
- **逐局彙整**: 清色球獲得數、發出數、被消除數、被削頂削除數、局終殘存數、局終未發出數、**被上限擋下數(v20; 現行放置下應恆為 0)**、全盤清除次數與得分合計(自我校驗: 獲得 = 發出 + 未發出; 發出 = 被消除 + 被削頂削除 + 局終殘存; 被上限擋下的不計入獲得)。

### 多樣化任務的被動基準(v17 改, v18 改「用新形狀方塊」)
- **大團與重力下落: 用滿寬後、該任務不是當前任務時的發生率**。理由: v15 以開地期間 9 欄段為基準, 實測比滿寬後偏低 32%, 會把被動讀成「有在做」。
- **記法(大團與重力下落)**: 盤面滿寬後的每一塊, 依當下的當前任務種類, 對另外兩種中的大團 / 重力下落各記「這一塊若它是當前任務會推進多少」, 並記該段的塊數:
  - **大團**: 本塊 4a 中 n ≥ 5 的團數(n 含萬用格)。
  - **重力下落**: 本塊 4b 中實際發生位移的重力事件數。
- **判讀(大團與重力下落)**: 以該任務非當前時的每塊發生率 p 算被動期望等待 需求 ÷ p 塊, 與「它是當前任務時」的實際等待塊數比較。
  - 參照線(3 局合併, 不構成判定門檻): **實際 ÷ 期望 ≤ 0.6 為有在做、≥ 0.85 等同被動**; 未達成的任務以已耗塊數計入。
- **「用新形狀方塊」(v18 改)**: 改記**新外型塊放下時當場有消的比例**(命中率; 「當場有消」= 本塊 4a 有含本塊格位的成立團, 同時記推進團數), **依外型(角形 / 杯形 / 十字形)、首發 / 非首發、是否為當前任務分開記**。
  - 理由: 首發那張幾乎必中(實測 4/4), 非首發約 0.31、角形約 0.28; 把首發與各外型混在一個每塊發生率裡, 會把被動讀成「有在做」。v17 的「滿寬後非當前任務時每塊發生率」記法對本任務不再使用。
  - **判讀**: 以「非當前任務時、同外型、非首發」的命中率為被動基準, 與「當前任務時、同外型、非首發」的命中率比較。**第 1 輪不判讀**(需求 1 且幾乎必在首發那張完成), 從第 2 輪起讀。判讀參照線見「待定」。

### 保留 v13 的每局彙整與重力球相關
- **每局彙整**(逐局): **局序**(本次開啟遊戲後第幾局, v17 新增)、總塊數、總秒數、**硬降使用率**、四次延展各自的塊數與秒數(未發生記「—」)、已解鎖欄數、消除總顆數、**目標色顆數**、**每塊消除顆數**、消除團大小分佈(n=4/5/6/7+)、**削頂次數**、**削頂削除總格數**、各欄最終高度、**最終封閉洞數**、結束原因、最終分數、最終分數倍率。
- **重力球相關彙整**(逐局):
  - **重力球出現總數 / 被消除數 / 被削頂削除數 / 局終殘存數**(四者之和須等於出現總數, 自我校驗式)。
  - **重力事件觸發次數**、**實際發生位移的事件數**、**下落格位總數**、**單次事件最大位移格位數**、**單次事件最大下落格數**。
  - **追加消除發生次數**與**追加消除顆數**。
  - **「空轉事件」次數與空轉率**(空轉率 = 零位移事件數 ÷ 觸發次數)。**這是判斷受影響集合定義是否取得太窄的直接量, 也是重力球機制成敗的瓶頸指標。**
  - **含球團貢獻的真顏色顆數**: 所有含重力球的成立團裡, 真顏色格的顆數合計。用來算 **E_無球 = (消除總顆數 − 含球團真顏色顆數 − 追加消除顆數) ÷ 總塊數**, 把「技術好」與「球用得好」分開讀。
- **大型重力事件一筆**(逐筆): 僅在**單次事件位移格位數 ≥ 10** 時記錄 — **當時塊數、觸發團的顆數與顏色、受影響連通塊數、位移格位總數、最大下落格數、是否引發追加消除**。**大事件的頻率就是「有沒有變成沉降」那條紅線的觀測值。**
- **定義(寫死)**:
  - **硬降使用率 = 以硬降結束的塊數 ÷ 總塊數**。
  - **消除總顆數不含萬用格**: 萬用格沒有顏色; 算進去會讓每塊消除顆數整體上抬, 破壞它與供給的比例關係。重力球單獨計在「重力球被消除數」, 清色球計在「清色球」段。**全盤清除的格數不計入消除總顆數(v19)**, 單獨記在「清色球」段。
- **紅線(帶懸空標示之後的值; 讀數以佔比為主、次數為輔)**:
  - **空轉率**: 預期 **0.25~0.35**; **綠 ≤0.25 / 黃 0.25~0.45 / 紅 ≥0.50 且 3 局皆然**。**反向警戒 ≤0.10** — 低到這個程度代表受影響集合取得太寬, 要往「是不是快變成沉降」的方向看, 對照大型事件佔比。
  - **大型重力事件(≥10 格)**: **佔比門檻 15% / 30%**; **次數門檻 綠 ≤1.2 次/局 / 黃 1.2~3 次/局 / 紅 ≥4 次/局**。
  - **追加消除**: 預期 **1.5 次/局, 約佔消除總顆數 4%**; **紅線 ≥5 次/局 或 佔比 ≥12%**。觸發紅線走重力事件退路順位第 ② 級, 不動深度上限。

### 校正推算用(數值規劃, v15 列出, v17 修正與新增, v18 新增, v20 修正)
- **逐塊, 生成時**: 塊序、形狀、格數、是否含球(**v19: 分重力球 / 清色球 / 無**); **生成區所在欄的最高欄高(v17 新增)**(生成外框涵蓋的各欄中最高的欄高); **是否為首發那張(v18 新增)**(被首發保證排定的那一張為是, 其餘為否)。
- **逐塊**: 生成時 t、鎖定時 t、結算結束時 t(校正每塊秒數曲線)。
- **逐塊, 鎖定時**: 落距、當下落速、是否硬降; 當下寬度、當前任務序號與種類。
- **逐塊, 鎖定前(v18 新增)**: 盤面上的懸空連通塊數(不含本塊, 與懸空結構標示同一條定義)。
- **逐塊, 鎖定後消除前(v17 新增)**: Σ欄高、已填格數。
- **逐塊, 放下後第一段消除與追加消除分開**: 成立團清單(每團顆數、色、含重力球數、**含清色球數(v19)**、是否目標色、**是否含本塊格位**)。
- **逐塊, 定版後**: Σ欄高、已填格數、封閉洞數、最高欄高; **本塊的封閉洞增量 = 鎖定後消除前的封閉洞數 − 鎖定前的封閉洞數(v17 修正)**(校正各外型的造洞率, 不被本塊引發的消除與削頂抵掉)。
- **逐重力事件**: 位移格數、塊數、各塊落格數、動畫秒數、是否被上限壓縮; **零位移事件的動畫秒數記 0.2(v17 修正)**; **距上次全盤清除的塊數(v20 新增)**。
- **逐削頂事件(v16)**: **記錄時機 = 第 6 步削頂實際執行完畢時**(第 4 次延展事件內、第偶數個多樣化任務獎勵事件內)。欄位: **來源**(滿寬 / 第 d 個多樣化任務, d 為偶數)、當時塊數與秒數、實際削除格數(其中重力球數、**清色球數(v19)**)、**削前 Σ欄高(v18 明定: 在第 4e 步定版之後、削頂執行之前取)**與削後 Σ欄高、削開的封閉洞數; **削後 10 塊內, 被削開的洞有幾格被填回(v17 新增)**(局終不足 10 塊者記實際塊數)。盤面全空而未執行者照記一筆, 削除格數 0。
- **逐延展**: 延展序號、延展側、塊數與秒數。
- **逐新外型**: 解鎖塊數、第一次被發到的塊數、**是否因首發保證邊界而順延(v17 新增)**。
- **逐任務**: 進入到達成(或局終未達成時已耗)的塊數。
- **逐局**:
  - **呈現時長分類合計(v17 修正: 第 6 輪紀錄全為 0, 必須是實際累計值)**: 消除結算、重力事件、追加消除、**全盤清除(v19)**、延展事件、多樣化任務獎勵事件, 各自合計秒數。
  - 死亡當下 Σ欄高 ÷ 寬度; **死亡當下生成欄高度(v17 修正: 原為空)** — 取造成結束的那一塊(block out 為試算生成的那一塊, lock out 為本塊)生成外框涵蓋各欄的最高欄高; 放棄局記放棄當下下一塊的值。
  - 局終寬度、死亡塊數、死亡階段。
  - **連續 20 塊 Σ欄高淨變化(含削頂與全盤清除)的最小值(v17 改, 取代「連續 20 塊回收量最大值」; v20 修正: 第 7 輪紀錄仍輸出舊定義, 本版必須改為這個定義)**: 對每一段連續 20 塊, 取「第 20 塊定版後(含該次削頂) Σ欄高 − 該段第 1 塊鎖定前 Σ欄高」, 記全局最小值; 監看無限延續 — 最小值長期 ≤ 0 表示盤面在後段不再變高。
- **推算模型(v17, v20 補清色球)**: 回收比例 r 拆成兩個量 — **消除回收**(每塊消除帶走的堆高)與**削頂每格回收**(削頂每削 1 格帶走的堆高; v17 實測前期約 1.7 倍、滿寬後約 2.5 倍以上; **v20 第 7 輪實測 1.87, 比假設低 25%**); 每塊秒數改用堆高式(實測約 4.4 − 0.19 × 平均堆高, 崩盤段約 1 秒; **v20 第 7 輪確認成立**)。上列 v17 新增的「鎖定後消除前」與「削後 10 塊填回」即為拆開這兩個量所需。**清色球(v20)**: 一次全盤清除的格數約 1~3 次削頂, 對堆高只有約 0.5~0.65 次削頂; 由「4d 前後的盤面」與「清除後 10 塊內」欄位校正。
- **敏感度排序(v17 依實測改)**: 後期任務發生率 → 削頂每格回收 → 消除回收 → 開地塊數 → 死亡時平均堆高。被實測推翻就要重算。清色球在排序中的位置: 見「待定 / 局長」。

### 試玩要看的三件事(標優先序; 同一場試玩時間不夠時由上往下砍)
1. **【P1】整局有沒有一直在追下一個任務(第 7 輪答案為是; v19 起改看清色球)**: 問「**清色球觸發時的感覺, 跟削頂比起來怎麼樣?**」, **v20 加問「清掉那一片之後, 你覺得有得到什麼嗎?」**(一次全盤清除格數大、對堆高只有約 0.5~0.65 次削頂, 看玩家感受到的是畫面份量還是空間); 對照清色球各欄位(何時拿到、何時出現、隔幾塊才用、清哪一色與是否為最多色、清掉多少、清除前後盤面、清除後 10 塊內的重力事件與填回)與「連續 20 塊 Σ欄高淨變化最小值」, 看它是不是最大的正向回饋、有沒有讓一局失控。多樣化階段仍對照每個任務的達成時間(與被動基準比較)。
2. **【P1】新外型有沒有帶來新鮮感, 還是只是變難**: 問「解鎖新形狀方塊的時候有沒有期待」「新形狀方塊好不好放」, v17 加問「做後面那些任務時, 你想拿的是新形狀方塊還是削頂?」(第 3 個多樣化任務只給新形狀方塊, 兩者在紀錄上可部分分開, 但玩家的意願仍只能靠問), **v18 加問「你覺得『放下新形狀的方塊, 當場消掉一團』要怎樣才算?」**(對照「以為做到卻沒算」次數; v19 起面板有小圖、預覽有記號, 看是否仍被誤會); 對照每種新外型放下後的消除表現, 以及「用新形狀方塊」任務第 2 輪起的命中率對被動基準。
3. **【P2】重力下落的動畫有沒有讓你看懂塊是怎麼掉、掉完發生什麼。**

## 數值參數表

標「結構參數」者為規則骨架, 不因調校變動。標「**v20 改**」者為本版改動。標「v19」「v18」「v17」「v16」「v15 定案」者為前幾版定案的值。標「**企劃提案**」「**企劃初始值**」者待數值確認。標「**待重推**」者待數值以實測式重推。其餘為 v14 以前既有定案值, 本版不動。

| 參數 | 值 | 說明 |
| --- | --- | --- |
| 畫面寬 | 960 | 邏輯像素, 需同時容納滿寬 10 欄盤面與左右兩側的資訊欄 |
| 畫面高 | 640 | 邏輯像素, 需容納 19 列盤面與上方分數/預覽區 |
| 格子邊長 | 26 | 邏輯像素。滿寬盤面 260 寬 × 494 高 |
| 初始欄數 | 6 | 絕對欄 0~5 |
| 左右各可延展欄數 | 2 | 結構參數, 依交替順序各開 2 欄 |
| 滿寬欄數 | 10 | 絕對欄 −2~7 |
| **延展側順序** | **左 → 右 → 左 → 右** | 結構參數。第 1 次開左; 下一次延展側全程常駐可見(任務面板「得到什麼」), 第 4 次後不顯示 |
| 盤面高度 | 19 | 列。**局長的首要旋鈕, 只往上調**。維持; +1 列的影響**待重推** |
| 緩衝列數 | 2 | 列 20、21; 列 22 以上不設硬界。列 20 起不可見, 但全程參與成團判定、削頂、重力事件與全盤清除 |
| 消除門檻 | 4 顆 | 結構參數, 四方向相連, 不含對角; 團中須至少含 1 個真顏色格 |
| **系統性重力沉降** | **不做** | 結構參數。唯一的位移來源是玩家消掉重力球觸發的重力事件, 且為剛體整體下落 |
| **連鎖倍率 / 連鎖段數** | **不存在** | 結構參數。追加消除深度硬性上限 1, 不給任何倍率或依深度遞增的計分 |
| **萬用格** | **重力球、清色球** | v19; **v20 改名**(原連鎖球)。成團判定中兩者完全相同、仲裁不分彼此; 一塊最多 1 顆萬用格 |
| **重力球出現率** | **12.5%(每 8 塊 1 顆)** | 以 bag 控制, 不逐塊擲骰, 不論形狀。**局長旋鈕第 4 順位(v20)**; 清色球不佔用 |
| **一塊的重力球數上限** | **1 顆** | 結構參數 |
| **重力球的不連續保證** | **兩顆球之間至少隔 1 塊** | 結構參數。跨 bag 相鄰時該組重洗 |
| **重力球在塊內的位置** | **該塊所有格等機率抽 1** | 該格不從顏色 bag 取色、不消耗 bag |
| **重力球的顏色歸屬** | **萬用, 但不是任何色** | 結構參數。**永遠不推進開地任務**; 不計目標色分數 |
| **一顆萬用格參與的團數** | **最多 1 個** | 結構參數。仲裁順位: 真顏色格數 → 團總格數 → 錨點 y 大 → 錨點 x 小 |
| **團內萬用格數上限** | **不設上限** | 在 12.5% 與不連續規則下同團 2 顆以上機率極低 |
| **重力事件的種子** | **被消團原佔格座標的四方向鄰格; 該座標本身若已被較早事件填入亦納入** | 以原團座標為基準, 後發事件的作用範圍不因執行順序而縮小 |
| **重力事件的受影響範圍** | **自種子四方向連通的已填格位塊** | 結構參數。不相連的完全不動; 集合在事件開始時一次決定, 不因下落而擴張 |
| **懸空判定** | **整塊下移 1 格不觸底且不與塊外已填格位重疊** | 結構參數。**與懸空結構標示共用同一條定義, 不得分成兩套** |
| **懸空結構標示** | **全程常駐, 無開關** | 須讀得出哪些格位懸空與塊的歸屬; **不標示下落格數**; 輸入只有已鎖定格位 |
| **下落方式與格數** | **剛體整體下落, 落到不能再落** | 結構參數。多塊時依最低格位列號小者先、相同則最小 x 小者先, 逐塊落到底 |
| **單次事件下落格數上限** | **不啟用(備用值 5 格)** | 備用旋鈕, 退路順位第 ③ 級, 預設關閉 |
| **重力事件的計分與計數** | **不計分; 只推進多樣化任務「重力下落」** | 結構參數。它移動格位, 不移除格位; 實際發生位移的事件才推進 |
| **追加消除** | **深度上限 1, 不給倍率** | 結構參數。**上限不得放寬到 2 以上**; 不推進「大團」「用新形狀方塊」; 其中的清色球照常觸發全盤清除(v20 定案) |
| **清色球的玩家名稱** | **「清色球」** | **v20 改**(原「連鎖球」, 會被讀成連消)。規格內部同名 |
| **清色球獎勵事件的效果說明** | **「能當任何顏色, 消掉它全盤同色一起消失。」** | **v20 改**, 定案文字 |
| **清色球的來源** | **第 1 個多樣化任務(與角形同給) + 第 5 個多樣化任務** | **v20 改**(v19 企劃提案為第 1 個 + 第 4 個起每一個)。第 4、6 個只削頂; 第 7 個起奇數無獎勵、偶數削頂 |
| **每次給的清色球數** | **1 顆** | v19 |
| **清色球每局上限** | **2 顆(以獲得數計)** | **v20 改**。保護值; 現行放置下不會觸及, 被擋下的記入埋點 |
| **第 1 顆清色球出現時點** | **獲得後第 15 塊; 遇重力球排定的塊順延 1 塊** | **v20 改**(v19 為獲得後第 2 塊)。延後後約清 12~14 格(立即發出只清 6~10 格); 局長約 0~+3 秒 |
| **第 5 個的清色球出現時點** | **獲得後第 2 塊(獎勵後第一個新抽出的預覽); 遇重力球塊順延** | v19 原規則。預覽中已確定的一塊不改寫 |
| **清色球間隔** | **兩顆之間至少隔 1 塊** | v19 |
| **清色球在塊內的位置** | **該塊所有格等機率抽 1** | v19。不從顏色 bag 取色、不消耗 bag |
| **清色球在盤面上的保留** | **沒成團就一直留著, 不過期; 可被削頂削掉(不觸發、不補發)** | **v20 改**(寫明) |
| **全盤清除的顏色** | **該成立團的顏色(掃描時的 c)** | v19 結構參數。一團只有一種真顏色; 同次多團取聯集, 同色清一次 |
| **全盤清除範圍** | **全盤該色所有真顏色格, 含不可見區; 萬用格不清** | v19 結構參數 |
| **全盤清除的重力 / 重掃** | **不觸發重力、不重掃** | v19 結構參數(製作人定)。只留空洞 |
| **全盤清除得分** | **每次固定 500 分, 不乘分數倍率; 一種清除色算一次** | **v20 改**(v19 為不計分)。不按格數計, 避免囤著等最大一次; 計次單位見「待定」 |
| **全盤清除的計數** | **被清格不計基礎分、不推任何任務、不計入消除總顆數; 只有清色球參與的那一團照常計** | v19 結構參數(製作人定); v20 起另加固定 500 分 |
| **全盤清除的時序** | **4d: 追加消除之後、定版之前** | v19 結構參數 |
| **任務軌** | **單一: 開地任務 ×4 → 多樣化任務不設上限** | 結構參數。任何時刻只有一個當前任務 |
| **計數 A / E** | **已達成任務數 / 已執行獎勵次數** | 結構參數。恆有 E ≤ A; **獎勵觸發條件 = E<A**; 每次結算 A 至多 +1、E 至多 +1 |
| **G** | **max(0, E − 4)** | 已完成的多樣化任務數, 供落速公式使用 |
| 未進入任務的剩餘值與目標色 | 不設值 | 結構參數 |
| **溢出處理** | **只在開地任務之間: 扣抵下一個, 新任務剩餘最少 1 顆** | v13 定案, v14 限定範圍。不同種類之間不溢出; 多樣化任務相鄰必不同種, 永不溢出 |
| **目標色** | **只在開地任務期間存在** | v14 結構參數 |
| **目標色抽法** | **第 1 個任務自 3 色抽 1; 第 2~4 個排除上一個的色, 自 2 色抽 1** | 結構參數。等機率; 任務內不變; 於獎勵事件尾端公告; 玩家不可改選 |
| **開地任務需求序列** | **3 / 5 / 6 / 8 顆(合計 22)** | v17(原 3/5/7/9)。回放推算 v16 被動慢型約一半死在第 4 個開地任務; 降需求讓被動型少約 3 塊開地 |
| **開地任務獎勵** | **第 1~3 個: +1 欄; 第 4 個: +1 欄 + 削頂 + 1,500 分** | v16 |
| **多樣化任務種類與輪替** | **大團 → 用新形狀方塊 → 重力下落, 固定循環** | v17(雙消刪除), v18 改名。種類 = (k − 5) mod 3; 不另外向玩家預告下一個 |
| **大團需求** | **第 1、2 輪 2c − 1; 第 3 輪起 3c − 2(1 / 3 / 7 / 10…, 不封頂)** | **v20 改**(v17 為 2c − 1 全輪)。c = ⌊(k − 5) / 3⌋ + 1; v20 實測第 1 輪 7/7 達成 |
| **用新形狀方塊需求** | **第 1、2 輪 2c − 1; 第 3 輪起 3c − 2(1 / 3 / 7 / 10…, 不封頂)** | **v20 改**(v18 為 2c − 1 全輪)。第 1 輪幾乎必在首發那張完成(實測 4/4), 定位上手任務; 讀數從第 2 輪起判讀 |
| **用新形狀方塊的任務文字** | **「放下新形狀的方塊, 當場消掉一團」** | v18 |
| **用新形狀方塊的計入** | **本塊為已解鎖新外型時, 4a 中每個含本塊格位的成立團 −1** | v17。含本塊的萬用格與塊內自消; 原 7 種放下的消除(含先用新外型鋪、再用原 7 種收尾, v18 明定)、追加消除、重力事件、全盤清除、削頂皆不算 |
| **用新形狀方塊的讀懂輔助** | **任務期間: 面板畫已解鎖新外型小圖; 預覽若為新外型帶小記號** | v19。只在本任務為當前任務期間出現, 不常駐 |
| **重力下落需求** | **固定 1** | v15 定案 |
| **多樣化任務需求量(旋鈕)** | **見上** | **局長旋鈕第 3 順位(v20)**; 它同時決定削頂與第 5 個清色球來得多早, 淨方向待數值判斷 |
| **多樣化任務的計數步驟** | **只看放下後的第一段消除: 大團、用新形狀方塊只計 4a; 重力下落計 4b 有位移的事件** | v15 定案。追加消除不推進大團與用新形狀方塊 |
| **多樣化任務本身的分數** | **不另給分** | v15 定案, 大團已有 n≥5 固定額, 另給分會出現刷分路線 |
| **多樣化任務獎勵** | **d=1: 角形 + 清色球; d=2: 杯形 + 削頂; d=3: 十字形; d=4: 削頂; d=5: 清色球; d=6: 削頂; d≥7: 奇數無獎勵、偶數削頂** | **v20 改**(v19 企劃提案為 d≥4 每個清色球)。每個任務最多一種減壓; 同一事件內依序 解鎖 → 清色球 → 削頂 |
| **解鎖獎勵的玩家用詞** | **「新形狀方塊」** | v18(原「新方塊」)。面板、事件文字、說明一律使用 |
| **新外型種數** | **3 種** | v15 定案。v17 實測: 被動 1~2 種、刻意 3 種 |
| **新外型解鎖順序** | **角形 V(3 格)→ 杯形 U(5 格)→ 十字形 X(5 格)** | v15 定案。對應 大團 → 用新形狀方塊 → 重力下落; spawn 與旋轉見「新外型方塊」 |
| **新外型塊內自消** | **合法, 不另外處理** | v15 定案。杯形約 3%、十字形約 5%; 「用新形狀方塊」照算 |
| 顏色數 | 3 | 結構參數, **禁改** |
| 方塊形狀 | 標準 7 種四格形狀 + 已解鎖的新外型 | 每格顏色獨立; O 形與十字形旋轉須真實旋轉顏色(含萬用格的位置) |
| 方塊生成位置 | 外框底緣固定列 20 | 左緣相對欄 = ⌊(w − 形狀寬)/2⌋ |
| 形狀寬(spawn 態) | I=4, O=2, 角形=2, 其餘=3 | I 形略不容易觸發 block out, **不做補償** |
| 顏色 bag 粒度 | 18 格 | 每 bag 含各色各 6 格; **萬用格不消耗 bag** |
| 同塊各格顏色填入順序 | y 小者在前, y 同則 x 小者在前 | 結構參數 |
| **形狀 bag 組成** | **原 7 種各 1 張 + 每種已解鎖新外型各 2 張(7 / 9 / 11 / 13 張)** | v15 定案。解鎖從下一個 bag 起生效 |
| **新外型首發保證** | **解鎖後第一個 bag, 新外型的其中 1 張排第 1 張** | v15 定案。只影響該 bag 的第 1 張, 其餘照常洗牌 |
| **首發保證邊界** | **解鎖當下預覽已從新 bag 抽出 → 該 bag 視為進行中, 順延到再下一個 bag** | v17 |
| **重力球 bag 粒度** | **8 塊** | 組內恰 1 塊含球, 洗牌決定位置 |
| **削頂** | **每一欄削掉最上面 1 個已填格位** | v14 定案。逐欄判定、各欄互不影響; 萬用格視為一般已填格位且被削時不觸發; 畫面上不講削幾格 |
| **削頂的出現時機** | **第 4 次延展 + 第偶數個多樣化任務的獎勵(d = 2、4、6…); 不設次數上限** | v18(v16 為每一個多樣化任務)。每個都削會讓開滿中位約 280 秒、強玩家 400~520 秒; 每兩個一次約 240 / 275 秒。不採用「每輪最後一個才削」(被動型後期碰不到削頂); v17 不採用「全解鎖後不附削頂」; v20: d = 4、6、8… 削頂是唯一獎勵 |
| 削頂的計分與計數 | 皆不計 | 結構參數。只計入埋點的削除格數 |
| 削頂後的重掃 | 不做 | 結構參數。但懸空標示要重算 |
| 滿寬完成獎勵 | 1,500 分 | 一次性, 於第 4 次延展事件內結算; **不乘分數倍率** |
| **分數倍率上限** | **2.5** | **保護值, 不是調校旋鈕**。v19 起沒有倍率獎勵, 倍率最高 1.6(滿寬), 上限實際不會觸及 |
| **落速階梯** | **每完成一個多樣化任務 +0.5 格/秒, 封頂 2 階** | 階數變數用 **G**; 基值與上限同步 +; **落速最高 7.0 格/秒**; 不論該次獎勵內容(含沒有獎勵) |
| 初始落速 | 4.0 格/秒 | 定案 |
| 速度倍增週期 | 290 秒 | 定案 |
| 落速上限(基準) | 6.0 格/秒 | 約 t≈170 秒觸頂; 另加階梯至多 +1.0 |
| **落速公式** | **min(4.0 × 2^(t/290) + 0.5×min(G,2), 6.0 + 0.5×min(G,2))** | G = max(0, E−4); G=0 時退化為基準公式 |
| 落速原則 | 開局 4~5 秒 / 局末 ≥1.5 秒 | **結構參數** |
| 加速免疫時間 | 0 秒 | 落速自 t=0 起即依曲線上升 |
| 更新步長 | 1/60 秒 | 單步可落多格, 逐格判定碰撞; 單步上限 21 格為保護值 |
| 鎖定延遲 | 0.5 秒 | 軟降同樣適用; 硬降不適用 |
| 鎖定延遲最大重置次數 | 1 | 若回報「後段手感斷崖」, 可退的中間檔是 2 次 |
| 旋轉踢牆試位 | 左 1 格 → 右 1 格 | 皆失敗則旋轉不成立; 向上溢出不算失敗; 新外型同樣適用 |
| 軟降速度 | 固定 12 格/秒 | 落速上限 7.0 < 12, 仍成立 |
| 長按起始延遲(DAS) | 100 毫秒 | 鍵盤橫移 |
| 長按重複間隔(ARR) | 33 毫秒 | 鍵盤橫移 |
| 放棄本局長按時間 | 1 秒 | 真實時間計; 說明畫面中不受理 |
| 下一塊預覽數 | 1 塊 | 結構參數。須能讀出形狀、每格顏色**與萬用格(重力球 / 清色球)在哪一格** |
| 消除結算呈現時長 | 0.4 秒 | 有成立團才計。可調 0.2~0.6 秒 |
| **重力下落動畫: 每格時長** | **0.06 秒** | v15 定案 |
| **重力下落動畫: 每塊最短時長** | **0.15 秒** | v15 定案。一塊的下落時長 = max(0.15, 落格數 × 0.06) |
| **重力下落動畫: 每塊落地停頓** | **0.12 秒** | v15 定案 |
| **重力下落動畫: 單次事件上限** | **1.5 秒** | v15 定案。超過時等比壓縮, 不跳塊; 對局長影響不到 1% |
| **重力事件零位移呈現** | **0.2 秒** | **必須產生可單獨辨識的回饋, 不得無聲跳過** |
| **追加消除呈現時長** | **0.4 秒** | 有追加消除才計。可調 0.3~0.6 秒; **必須與重力下落動畫在時間上明顯分離** |
| **全盤清除呈現時長** | **1.0 秒** | v19, **v20 定案**。有清除色才計, 一次結算一份; 可調 0.8~1.5 秒; 獨立一段, 不得與其他段疊合 |
| 延展事件呈現時長 | 1.2 秒 | 含第 4 次的削頂與滿寬獎勵、尾端的下一個任務公告, 不另計。可調 0.8~1.5 秒 |
| **多樣化任務獎勵事件呈現時長** | **1.5 秒** | v17(原 1.2)。內含 0~2 段演出與尾端公告; 已在可調範圍 0.8~1.5 秒的上限; 單段同樣 1.5 秒; **沒有獎勵(d ≥ 7 奇數)時同樣 1.5 秒(v20, 企劃初始值)** |
| **局長微調的優先順位** | **盤面高度(只往上)→ 清色球次數與位置 → 多樣化任務需求量 → 重力球出現率 → 呈現時長** | **v20 改**。落速不在清單內; 削頂不再是旋鈕 |
| 單團基礎分 | 25 × n | n 為該團顆數, **含團內的萬用格**; 線性項 |
| n ≥ 5 的固定額 | +25 分 | 不隨 n 遞增。含萬用格的團照常適用(n 以含萬用格計) |
| **重力球的額外分數** | **不給** | 結構參數。另給固定額會把空間工具變成刷分手段; 清色球本身也不另給分(全盤清除的 500 分屬清除事件, 見上) |
| 同次結算多團處理 | 各團分開計分後相加 | 無任何倍率 |
| **目標色分數** | **12 分/顆, 只在開地任務期間** | 不分位置; 重力球不計 |
| 分數倍率 | **1.0 + 0.15 × 已解鎖欄數**, 上限 2.5 | v19: 無倍率獎勵項。已解鎖欄數 0~4; 滿寬時 1.6 |
| 本次結算得分公式 | { Σ_團 [25 × n + (n≥5 ? 25 : 0)] + 目標色顆數 × 12 } × 分數倍率 + 500 × 本次清除色種數 | n 含萬用格; 倍率與目標色取本次結算開始時的值(多樣化任務期間目標色顆數恆為 0); **追加消除的團與顆數照同一式計入本次結算**; 滿寬完成獎勵不在此式內; **削頂削除與全盤清除的格位完全不進括號內的式子; 全盤清除的 500 分不乘倍率(v20)** |
| 軟降/硬降分數 | 0 | 不給分, 避免鼓勵無意義硬降刷分 |
| **目標單局時長** | **開滿並進入多樣化任務的局次 230~270 秒; 全檔中位 200~250 秒; 硬上限 320 秒** | **v20 改(製作人裁決)**, 原 190~230 / 165~200 / 300。理由: v18 實測開滿中位已 248.7 秒、最長 302 秒, 且老闆越玩越久; 要守舊目標只能砍老闆最喜歡的減壓, 老闆從未反映局太長 |
| **滿寬後窗口** | **第 7 輪實測 51~78 塊** | **v20 改**(v17 實測 27~64 塊) |
| **目標色佔比參照(開地階段)** | **被動期望 0.35; 3 局合併訊號線 0.45** | 照 v13; 單局不判讀。v17 實測被動 0.32、刻意 0.61~0.67, 分得開 |
| **多樣化任務被動基準參照(大團、重力下落)** | **實際 ÷ 被動期望等待 ≤ 0.6 有在做; ≥ 0.85 等同被動(3 局合併)** | v17: p 取滿寬後該任務非當前時的每塊發生率(原為開地期間 9 欄段, 偏低 32%); 不構成判定門檻 |
| **用新形狀方塊被動基準** | **新外型當場命中率, 依外型 × 首發 / 非首發 × 是否當前任務分開記; 第 1 輪不判讀** | v18。實測參照: 首發 4/4、非首發約 0.31、角形約 0.28(一人 4 局, 只當參考); 判讀參照線待定。v19 加了面板小圖與預覽記號, 當前任務時的命中率可能因此上升 |
| **E_無球** | **(消除總顆數 − 含球團真顏色顆數 − 追加消除顆數) ÷ 總塊數** | 分開「技術好」與「球用得好」 |
| **空轉率紅線** | **預期 0.25~0.35; 綠 ≤0.25 / 黃 0.25~0.45 / 紅 ≥0.50 且 3 局皆然; 反向警戒 ≤0.10** | 帶懸空標示後的值。**讀數以佔比為主、次數為輔** |
| **大型重力事件紅線** | **佔比 15% / 30%; 次數綠 ≤1.2 / 黃 1.2~3 / 紅 ≥4(次/局)** | 佔比是盤面幾何的性質。v20: 全盤清除後的重力事件可能變大, 以逐重力事件「距上次全盤清除的塊數」分「清除後 10 塊內」與其餘兩段讀 |
| **追加消除紅線** | **預期 1.5 次/局、約佔 4%; 紅線 ≥5 次/局 或 佔比 ≥12%** | 觸發時走退路第 ② 級, 不動深度上限 |
| **消除回收** | **待重推** | v17: 取代單一回收比例 r(原假設 0.34); 每塊消除帶走的堆高, 由「鎖定後消除前」與「定版後」的 Σ欄高差校正 |
| **削頂每格回收** | **第 7 輪實測 1.87** | **v20 改**(v17 實測前期約 1.7 倍、滿寬後約 2.5 倍以上; 1.87 比假設低 25%)。由逐削頂事件與「削後 10 塊填回」校正 |
| **清色球回收** | **一次全盤清除: 格數約 1~3 次削頂, 對堆高約 0.5~0.65 次削頂** | **v20 改**(v19 待數值補)。第 1 顆延後後約清 12~14 格; 由「4d 前後的盤面」與「清除後 10 塊內」校正 |
| **每塊秒數模型** | **約 4.4 − 0.19 × 平均堆高(崩盤段約 1 秒)** | v17: 由堆高決定而不是塊序; v20 第 7 輪確認成立 |
| **試玩紀錄檔名** | **`gamelog-WebGame_SlimeTetris-<YYYYMMDD-HHMMSS>.json`** | 守則 R3; 每局結束(含放棄)自動下載 |

## 物件清單

| 物件 | 狀態 | 說明 |
| --- | --- | --- |
| 盤面格位 | 空 / 已填(色 A/B/C) / 已填(重力球) / 已填(清色球) / 消除標記中 / 削除標記中 / 全盤清除中 / 下落中 | 玩家需辨識顏色, 並能一眼分辨哪一格是重力球、哪一格是清色球(兩者可區分)。各種「標記中/清除中/下落中」須彼此可區分; **畫面上鎖定後不區分來自哪一種方塊**(v17: 「用新形狀方塊」只看當塊, 不需要); v18: 紀錄內部另記「是否來自新外型」, 只供埋點「以為做到卻沒算」, 不影響任何判定 |
| 重力球格位 | 隨方塊下落中 / 已鎖定在盤面 / 成團消除中(觸發) / 隨重力事件下落中 / 被削頂削除中 | 萬用格; 每塊最多 1 顆萬用格; 未被消除時照常佔格位、參與 lock out 與 block out; 被削頂削除時不觸發下落; 不被全盤清除移除 |
| **清色球格位(v19; v20 改名)** | 待發(已獲得、尚未進入任何方塊; 第 1 顆等到獲得後第 15 塊) / 在預覽中 / 隨方塊下落中 / 已鎖定在盤面(未成團, 一直留著) / 成團消除中(觸發全盤清除) / 隨重力事件下落中 / 被削頂削除中 / 作廢(局終未發出或殘存) | 萬用格, 成團判定與重力球完全相同; 不與重力球同塊; 一局最多 2 顆; 留在盤面上時照常佔格位、參與 lock out 與 block out, 之後補成團才生效; 被削頂削除時不觸發、不補發; 不被其他清色球的全盤清除移除; 在預覽中出現時要認得出就是獎勵事件展示過的那一個 |
| 同色團 | 未達門檻(1~3 顆) / 成立消除(≥4 顆) / 成立消除且含清色球(v19) | 四方向相連; 局面上不存在「已達 4 顆但未消」的持續狀態; 團可含萬用格, 但一顆萬用格只屬於一個團; 一團只有一種真顏色; **不顯示顆數** |
| 懸空結構標示(常駐) | 標示中(依所屬連通塊可彼此區分) / 不標示(該塊不懸空) | 全程常駐、無開關; 須讀得出哪些格位懸空與哪些格位同屬一塊; **不表達下落格數**; 與重力事件共用同一條懸空定義; 輸入只有已鎖定格位; 全盤清除後重算 |
| 懸空結構(事件中) | 不受影響 / 受影響且懸空(將下落) / 下落中 / 已落定 / 受影響但不懸空(不動) | 重力事件期間的暫態; 受影響的塊依規則順序逐塊演出下落, 一塊落定後才輪下一塊; 下落中保持形狀不變 |
| 空洞 | 開放(上方無格位) / 封閉(上方被格位蓋住) | 不會自動填回; 回收管道四條(落下的方塊落入 / 重力事件 / 削頂(第 4 次延展與第偶數個多樣化任務的獎勵) / 清色球的全盤清除)。**兩者在盤面上不做區分呈現**, 封閉洞數只在紀錄中 |
| 重力事件指示 | 出現(有位移) / 出現(零位移) / 消失 | 標出觸發點; 有位移時伴隨下落動畫, 時長依落距; **零位移必須是一個可單獨辨識的回饋, 停留 0.2 秒, 不得無聲跳過**; 多個事件依序出現, 不合併; 當前任務為重力下落且本事件算數時, 讀得出「這次算數了」 |
| 落地回饋 | 出現 / 消失 | 重力事件中每一塊落定時各出現一次, 讓玩家看出「這一塊到底了」 |
| 追加消除指示 | 出現 / 消失 | 標出「這批是下落之後才成立的消除」, 須與一般消除結算標記可區分, 且在所有下落動畫之後以獨立一段呈現; 停留 0.4 秒; 不出現大團與用新形狀方塊的算數回饋 |
| **全盤清除指示(v19; v20 加得分)** | 出現 / 消失 | 結算 4d, 在消除結算、重力下落、追加消除之後獨立一段呈現, 讓玩家看出「這個顏色在全盤一起消失」, 並呈現這次加的 500 分(每種清除色一份); 一次結算一段(多色時同一段內一起呈現); 停留 1.0 秒(企劃提案); 不出現任何任務的算數回饋; 不顯示格數 |
| 盤面邊界 | 寬度 6 / 7 / 8 / 9 / 10 | 當前可放置範圍; 絕對欄不重編號; 滿寬後固定 10 |
| 不可見區 | 不可見 | 列 20 以上; 參與成團判定、削頂、重力事件與全盤清除; 列 22 以上不設硬界 |
| 落下方塊 | 下落中 / 軟降中 / 鎖定延遲中 / 已鎖定 | 由 3~5 個格位組成(原 7 種為 4 格; 新外型 3 / 5 / 5 格), 每格為一種顏色或一顆萬用格(一塊至多 1 顆); 旋轉態 4 種(O 形與十字形亦有 4 種) |
| 新外型 | 未解鎖 / 已解鎖(下一個 bag 起加入; 首發保證可能順延一個 bag) / 已加入發牌 | 角形 V、杯形 U、十字形 X; 依序解鎖; 每種 2 張; 重開一局歸零; 玩家可見名稱為「新形狀方塊」(v18); 被首發保證排定的那一張在紀錄中標為首發 |
| 落點指示(ghost) | 顯示中 | 全程顯示; 呈現每一格的顏色與萬用格位置(重力球 / 清色球可區分); 不含任何預測; 不參與懸空判定; 無開關 |
| 下一塊預覽 | 顯示中 / 顯示中(帶新形狀記號) / 遮蔽中 | 固定 1 塊; 須能讀出形狀(含新外型)、每格顏色與是否含萬用格及在哪一格(清色球出現時要讀得出就是之前拿到的那一顆, v20); **v19: 「用新形狀方塊」為當前任務且該塊為新外型時帶新形狀記號, 其餘時間不帶**; 暫停時遮蔽 |
| **任務面板** | **開地任務進行中 / 多樣化任務進行中 / 多樣化任務進行中(帶新形狀小圖) / 已達成, 待結算 / 下一個任務公告中** | 常駐顯示「做什麼 / 得到什麼 / 還差多少」三件事, 用玩家語言, 不出現規格內部名稱, 不顯示任務序號; 剩餘 ≤ 0 時不顯示負數; **「做什麼」三種多樣化任務的文字照「多樣化任務」表**; **v19: 「用新形狀方塊」進行中時, 文字旁畫出已解鎖新外型的小圖(未解鎖的不畫), 任務結束即移除**; **「得到什麼」(v20): 第 1~3 個開地任務只寫長一欄的側, 第 4 個寫長一欄 + 削頂 + 1,500 分; 多樣化任務 d=1「新形狀方塊 + 清色球」、d=2「新形狀方塊 + 削頂」、d=3「新形狀方塊」、d=4「削頂」、d=5「清色球」、d=6「削頂」、d≥7 奇數「沒有獎勵」、偶數「削頂」**; 不寫「新方塊」; 削頂不寫格數; 多樣化任務不預先展示外型; 不預告下一個任務; 暫停時遮蔽 |
| 目標色指示 | 色 A / B / C / 不顯示(多樣化任務期間) | 屬任務面板「做什麼」的內容; 只在開地任務期間存在 |
| 下一次延展側指示 | 左 / 右 / 不顯示(已滿寬) | 屬任務面板「得到什麼」的內容; 只回答「下一次開在哪一側」, 不回答何時 |
| 消除結算標記 | 出現 / 消失 | 標出本次消除中哪些推進了當前任務(開地: 目標色格; 大團: 算數的那一團; **用新形狀方塊: 算數的各團**), 讓玩家看得出「這次算數了」(事後回饋, 非事前提示); **不標示團內含幾顆萬用格**; 停留 0.4 秒 |
| 延展事件指示 | 出現 / 消失 | 延展實際執行時獨立呈現, 指出新欄開在哪一側; 第 1~3 次只有 +1 欄, 第 4 次另含削頂與滿寬完成獎勵; 停留 1.2 秒(含尾端的下一個任務公告); 不與消除結算標記、重力事件指示同時出現 |
| 削頂指示 | 出現 / 消失 | 屬第 4 次延展事件與**第偶數個**多樣化任務獎勵事件的一部分(v18); d = 2 在解鎖之後演出, d = 4、6、8… 是事件內唯一一段(v20); 指出哪些格位被削掉; **不顯示格數** |
| 滿寬完成獎勵指示 | 出現 / 消失 | 第 4 次延展事件內呈現一次性 1,500 分 |
| **新外型解鎖事件指示** | **出現(解鎖 + 清色球) / 出現(解鎖 + 削頂) / 出現(只有解鎖) / 消失** | 多樣化任務第 1~3 個的獎勵事件; 獨立事件, 先展示這種新外型的形狀, 事件文字稱「新形狀方塊」(v18); 第 1 個接著給清色球, 第 2 個接著削頂, 第 3 個只有解鎖; 停留 1.5 秒(含尾端的下一個任務公告); 不與消除結算、重力事件同時出現 |
| **清色球獎勵事件指示(v19; v20 改名與位置)** | **出現(第 1 個多樣化任務, 接在解鎖之後) / 出現(第 5 個多樣化任務, 只有清色球) / 消失** | 展示清色球本身並以一句話講效果:「能當任何顏色, 消掉它全盤同色一起消失。」(v20 定案); 第 1 顆要到獲得後第 15 塊才出現, 這是延後期間玩家唯一看到它的地方; 獨立事件, 停留 1.5 秒(與同事件其他段合計) |
| **只削頂 / 沒有獎勵的任務完成事件指示(v20)** | **出現(只削頂: d = 4、6、8…) / 出現(沒有獎勵: d ≥ 7 奇數) / 消失** | 多樣化任務獎勵事件的其餘兩種形態; 只削頂時演出削頂指示; 沒有獎勵時只呈現任務完成與尾端公告; 停留 1.5 秒(企劃初始值) |
| 暫停遮罩 | 出現 / 消失 | 覆蓋盤面與所有情報區(含懸空標示、任務面板) |
| 說明畫面 | 隱藏 / 開場顯示中 / 局中顯示中 / 結束畫面顯示中; 各自帶「第 p 頁」 | 守則 R2。內容照 `guide.md`; 可翻頁不循環; 任何一頁皆可關閉; 局中顯示時同時處於暫停(盤面遮蔽); 顯示中不受理放棄長按 |
| 放棄計時指示 | 出現 / 消失 | 長按 R 期間顯示剩餘時間 |
| 分數 | 數值 | 即時更新 |
| 分數倍率 | 數值 | 常駐可見; 只有延展會改變它(v19: 倍率獎勵已刪), 玩家要看得到自己拿到了 |
| 最佳紀錄 | 數值 / 尚無紀錄 | 單一筆, 放棄局不更新 |
| 經過時間 | 數值 | 暫停與說明畫面期間不推進, 其餘結算期間(含重力下落動畫、追加消除與全盤清除)照常推進 |
| 遊戲狀態 | 說明畫面中 / 進行中 / 結算中 / 重力事件中 / 暫停 / 結束 | 結算中與重力事件中皆鎖定方塊輸入; 說明畫面中只接受翻頁與關閉 |
| 結束資訊 | 隱藏 / 顯示 | 結束時呈現分數、存活秒數、已解鎖欄數、已解鎖新外型數、局終停在的任務與還差多少、結束原因、是否破紀錄 |
| **試玩紀錄** | **記錄中 / 已下載** | 守則 R3。逐筆與逐局累積, 局終自動下載 JSON; 重開一局重新開始記錄; 局序跨局累加(本次開啟遊戲後第幾局) |

## Demo 範圍邊界
- 不做: **「分數倍率提高」這個多樣化任務獎勵(v19 刪除)** — 使用者排序最低; 已解鎖欄數帶來的倍率照舊
- 不做: **全解鎖後每個多樣化任務都給清色球(v19 企劃提案, v20 不採用)** — 數值以第 7 輪實測算出強玩家會拉到 350~375 秒且可能自我維持; 一局只給 2 顆(第 1、5 個)
- 不做: **同一個多樣化任務同時給削頂與清色球(v20)** — 每個任務最多一種減壓
- 不做: **第 1 顆清色球獲得後立即發出(v20)** — 落在滿寬後最薄的盤面, 只清得掉 6~10 格, 比滿寬削頂還弱
- 不做: **「連鎖球」這個名稱(v20 改名清色球)** — 會被讀成連消, 而本作不存在連鎖
- 不做: **全盤清除按被清格數計分(v20)** — 固定 500 分; 按格數計會讓玩家囤著等最大一次
- 不做: **全盤清除觸發重力下落、重新掃描成團或推進任務(v19)** — 只留空洞; 一次清掉約三分之一盤面若一起掉, 就是第 2 輪被否決的「系統決定塌哪裡」; 推任務會讓它變成刷任務的工具
- 不做: **全盤清除移除萬用格** — 重力球與清色球沒有顏色, 原地保留
- 不做: **同一塊同時有重力球與清色球**; **清色球佔用或改寫重力球 bag**; **清色球在仲裁中優先**
- 不做: **清色球的引爆鍵、道具欄、玩家保留或延後發出**; **留在盤面上的清色球過期或自動觸發**
- 不做: **清色球出現在開地任務期間** — 最早是第 1 個多樣化任務的獎勵, 不早於滿寬
- 不做: **「用新形狀方塊」的小圖與記號常駐顯示(v19)** — 只在這個任務期間出現; 其餘時間照 v17 的「畫面別太花」
- 不做: **每一個多樣化任務都附削頂(v18 改為第偶數個)** — 每次削頂換回的存活塊數比完成任務花的塊數還多, 局長結構性超標
- 不做: **削頂只在每輪最後一個多樣化任務出現(v18 不採用)** — 第一次後期削頂落在第 3 個多樣化任務, 被動型多半走不到
- 不做: **獎勵名稱寫「新方塊」(v18)** — 一律「新形狀方塊」, 「新方塊」會被讀成下一塊
- 不做: **「雙消」任務(v17 刪除)** — 高盤面上幾乎不會發生, 成為牆; 由「用新形狀方塊」取代
- 不做: **盤面上標記哪些格位來自新外型、或把已鎖定的舊新外型格計入「用新形狀方塊」(v17)** — 只看當塊, 玩家在放置那一刻就能判斷。紀錄內部記格位來源(v18 埋點)不在此列, 它不上畫面、不影響判定; v19 的預覽記號只標尚未放下的下一塊, 也不在此列
- 不做: **新外型全解鎖後的獎勵不附削頂(v17 不採用)** — 違背老闆「後期也要有削頂」; 全解鎖後第偶數個照常附削頂
- 不做: **v13 第 5 階起的輪替獎勵關卡與其三種獎勵(倍率 / 重力球補給 / 削頂輪替)(v14 刪除)** — 滿寬之後由多樣化任務取代; 不恢復輪替與重力球補給(清色球的發出只借用其出入口寫法, 不是重力球補給)
- 不做: **第 1~3 次延展附削頂(v16 刪除)** — 前期盤面薄, 削頂效果小; 削頂只留在滿寬與第偶數個多樣化任務的獎勵
- 不做: **同色團的顆數數字(v14 刪除)** — 盤面上不標示任何團的顆數
- 不做: **消除結算標記上「本團含幾顆萬用格」的標示(v15 刪除)** — 與刪顆數數字同一方向
- 不做: **削頂強度隨次數遞增、畫面上標示削了幾格(v14 刪除)**
- 不做: **多樣化任務的位置條件** — 不指定欄、不分左右半
- 不做: **「接團」類任務(v15 製作人否決)** — 判定要回溯「原本是否分開的兩團」, 玩家讀不出也難驗證
- 不做: **多樣化任務與開地任務穿插(製作人取捨)** — 穿插會拉長窄盤面期, 新外型在窄盤面上容易放不下
- 不做: **多樣化任務隨機抽種類** — 固定順序輪替; 不另外向玩家預告下一個是什麼(v15), 畫面上只有一個當前任務
- 不做: **任務面板預先展示下一種新外型** — 外型在解鎖事件才揭曉(v19 的小圖只畫已解鎖的)
- 不做: **在 10 欄內無法擺放的外型**; **只是換個樣子、沒有不同放置性格的外型**; **一塊 2 顆以上萬用格(新外型同樣適用)**
- 不做: **左右分邊的任何形式** — 中線、空間歸屬、兩側各自的目標色與進度、便宜 / 昂貴側一律不存在; 任務計數與位置完全無關
- 不做: **任何由玩家挑選、改選、召喚或拒絕目標色、任務或獎勵的操作**
- 不做: **滑鼠操作(守則 R1)** — 只用鍵盤
- 不做: **玩家指定延展側** — 延展側由規則固定左右交替
- 不做: **固定一個目標色貫穿四個開地任務** — 會讓目標色在第二個任務以後變成背景
- 不做: **系統性重力沉降** — 不存在任何不依賴重力球的自動沉降、定時壓實、逐格下墜。**這是第 2 輪試玩指認的問題根源, 不得以「局太短」「洞太多」為由恢復。** 重力事件不是它的回歸: 觸發由玩家決定、範圍限於相連結構、下落是剛體整體位移而非逐格壓實
- 不做: **重力下落瞬移** — 必須逐塊演出; **重力下落動畫多塊同時落**; **動畫影響判定結果**
- 不做: **重力事件的擴散傳播** — 受影響集合在事件開始的那一刻一次決定
- 不做: **重力事件以全盤為判定範圍** — 不相連的結構一律不動, 即使它懸空
- 不做: **收窄種子到「僅重力球那一格的四鄰」** — 不但治不了空轉還會讓空轉更嚴重; 不得列為退路
- 不做: **追加消除的深度放寬到 2 以上**; 不做: **追加消除給倍率、段數或任何依深度遞增的計分**
- 不做: **重力球推進開地任務** — 它是空間工具不是任務捷徑, 兩條軸分開
- 不做: **重力球的額外分數、清色球本身的額外分數**(全盤清除的固定 500 分屬清除事件, 不在此列); 不做: **一顆萬用格同時讓兩個顏色的團成立**; 不做: **純萬用格構成的團**
- 不做: **重力球的引爆鍵、道具欄、保留功能**; 不做: **逐塊擲骰決定重力球**
- 不做: **削頂削掉重力球時觸發下落、削掉清色球時觸發全盤清除**
- 不做: **懸空標示與重力事件用兩套懸空定義**
- 不做: **只標「這一格懸空」而不表達連通塊歸屬** — 會把玩家的心智模型推回逐格壓實
- 不做: **懸空標示表達「會下落幾格」或「這次事件會不會牽動它」** — 那是落點試算
- 不做: **懸空標示的開關、切換或查詢介面**
- 不做: **零位移的重力事件無聲跳過**
- 不做: **多樣化任務設上限**; **大團與用新形狀方塊的需求封頂**
- 不做: **滿寬後引入新外型以外的壓力來源** — 不加道具、障礙塊、特殊史萊姆、倒數計時; 後段難度只來自新外型、落速階梯與盤面蜂窩化(清色球是任務獎勵、只減壓不加壓, 不在此列)
- 不做: **落速階梯無上限上抬** — 封頂 2 階(上限 7.0)
- 不做: **用已達成任務數 A 當落速階梯的變數** — 一律用 G
- 不做: **把分數倍率上限當調校旋鈕** — 2.5 是保護值; v19 起實際不會觸及
- 不做: **bot、統計判定門檻、分層檢定、多人樣本計畫**
- 不做: **以盤面列號判定削頂範圍**; 不做: **削頂改成清底部一列**; 不做: **削頂給分**; 不做: **削頂後重新掃描成團**
- 不做: **以「剩餘 ≤ 0」當獎勵事件的觸發條件** — 一律用 E<A
- 不做: **為未進入的任務預先設定剩餘值或目標色**
- 不做: **換色或下一個任務公告另立事件、另計呈現時長** — 那是獎勵事件的尾端公告
- 不做: **I 形 block out 機率偏低的補償**
- 不做: **整列填滿消除**; **任何保底清盤機制** — 盤面的回收管道只有四條: 落下的方塊落入、玩家觸發的重力事件、任務獎勵的削頂(第 4 次延展與第偶數個多樣化任務)、任務獎勵換來且由玩家決定何時用的清色球(一局最多 2 顆)
- 不做: **消除門檻降到 3 顆**; **隨團顆數遞增的加成**; **對角相鄰成團**
- 不做: 延展加成任務進度; 惰性格與任何新欄補償; 局間成長、收藏、關卡地圖等元 progression(新外型與清色球每局歸零)
- 不做: **超過 4 次的延展** — 滿寬 10 欄固定
- 不做: **暫存(hold)機制**; **2 塊以上的下一塊前瞻**; 連消(combo)、Back-to-Back 等額外計分規則
- 不做: 難度選擇、起始等級選單; 排行榜、雲端紀錄、帳號; 教學關卡與引導流程(遊戲內說明是可翻頁的靜態說明, 不是教學關卡)
- 不做: **預測性的即時提示** — 事前的「會不會成團 / 會不會推進任務 / 會消幾顆 / 會觸發多大的下落 / 下落後會不會再消一批 / 清色球會清掉哪些格 / 距離下一次獎勵還差幾塊 / 清色球還要幾塊才出現」一律不做。懸空標示、下一次延展側指示與「用新形狀方塊」的預覽記號不屬此列(都是現況讀數)
- 不做: **ghost 開關**; **ghost 參與懸空判定**
- 不做: **完整 SRS 踢牆表**; **O 形與十字形旋轉做成 no-op**; **暫停時看盤**(含說明畫面期間)
- 不做: **單次結算執行 2 次以上的獎勵事件**
- 不做: **延展、解鎖、清色球獎勵、全盤清除、重力事件與消除結算合併呈現** — 必須各自是可單獨辨識的事件(同一個獎勵事件內的解鎖、清色球與削頂依序演出, 屬同一事件, 不在此列)
- 不做: **以「只是表演」為由縮短結算呈現時長**
- 不做: **把落速當局長旋鈕**; **把盤面高度往下調**; **調高開地需求門檻去堵被動高手**
- 不做: 遊戲內時間上限 / 強制收局計時器; block out 的預判

(v20 製作人定案, 自待定移出: 全盤清除呈現 1.0 秒; 500 分以「一種清除色算一次」計; 沒有獎勵與只削頂的任務完成事件同樣 1.5 秒, 與其他多樣化獎勵事件一致, 不另立時長)

## 待定
- **局長(待下一輪實測)**: 目標已由製作人改寫為開滿 230~270 秒、全檔中位 200~250 秒、硬上限 320 秒; v20 的清色球放置(只 2 顆、第 1 顆延後約 0~+3 秒)與第 3 輪起 3c − 2 是否讓強玩家落在 320 秒內, 待下一輪紀錄對照「連續 20 塊 Σ欄高淨變化最小值」(本版修正後的定義)確認有無無限延續。清色球在推算模型敏感度排序中的位置, decisions 未給, 待數值依本版新增欄位排入。
- **「用新形狀方塊」被動基準的判讀參照線**: decisions v18 定了記法(命中率依外型、首發 / 非首發、是否當前任務分開記)與實測值(首發 4/4、非首發約 0.31、角形約 0.28), 但沒給像大團 / 重力下落「實際 ÷ 期望 ≤ 0.6 / ≥ 0.85」那樣的參照線。本版判讀方式是比較當前任務時與非當前任務時的同外型非首發命中率, 參照線交數值定。v19 加了面板小圖與預覽記號(只在當前任務時出現), 兩邊的差距會因此多一個來源。
- **開地階段死局率(待下一輪實測)**: 3/5/6/8 是依回放推算「被動慢型約一半死在第 4 個開地任務」而降的, 降完後是否仍大量死在開地待實測確認; 若仍超標, 可動的是開地需求序列或盤面高度, 由數值提案、製作人定。
- **消除回收的值(待數值以實測式重推)**: v17 把回收比例 r 拆成消除回收與削頂每格回收, 後者 v20 已有實測值 1.87, 前者待「鎖定後消除前」欄位校正; 局長與死局率的推算都建立在它上面。
- **多樣化任務需求量這個局長旋鈕的淨方向**: 順位已由 v20 定為第 3; 但需求量同時決定新外型與落速階梯(加壓)以及削頂與第 5 個清色球來得多早(減壓), 往下調的淨效果不是單向, 待數值判斷。
