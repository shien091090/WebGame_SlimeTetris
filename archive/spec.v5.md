# Demo 規格 v5

## 概述
一句話概念: 在一個窄到讓人難受的盤面上疊史萊姆, 把同色黏成一團消掉, 換來盤面向左右延展, 玩家自己把牢房拓成球場。

Demo 要驗證的問題: 在同色連鎖消除的盤面上, 左右解鎖延展能不能讓玩家為了開地而刻意選擇要消哪個顏色, 而不是只挑最容易消的那團?

本規格只描述玩法規則與體驗流程。基本規則(同色四方向相連 4 顆 + 連鎖)自 v3 起不變。**本版相對 v4 的唯一大改是把整套驗證裝置移出 Demo**: 三隻 bot、前置條件、主判定門檻、供給基線與機會標記等統計設計全部刪除, 改為 console 輸出 + 老闆試玩 + 三個觀察項(見「埋點與試玩觀察」)。數值參數表同步改為 decisions v5 的拍板值; 這些數字是推算值, 未經實測, Demo 跑起來後以試玩體感為準調整, 規則層不受影響。

## 核心循環
1. 玩家接到一個混色四格方塊(每格顏色各自獨立), 同時看得到下一塊的顏色組成、左右兩側當前目標色, 以及本局哪一側是昂貴側。
2. 玩家放下方塊; 盤面穩定後, 同色史萊姆以上下左右相連達 4 顆的團整團消失, 上方史萊姆沉降, 若再度成團則自動連鎖。
3. **取捨寫死在這裡, 分兩層**:
   - **第一層(主層, Demo 的驗證問題建立在這層)**: 放置當下決定**讓哪一色跨過 4 顆**。一塊四格只有一個落點與一個旋轉態, 把黃格補成第 4 顆黃, 就放棄了同一塊把藍格補成第 4 顆藍, 兩者空間互斥。第一層的另一半是「要不要為了目標色, 繞開已經成形的非目標色團」。
   - **第二層(較輕)**: 在即將消除的團**上方**預先堆同色, 沉降後自動成團 — 這是連鎖的事前規劃, 是本作唯一的真槓桿。
4. 消掉的每一顆目標色史萊姆, 讓該側解鎖數 −1(連鎖中消掉的照算); 歸零則該側向外延展一欄, 新欄全空。
5. 盤面變寬 → 容量變大、更耐堆、也更放得下兩組不同色的布局 → 玩家活得更久, 走得到下一次解鎖。
6. 兩側解鎖門檻刻意不對稱, 哪側昂貴每局隨機, 玩家必須決定「先拿便宜那側早點變寬, 還是先啃貴的免得來不及」。
7. 落速持續上升。真正的收局不是落速本身, 而是速度快到玩家來不及分色, 消除效率自己崩掉, 雜色堆積壓垮盤面。

**規則上不存在「把團留著繼續長大」這件事, 任何文件不得寫成那樣**: 團一碰到 4 顆立刻整團消失, 局面上永遠不存在 ≥4 顆的團。n≥5 只能靠單次放置一口氣跨越產生。本作所謂「更大」只能指**連鎖段數**, 不得用來指團的顆數。

## 規則

### 盤面與延展
- 盤面初始 6 欄寬、16 列高, 左右各可解鎖 2 欄, 滿寬 10 欄。
- **列座標系**: 列 1 為最底列, 列 16 為盤面頂列(以上為不可見)。列 17、18 為緩衝列。**列 19 以上不設硬界**: 盤面在邏輯上向上無限延伸, 列 17 起的所有格位一律不可見, 但與盤面格位一視同仁, 可構成連通團、可被消除、也參與沉降。此設計讓旋轉永遠不會因為「頂到天花板」而失敗(見「方塊與下落」)。
- **欄座標系(寫死)**: 開局 6 欄的最左欄固定為**絕對欄 0**、最右為絕對欄 5。左側延展依序新增絕對欄 −1、−2; 右側延展依序新增絕對欄 6、7。**絕對欄全局不重編號**。**相對欄 = 絕對欄 − 當前最左欄**(故相對欄恆為 0 ~ w−1, w 為當前寬度)。規則文字中談「最左/最右」一律指相對欄。
- 左側與右側各有獨立的解鎖進度, 互不共用計數。
- 每側解鎖分 2 階, 一局最多經歷 4 次延展。**「中位玩家會經歷 4 次」是驗證問題成立的前提, 是數值必須保證的目標值, 不是描述。**
- **解鎖計數為倒數的剩餘顆數**: 每階開始時設為該階需求顆數, 每消掉 1 顆該側目標色即 −1, 歸零時該階達成。
- **兩側需求不對稱**: 一側為便宜側(第 1 階 7 顆、第 2 階 11 顆), 另一側為昂貴側(第 1 階 10 顆、第 2 階 16 顆)。哪一側拿到昂貴配置於每局開始時等機率隨機決定, 全局固定不變, 且全程可見。左右的最大解鎖欄數仍對稱(各 2 欄)。
- **溢出顆數不捨棄**: 單次結算中跨過當階需求時, 超出的顆數直接扣抵該側下一階的剩餘顆數。該側已滿級時, 其後消掉的該側目標色改按滿級兌分率換算為分數。
- **單次結算中「每一側」最多延展 1 欄**。兩側在同一次結算各自達標時, **兩側各延展 1 欄, 本次結算盤面總寬 +2**; 這是允許的。被限制的只有「同一側在同一次結算連續延展 2 欄以上」。
- **延後的延展由每一次鎖定後的結算補檢查, 不需要該次結算有任何消除發生**(見「結算時序」第 8 步), 以避免玩家長時間消不掉東西時延展永遠卡住。
- **延展被延後期間的狀態(寫死)**: 解鎖進度與計分一律以「當前階」即時處理, 不設待處理池 — 該階剩餘歸零即視為達成, 溢出顆數立刻進入下一階的剩餘; 若該側已無下一階, 其後顆數按滿級兌分率計分。**目標色的重抽綁定「延展實際執行」的那一刻, 不綁「達標」那一刻**: 延後期間該側目標色維持不變、繼續顯示, 期間消掉的該色照常扣抵下一階。進度顯示為「已滿, 待延展」。
- **新增的欄位全空, 不補任何格位**。既有格位不位移, 絕對欄與高度皆不變。
- **延展執行必須是一個獨立、明確、可單獨辨識的事件(寫死)**: 它在時序上與消除、沉降完全分離(結算時序第 9 步), 有自己的起點與終點, **不得與消除結算併在同一個回饋裡、不得與沉降同時發生、不得被連鎖的呈現蓋過**。兩側同時延展時視為兩個事件, 依先左後右依序執行與呈現, 不合併為一次。理由: 這是玩家「我開了一欄」的唯一回饋來源, 也是新手讀懂延展價值的唯一途徑; 混進消除結算就會被當成消除的附帶效果。
- 兩側都達成第 2 階後盤面固定 10 欄, 不再延展。

### 目標色與顏色
- 顏色共 3 種(**禁改**)。左右兩側各常駐一個目標色, 兩側目標色恆不相同, 且兩者全程可見。
- **開局初始化順序(寫死)**: 先等機率決定昂貴側在左或在右 → 自 3 色等機率抽左側目標色 → 自剩下 2 色等機率抽右側目標色。順序固定先左後右, 與延展重抽共用同一套「排除另一側當前色、先左後右」的邏輯, 不另立規則。
- 目標色固定不變, 直到該側的延展實際執行; 執行延展時重抽該側目標色, 從「不等於另一側當前目標色」的 2 色中等機率抽 1(可能抽回原本那色)。
- **兩側於同一次結算同時延展時, 重抽序列化執行, 順序固定為先左後右**: 先抽左側(排除右側當前色), 再抽右側(排除左側剛抽定的新色)。此順序保證重抽後兩側必不同色。
- 某側達到滿級(第 2 階的延展已執行)後, 該側目標色凍結並繼續顯示, 不再重抽。
- 方塊的每一格顏色獨立, 由顏色 bag 依序填入: **每 18 格為一個 bag, 內含各色各 6 格**, 洗牌後依序發放, 用盡再洗下一個 bag。
- 方塊形狀使用標準 7 種四格形狀, 以 7-bag 發放; 形狀與顏色兩個 bag 各自獨立。
- **重開一局時, 顏色 bag 與形狀 bag 一律清空重洗**, 不延續上一局的殘餘序列, 兩個 bag 各自重新洗牌。

### 消除與連鎖
- **成團定義**: 同一顏色的格位以上下左右四方向相連構成一個連通團。**對角相鄰不算相連**。
- 連通團的格數 ≥ 4 時, 該團成立消除, 整團(不只 4 顆)全部消失。
- **緩衝列與其上的不可見區全程參與成團判定與沉降(寫死)**: 不因不可見而享有任何豁免。此條與「lock out 以殘存格位判定」是同一個設計意圖: 保留「從高位靠連鎖救回來」的可能, 那是拿掉團大小加成後連鎖唯一剩下的救命價值。
- **判定時機為盤面穩定後統一判定**: 方塊鎖定或一次沉降完成後, 掃描全盤(含不可見區)所有連通團, 一次性標記所有成立的團並同時消除。不逐顆落定即判。
- 同一次判定中若有多個成立團(含不同顏色), 一律同時消除, 不分先後。
- **沉降規則**: 消除後, 每一欄中位於空洞上方的格位逐格向下壓實, 保持該欄內的相對上下順序; 不做連通塊整體落下, 不做橫向滑落。
- **連鎖**: 一次沉降完成後重新掃描全盤, 若再有成立團則自動消除並再次沉降, 如此反覆直到掃描不到成立團為止。連鎖段數不設上限。觸發第 1 次消除的那一段記為第 1 段, 其後依序遞增。
- **連鎖只放大分數, 不額外加成解鎖進度**。連鎖中消掉的目標色仍以「每顆 −1」計入解鎖, 沒有任何倍率。
- **一個團的顏色若是左側目標色則只計左側, 是右側目標色則只計右側**。兩側目標色恆不同, 同一團不可能同時計入兩側。
- **同一次結算中同時消掉左側目標色團與右側目標色團時, 兩側各自按自身顆數計數, 互不排斥、互不折抵**。
- 非目標色的團正常消除、正常計分, 但不貢獻任何解鎖計數。
- 鎖定的方塊本身若與既有格位構成 ≥ 4 的同色團, 照常成立消除; 不存在「剛落下的格位免疫一次判定」這種保護。
- **團的顆數不帶任何額外加分**(基礎分純線性)。

### 死局與盤面耗盡
- **本作不存在任何保底清除手段**。整列填滿不會消除, 也沒有全盤下沉、定時清列、雜色轉色等補償。
- 盤面填滿但尚未觸發結束條件時, 遊戲照常繼續: 方塊持續生成、持續下落, 直到滿足 block out 或 lock out。
- **不做「無處可放即結束」的提前判定**。
- 玩家可隨時以「放棄本局」主動結束已無救的一局, 該局照常結算, 但標記為放棄局。

### 結算時序
方塊鎖定後依下列順序執行, 順序寫死不可調換:
1. 方塊鎖定, 進入結算狀態, **鎖住玩家所有方塊操作輸入**(僅暫停與放棄長按仍受理, 暫停的生效時點見下)。記錄本次鎖定方塊的 4 個格位為「本塊格位集合」。
2. 令連鎖段數 k = 1。
3. 掃描全盤(含不可見區)所有同色連通團, 取出所有格數 ≥ 4 的團。**無成立團則跳至第 8 步**。
4. 對本段所有成立團累加各團分數(乘上第 k 段的連鎖倍率), 並將屬於某側目標色的團顆數逐顆扣減該側剩餘解鎖顆數。**分數倍率取本次結算開始時的已解鎖欄數**, 本次結算中若發生延展, 新欄不計入本次倍率。同時把本段消掉的格位自「本塊格位集合」中移除。
5. 一次性清除本段所有成立團的格位, 執行沉降, 等待沉降完成。
6. k ← k + 1, 回到第 3 步。
7. (連鎖結束)
8. 依序檢查左側、右側解鎖是否達成(**先左後右**), **每側最多處理 1 階**。此步每次結算都執行, 即使本次結算沒有任何消除 — 延後的延展在此補上。
9. 對達標側執行延展: 向外新增 1 欄, 全空, 盤面寬度 +1。**延展在此作為獨立事件執行**, 與第 4、5 步的消除結算分離; 兩側同時達標時, 依先左後右各執行 1 次, 兩次事件不合併。
10. 對本次有完成延展的側執行目標色重抽(先左後右); 已滿級側不重抽。
11. **結束判定(block out 與 lock out 在此同一步一起判)**; 未結束則解除輸入鎖定並生成下一塊。
- **結算期間本局經過時間 t 照常推進**, 落速不凍結。
- **結算期間(含連鎖、沉降、延展、重抽)所有方塊操作輸入一律無效, 不緩衝不排隊**; 僅暫停與放棄長按受理。**按下暫停一律排隊, 於第 11 步「解除輸入鎖定」的那一刻才生效**, 不中斷連鎖、不打斷延展或重抽; 若該次結算即判定遊戲結束, 排隊中的暫停請求作廢。**放棄長按則在結算期間照常累計與生效**(它的語意是「我不玩了」, 沒有等結算的理由), 生效時立即結束本局並中止後續結算步驟。

### 方塊與下落
- 方塊於緩衝列(列 17、18)生成, 其 4 格外框的**左緣相對欄 = ⌊(w − 形狀寬) / 2⌋**(w 為當前盤面寬度, 偶數寬度時向左取整)。延展後生成位置依新寬度重算。
- 方塊依當前落速自動下落; **落速 = min(3.0 × 2^(t / 85), 30) 格/秒**, t 為本局經過秒數(暫停期間不推進)。
- **更新粒度**: 固定更新步長 1/60 秒。**單一更新步內的處理順序寫死如下**:
  1. 取本步輸入, 依 DAS/ARR 產生至多 1 次橫移與至多 1 次旋轉, **橫移先於旋轉**; 若本步有硬降輸入, 於此處立即執行硬降並跳過本步的步驟 2。
  2. 依落速累積下落距離, 單步可下落多格但**逐格判定碰撞, 不得穿透**; 碰到障礙即停在該格。單步最大下落格數上限為盤面高度 16。
  3. 判定是否接觸下方障礙, 處理鎖定延遲的進入、重置與逾時鎖定。
  - 此順序的結論: 同一步內玩家的橫移與旋轉永遠先於該步的下落結算。高落速下不會出現「玩家已經按了移動, 卻在移動生效前就被判定落地鎖定」。
- 方塊接觸下方障礙時進入鎖定延遲 0.5 秒; 期間任何成功的移動或旋轉重置計時, **單一方塊最多重置 1 次**, 超過即立刻鎖定。
- **軟降仍走完整鎖定延遲**, 與自然下落相同; 只有硬降跳過鎖定延遲。硬降時方塊立刻落到最低可行位置並即時鎖定。
- **簡化旋轉踢牆**: 原地旋轉若與牆或既有格重疊, 依序嘗試向左平移 1 格、向右平移 1 格; 任一可行即採用該位置完成旋轉, 兩者皆不可行則該次旋轉失敗。不做完整 SRS 踢牆表, 不做上推。
- **旋轉向上溢出不構成失敗(寫死)**: 盤面上方不設硬界, 旋轉後格位落到列 19 以上一律合法, 該處只是尚無格位的不可見空間。I 形在生成高度轉成垂直態即屬此情形, 照常成立。若該塊在有格位位於列 18 之上時就被鎖定, 那些格位照常留在盤面上、照常參與成團與沉降, 並由 lock out 判定收局。
- 每格顏色隨旋轉一同轉動。
- **O 形方塊的旋轉不是 no-op(寫死)**: O 形外框位置與佔格不變, 但其 **2×2 顏色矩陣必須繞中心真實旋轉 90 度**, 逆時針鍵則反向, 180 度鍵為對角互換。O 形佔 7-bag 的 14.3%, 且是唯一能把 4 格完整塞進單一 2 欄色道的形狀; 做成 no-op 等於這 14.3% 的塊玩家完全喪失顏色方位控制。
- **提供 180 度旋轉鍵**。鎖定重置降到 1 次後精修預算變緊, 靠連按兩次 90 度會吃掉整個預算; 本作要測的是決策, 不該被執行門檻遮住。180 度旋轉同樣適用簡化踢牆(先原地, 再左 1, 再右 1)。
- 不提供暫存(hold)。

### 暫停
- 暫停期間經過時間 t、落速、方塊狀態全部凍結。
- **暫停必須完全遮蔽盤面**, 使盤面內容不可讀; 下一塊預覽、目標色、解鎖進度同時遮蔽。恢復後才重新顯示。
- 暫停期間不接受任何方塊操作輸入, 只接受恢復與放棄長按。

### 埋點與試玩觀察
Demo 不做任何統計判定、不做 bot、不設判定門檻。只留 console 輸出供製作人讀, 結論由老闆試玩的體感給。

**console 輸出兩種, 全部必做**:
- **每局結束彙整一則**: 總塊數、總秒數、硬降使用率、四次延展各自的塊數與秒數、已解鎖欄數、消除總顆數、其中目標色佔比、連鎖段數分佈、消除團大小分佈(n=4 / 5 / 6 / 7+)、各欄最終高度、結束原因(block out / lock out / 玩家放棄)、最終分數。
- **每次延展輸出一行**: 第幾次、哪一側、當時塊數與秒數、當時盤面寬度。

**試玩要看的三件事**:
1. **玩家有沒有為了解鎖而選色**: 試玩後問「你有沒有為了開左邊那欄, 刻意去消某個顏色?」若答不出具體一次, 就是沒發生。
2. **四次延展走不走得完**: 讀 console 的延展塊數; 走不完就降解鎖需求。
3. **窄盤面有沒有壓迫感、變寬有沒有爽感**: 這是概念的賣點, 沒有就白做。

## 操作

| 輸入(鍵盤) | 行為 |
| --- | --- |
| 左方向鍵 / A | 方塊左移一欄 |
| 右方向鍵 / D | 方塊右移一欄 |
| 上方向鍵 / W | 順時針旋轉 90 度(含簡化踢牆; O 形真實旋轉顏色矩陣) |
| Z | 逆時針旋轉 90 度(含簡化踢牆; O 形真實旋轉顏色矩陣) |
| X | **旋轉 180 度**(含簡化踢牆; O 形為 2×2 顏色矩陣對角互換) |
| 下方向鍵 / S | 軟降, 下落速度為 **max(20 格/秒, 當前落速)**; 仍走完整鎖定延遲 |
| 空白鍵 | 硬降並立即鎖定 |
| 左右方向鍵長按 | 首次移動後延遲 100 毫秒(DAS)開始連續移動, 每 33 毫秒(ARR)一欄 |
| 下方向鍵長按 | 持續軟降, 放開即恢復當前落速 |
| Esc | 暫停 / 恢復(暫停時凍結並遮蔽盤面) |
| R(遊戲結束時按下) | 立即重開一局 |
| R(遊戲進行中、結算中或暫停時長按 1 秒) | 放棄本局: 立即結束, 結束原因記為「玩家放棄」, 照常結算輸出但不更新最佳紀錄, 停在結束畫面等玩家重開。未滿 1 秒放開則取消 |

| 輸入(滑鼠) | 行為 |
| --- | --- |
| 移動滑鼠 | 方塊橫向跟隨游標所在欄(對齊基準為方塊 bounding box 的中心欄, 寬度為偶數時向左取整), 逐欄移動且遇阻擋即停; **每欄移動套用與鍵盤相同的 DAS 100 毫秒 / ARR 33 毫秒**, 游標瞬間大幅橫移時方塊依 ARR 逐欄追上, 不瞬移 |
| 左鍵點擊 | 順時針旋轉 90 度(含簡化踢牆) |
| 中鍵點擊 | **旋轉 180 度**(含簡化踢牆) |
| 右鍵點擊 | 硬降並立即鎖定 |
| 滾輪向下 | 軟降一格 |
| 滾輪向上 | 逆時針旋轉 90 度(含簡化踢牆) |
| 左鍵點擊結束畫面的重開區域 | 等同結束時按 R, 立即重開一局 |

- **鍵鼠仲裁(寫死)**: 滑鼠橫移**只在「游標所在欄索引相對前一幀發生變化」的那一幀產生輸入**; 游標靜止時不產生任何輸入。因此鍵盤把方塊移到定位後, 不會被停在別處的游標拉回原位。下一次游標實際位移時, 滑鼠以當時游標欄為目標欄重新接管。旋轉、硬降、軟降類的離散輸入不受此限, 鍵鼠皆即時生效, 後發生者覆蓋前一個。
- **放棄本局後須先放開按鍵(寫死)**: 放棄長按滿 1 秒生效的瞬間, R 鍵進入鎖住狀態, **必須先偵測到該鍵放開, 才重新接受重開輸入**; 結束畫面對 R 的重開只認 key-down 邊緣。這避免長按的餘程直接觸發重開, 讓玩家看不到結束畫面。以滑鼠點擊重開區域不受此限。
- 落點指示(ghost)**全程顯示**, 標出方塊硬降後會停在的位置, **並呈現每一格的顏色**(玩家必須能在落點上直接讀出哪格是哪色, 否則第一層取捨要靠心算色位, 變成執行門檻而非決策); **不顯示任何「這樣放會消掉幾顆 / 會不會成團」的預測資訊**。ghost 不設開關。
- 暫停期間除 Esc 與放棄長按外所有輸入無效。

## 勝負與結束條件
- 本作無勝利條件, 只有結束與分數。
- 結束條件三者之一成立即結束:
  1. **Block out**: 下一塊實際生成的那一刻, 其 4 個生成格位中任一格與既有非空格重疊。
  2. **Lock out**: 本次鎖定方塊的**殘存格位全部位於盤面頂列(列 16)之上**。**殘存格位 = 本塊 4 格中, 在本次結算的全部連鎖結束後仍留在盤面上的格位**; 本次結算中被消掉的格位不計入。若 4 格全部被消掉, **不成立 lock out**。
  3. **玩家放棄**: 長按 R 滿 1 秒。
- **結束判定統一發生在「結算時序」第 11 步**, block out 與 lock out 同時點判定, 不在方塊鎖定的當下先判 lock out。理由: 先判會砍掉局末 3~6 塊, 且會否定「靠連鎖從高位救回來」。
- **block out 不做預判(寫死)**: 不在本塊鎖定時提前用下一塊的形狀去試算生成位置, 也不在延展前後預演。判定發生在第 11 步「生成下一塊」時, 以**本次結算後的盤面與寬度**計算該塊生成位置後檢查重疊; 重疊即結束, 該塊不進入可操作狀態。預判會讓玩家在還能操作的一塊上就被判死, 且與「靠連鎖救回來」的設計意圖相衝。
- **盤面填滿、無任何可消團、玩家已無可行落點, 都不構成獨立的結束條件**, 一律靠上述三條收局。
- 不設遊戲內時間上限。落速上限 30 格/秒 在目標時長區間內不會被觸及, **收局主力是「速度快到來不及分色 → 消除效率崩潰 → 雜色堆積壓垮盤面」**, 不是落速本身。
- 結束時凍結盤面, 顯示本局分數、存活秒數、已解鎖欄數、結束原因, 並與最佳紀錄比對。
- 最佳紀錄只保留單一筆, 以分數為比較基準; 本局分數較高則覆寫。**玩家放棄的局次不更新最佳紀錄**。
- 結束後可立即重開一局, 盤面、寬度、解鎖進度、目標色、昂貴側配置、速度、兩個 bag 全部重置並重新隨機。

## 數值參數表

標「結構參數」者為規則骨架, 不因調校變動。其餘為拍板的初始值, **皆為推算值、未經實測**; Demo 跑起來後依試玩體感調整。

| 參數 | 值 | 說明 |
| --- | --- | --- |
| 畫面寬 | 960 | 邏輯像素, 需同時容納滿寬 10 欄盤面與左右兩側的解鎖資訊 |
| 畫面高 | 640 | 邏輯像素, 需容納 16 列盤面與上方分數/預覽區 |
| 格子邊長 | 28 | 邏輯像素, 滿寬盤面 280 寬 × 448 高 |
| 初始欄數 | 6 | 開局盤面寬度, 絕對欄 0~5 |
| 每側最大解鎖欄數 | 2 | 結構參數, 左右對稱 |
| 滿寬欄數 | 10 | 兩側皆滿級後的寬度, 絕對欄 −2~7 |
| 盤面高度 | 16 | 列; 本作無整列消除, 高度是唯一的被動存活緩衝。**高度底線 12, 不得再低** |
| 緩衝列數 | 2 | 列 17、18; 其上不設硬界。不可見區全程參與成團判定與沉降 |
| 消除門檻 | 4 顆 | 結構參數, 四方向相連, 不含對角 |
| 連鎖上限 | 無 | 結構參數 |
| 連鎖對解鎖的加成 | 無 | 結構參數, 連鎖中消掉的目標色仍逐顆計 1 |
| 解鎖計數方式 | 逐顆 −1 | 結構參數 |
| 新解鎖欄內容 | 全空 | 結構參數, 不補惰性格 |
| 顏色數 | 3 | 結構參數, **禁改**。4 色會讓消除效率明顯下滑、最小可行寬度上升 |
| 方塊形狀 | 標準 7 種四格形狀 | 結構參數, 每格顏色獨立; O 形旋轉須真實旋轉 2×2 顏色矩陣 |
| 顏色 bag 粒度 | 18 格 | 每 bag 含各色各 6 格 |
| 形狀 bag 粒度 | 7 塊 | 標準四格形狀各 1 |
| 便宜側解鎖需求 | 7 / 11 顆 | 第 1 階 / 第 2 階, 該側目標色顆數 |
| 昂貴側解鎖需求 | 10 / 16 顆 | 第 1 階 / 第 2 階; 四階總計 44 顆 |
| 昂貴側歸屬 | 每局等機率隨機 | 左或右, 全局固定且可見 |
| 開局目標色抽法 | 先昂貴側 → 左 → 右 | 昂貴側等機率; 左側自 3 色等機率; 右側自剩 2 色等機率 |
| 溢出顆數處理 | 保留 | 跨階溢出立即帶入下一階剩餘; 該側已滿級則按滿級兌分率換分 |
| 單側單次結算最大延展 | 1 欄 | 兩側同時達標時各延展 1 欄, 本次總寬 +2 |
| 延後延展的補檢查 | 每次結算 | 不需該次有消除發生 |
| 初始落速 | 3.0 格/秒 | |
| 加速免疫時間 | 0 秒 | 落速自 t=0 起即依曲線上升 |
| 速度倍增週期 | 85 秒 | 落速 = 3.0 × 2^(t/85) |
| 落速上限 | 30 格/秒 | 約 t≈282 秒觸頂, 在目標時長區間內不會被觸及 |
| 更新步長 | 1/60 秒 | 單步可落多格, 逐格判定碰撞, 單步上限 16 格; 步內順序為 輸入(橫移→旋轉) → 下落 → 鎖定延遲處理 |
| 鎖定延遲 | 0.5 秒 | 軟降同樣適用; 硬降不適用 |
| 鎖定延遲最大重置次數 | 1 | 收局機制之一: 高落速段收緊精修預算。若試玩回報「後段手感斷崖」, 可退的中間檔是 2 次 |
| 旋轉踢牆試位 | 左 1 格 → 右 1 格 | 依序嘗試, 皆失敗則旋轉不成立; 90 度與 180 度共用。向上溢出不算失敗 |
| 軟降速度 | max(20 格/秒, 當前落速) | 取代舊的「當前落速 ×20 且不低於 20」 |
| 長按起始延遲(DAS) | 100 毫秒 | 鍵盤與滑鼠橫移共用 |
| 長按重複間隔(ARR) | 33 毫秒 | 鍵盤與滑鼠橫移共用 |
| 放棄本局長按時間 | 1 秒 | 防誤觸; 生效後須放開 R 才接受重開 |
| 單團基礎分 | 25 × n | n 為該團顆數, **純線性, 無任何大團加成** |
| 連鎖倍率級距 | 1.00 / 1.25 / 1.57 / 1.97 / 2.48 | 第 1~5 段, 第 6 段以上一律 2.48。**必須寫死查表**: 表值公比實為 1.2545, 用 1.25^(k−1) 生成會讓第 3~5 段全錯 |
| 同段多團處理 | 各團分開計基礎分, 同乘該段連鎖倍率 | |
| 解鎖顆數分數 | 5 分/顆 | 未滿級側每消掉 1 顆該側目標色另計分 |
| 滿級兌分率 | 5 分/顆 | 與解鎖顆數分數同率。滿級後該色已無解鎖功能, 給更高費率沒有設計理由 |
| 費率切換時點 | 無差別 | 兩率同為 5 分/顆, 跨滿級的那批顆數不需區分, 全批 5 分/顆 |
| 分數倍率 | 1.0 + 0.15 × 已解鎖欄數 | 已解鎖欄數 0~4, 倍率 1.0~1.6。取值下限受限於「走完 4 次延展的期望分數 ≥ 只在窄盤面刷連鎖的期望分數」, 破了這條最優解會變成不開地 |
| 本次結算得分公式 | { Σ_段k [ (Σ_團 25 × n) × 連鎖倍率(k) ] + 該側目標色顆數 × 5 } × 分數倍率 | 分數倍率取本次結算開始時的已解鎖欄數 |
| 軟降/硬降分數 | 0 | 不給分, 避免鼓勵無意義硬降刷分 |
| 目標單局時長 | 中位 150~180 秒 | 硬上限 300 秒; 時長主要由玩家按不按硬降決定, 觀測一律以塊數為主軸 |
| 預期延展時點(中位玩家) | 第 11 / 15 / 27 / 39 塊 | 推算值, 與 console 的延展塊數對照用 |
| 預期結束時點 | 約第 70 塊 | 推算值; 四個技能檔次都應走得完 4 次延展 |

## 物件清單

| 物件 | 狀態 | 說明 |
| --- | --- | --- |
| 盤面格位 | 空 / 已填(色 A/B/C) / 消除標記中 | 玩家需辨識顏色; 消除標記中為已判定成團、等待清除的短暫狀態 |
| 同色團 | 未達門檻(1~3 顆) / 成立消除(≥4 顆) | 四方向相連; 局面上不存在「已達 4 顆但未消」的持續狀態 |
| 盤面邊界 | 寬度 6 / 7 / 8 / 9 / 10 | 當前可放置範圍, 延展時向外推; 絕對欄不重編號 |
| 不可見區 | 不可見 | 列 17 以上; 供生成與 lock out 判定, 參與成團判定與沉降, 上方不設硬界 |
| 落下方塊 | 下落中 / 軟降中 / 鎖定延遲中 / 已鎖定 | 由 4 個各自帶色的格位組成; 旋轉態 4 種(O 形亦有 4 種顏色態) |
| 落點指示(ghost) | 顯示中 | 全程顯示, 標示硬降後的停駐位置, **並呈現每一格的顏色**; 不含成團預測, 無開關 |
| 下一塊預覽 | 顯示中 / 遮蔽中 | 需能讀出形狀與每格顏色組成; 暫停時遮蔽 |
| 左側目標色指示 | 色 A / B / C | 常駐, 暫停時遮蔽 |
| 右側目標色指示 | 色 A / B / C | 常駐, 恆與左側不同, 暫停時遮蔽 |
| 昂貴側指示 | 昂貴側在左 / 昂貴側在右 | 每局開局決定, 全程常駐可見 |
| 左側解鎖進度 | 第 1 階剩餘 N 顆 / 第 2 階剩餘 N 顆(承接上階溢出) / 已滿待延展 / 滿級 | 以剩餘顆數呈現, 每消 1 顆該側目標色即 −1 |
| 右側解鎖進度 | 第 1 階剩餘 N 顆 / 第 2 階剩餘 N 顆(承接上階溢出) / 已滿待延展 / 滿級 | 同上 |
| 連鎖段指示 | 隱藏 / 顯示第 k 段 | 結算期間指出目前為第幾段連鎖與該段倍率 |
| 消除結算標記 | 出現 / 消失 | 標出被消的團屬於哪一側目標色、扣了幾顆(事後回饋, 非事前提示) |
| 延展事件指示 | 出現 / 消失 | 延展實際執行時獨立呈現, 指出哪一側正在延展; 兩側同時執行時為兩次依序出現的事件, 不與消除結算標記同時出現 |
| 延展待執行指示 | 出現 / 消失 | 某側已達標但延展被延到下次結算期間持續顯示 |
| 目標色重抽提示 | 出現 / 消失 | 延展執行後指出該側目標色已重抽, 以及新色為何 |
| 暫停遮罩 | 出現 / 消失 | 暫停期間覆蓋盤面與所有情報區, 使內容不可讀 |
| 放棄計時指示 | 出現 / 消失 | 長按 R 期間顯示剩餘時間, 放開即消失 |
| 分數 | 數值 | 即時更新 |
| 最佳紀錄 | 數值 / 尚無紀錄 | 單一筆, 放棄局不更新 |
| 經過時間 | 數值 | 本局秒數, 暫停不推進, 結算期間照常推進 |
| 遊戲狀態 | 進行中 / 結算中 / 暫停 / 結束 | 結算中為連鎖判定、沉降、延展、重抽期間, 輸入鎖定 |
| 結束資訊 | 隱藏 / 顯示 | 結束時呈現分數、存活秒數、已解鎖欄數、結束原因、是否破紀錄 |

## Demo 範圍邊界
- 不做: **bot、統計判定門檻、分層檢定、多人樣本計畫、供給基線與機會標記等量測裝置** — 全部移出 Demo, 改為 console 輸出 + 老闆試玩。理由: 它已反過來決定遊戲參數, 交付成本大於遊戲本體, 而本工作室的驗證迴圈實際上就是老闆試玩。出貨前評估要不要做再議
- 不做: **整列填滿消除** — 完全移除, 不與同色消除並存。並存會讓顏色重新淪為消行的副產品
- 不做: **任何保底清盤機制** — 定時清列、全盤下沉、雜色轉色、「卡住就清最底列」全部否決。保底會讓玩家不必為顏色做決定也能活
- 不做: **消除門檻降到 3 顆** — 門檻 3 下約 58% 的方塊落地即消, 「要消哪個顏色」的決策直接消失
- 不做: **大團加成 / 任何依團顆數遞增的額外計分** — 佔總分不到 3%, 且 n 由完成那一塊的色組成決定, 玩家拉不動
- 不做: 對角相鄰成團 — 只認上下左右四方向
- 不做: 連鎖加成解鎖進度 — 解鎖速度必須可預測
- 不做: 惰性格與任何新欄補償
- 不做: 局間成長、收藏、關卡地圖等元 progression
- 不做: 速度以外的第二套壓力來源 — 道具、障礙塊、特殊史萊姆一律不做
- 不做: 每側 3 階以上的完整解鎖階梯與出貨用難度曲線, Demo 只做到 2 階
- 不做: **暫存(hold)機制** — 會大幅降低排色的規劃難度。**其死局緩解量記為已放棄的一項, 日後若死局率過高不得拿它重新爭論**
- 不做: 連消(combo)、Back-to-Back 等額外計分規則
- 不做: 難度選擇、起始等級選單 — 速度曲線全體玩家一致
- 不做: 排行榜、雲端紀錄、帳號 — 最佳紀錄只存單機單筆
- 不做: 教學關卡與引導流程 — Demo 以口頭說明帶入, 避免引導影響玩家是否「自發」為解鎖選色
- 不做: 目標色的手動指定或重抽功能
- 不做: **玩家自由指定消除歸屬** — 消掉哪個顏色就算哪一側, 不可改派
- 不做: **預測性的即時提示** — 事前的「會不會成團」提示不做; 消除後標出哪些團屬於哪側是學習回饋, 可以做
- 不做: **ghost 開關** — ghost 固定全程顯示且顯示每格顏色, 不列為變因
- 不做: **完整 SRS 踢牆表** — 只做左右各一格的簡化試位, 不做上推
- 不做: **O 形旋轉做成 no-op** — 必須真實旋轉其 2×2 顏色矩陣
- 不做: **暫停時看盤** — 暫停必須遮蔽盤面, 否則玩家可靠暫停慢慢規劃
- 不做: 暫停期間的盤面操作
- 不做: **同一側在單次結算連續延展 2 欄以上** — 兩側各延 1 欄使總寬 +2 是允許的, 不在此限
- 不做: **延展與消除結算合併呈現** — 延展必須是獨立事件
- 不做: 橫向滑落 / 連通塊整體落下等進階沉降 — 只做逐欄壓實
- 不做: 遊戲內時間上限 / 強制收局計時器
- 不做: block out 的預判 — 只在下一塊實際生成時判

## 待定
- 無
